{
  "name": "dsh-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "在 DeepSeek Harness (DSH) 底座上，将 agent-shell 工程思路解耦重做为 Cordis 插件：传送带上下文 / 三脑编排 / 工具自进化 / 护栏 / LLM 路由。",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "web": "dsh web",
    "plugin": "dsh plugin",
    "postinstall": "patch-package && node scripts/patch-web-app-public-url.mjs && node scripts/patch-app-boot-bom.mjs && node scripts/patch-goal-round-driver.mjs && node scripts/patch-profile-deps.mjs",
    "check:bom": "node scripts/check-bom.mjs",
    "check:all": "node scripts/check-all.mjs",
    "verify:p4": "node scripts/verify-p4-after-swap.mjs",
    "check:profile": "node node_modules/@deepseek-ai/dsh/lib/bin.js web --dump-config"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "^0.1.1-rc.2",
    "@dsh-brain/conveyor-context": "file:packages/conveyor-context"
  },
  "devDependencies": {
    "patch-package": "^8.0.1",
    "typescript": "^7.0.2"
  }
}
