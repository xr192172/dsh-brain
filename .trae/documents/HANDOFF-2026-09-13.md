# DSH Brain 项目交接文档（2026-09-13）

> 用途：换窗口继续开发。新窗口可先读本文件 + 【关键文档清单】里的目标文件，再按【待办】推进。
> 语言：中文。一次性给足路径，避免新窗口到处找。

## 1. 项目拓扑（一句话）

DSH（DeepSeek Harness，dsh-brain fork）做"自进化 + 三脑编排"。三脑 = 生产脑/开发脑(left) + 审查脑(right) + 实验脑(sandbox)。elv/dsh-hub 是那个本应统一管理三脑的 TS Hub（Go Hub 的翻译版）。

## 2. 关键文档清单（优先读这些）

| 路径 | 内容 |
|---|---|
| `d:\project_develop\dsh-brain\.trae\documents\wire-elv-hub-with-dsh.md` | **dsh 接入 Hub 总方案**（§1 架构 / §2 三实例 / §2A 单版本线 current+candidate / §2B 换代状态机契约 / §3 bridge 契约 / §3b effect / §6 验收）|
| `d:\project_develop\dsh-brain\.trae\documents\dev-task-wire-three-brains-to-hub.md` | **交给开发窗口的下一步开发任务**（T1 bridge 生产化 / T2 三实例分配 A/B / T3 前端统一管理视图 / T4 换代 catchup）|
| `d:\project_develop\dsh-brain\.trae\documents\elv-hub-translation-session-retro.md` | 爆仓会话 28f50f57 的复盘：它干了什么、怎么涨到 167K→462K（read 工具累出的）、压缩为何没救住 |
| `d:\project_develop\dsh-brain\.trae\documents\go-ts-translate-improvements.md` | design-canvas 的 Go→TS 翻译工具改进意见（A1-A4/B/C，开发窗口已基本采纳）|
| `d:\project_develop\elv\dsh-hub\docs\step3-three-brain-cycle.md` | elv/dsh-hub 的 gen.* 状态机 + mock 全绿 + 已知 catchup 阻塞 |
| `d:\project_develop\dsh-brain\docs\three-brain-evolution.md` | 三脑自进化架构文档 |

## 3. 正在修的压缩/上下文问题（重要 + 有明确历史）

**根因链（已解明）**：dsh 的 `agnes-2.5-flash` 之前被 `settings.yaml` 把 `contextWindow` 配成 128000 / `maxTokens` 8192，而官方真实是 **512K / 65536**。会话撑到 462K input + 65.5K completion > 512K → 400 死循环。
已改 `C:\Users\Admin\.dsh\settings.yaml` → `contextWindow: 512000 / maxTokens: 65536`。

**压缩引擎热补丁**（node_modules + patch-package 持久化）：
- 文件：`d:\project_develop\dsh-brain\node_modules\@deepseek-ai\dsh-compaction-basic\lib\index.js`
- 补丁：`d:\project_develop\dsh-brain\patches\@deepseek-ai+dsh-compaction-basic+0.1.1-rc.2.patch`（~15KB）
- postinstall: `patch-package`（已在 package.json scripts）
- 已做的修复（按时间序）：
  1. **C 确定性兜底**：summarizer 失败 → drop-oldest（不依赖模型，物理必缩）
  2. **A 落地即裁**：tool/result 落地超阈值即头尾+省略号（修过一版层级 bug：结果是裹在 `{type:"tool-result", content:[…]}`，需朝内层取）
  3. **B 递归分块折叠**：region 切块逐轮折叠，分隔用合法 user 消息（曾因塞裸 `{type:"text"}` 无 role 发坏 LLM，已修）
  4. **方案 A/阈值总账**：`accountForCompletion` 有效窗口 = `(contextWindow − maxTokens)`，再乘 0.85 吸收计量器低估
  5. **溢出门槛放宽**：400"exceeds maximum context length"也认成 overflow → 触发 B/C
  6. **防崩**：`measurement.nodes`/`surface.nodes` 为 undefined 时不再崩（配计量器兜底 + prune try/catch）

**待确认**：修复后是否还 400（用户需实测）。若还报，重点看新 input 数字。

## 4. 当前运行时状态

| 服务 | 地址/进程 | 状态 |
|---|---|---|
| switchboard | 31800（`?cmd=status/flow/panel`）| gen-3082 在跑 |
| dsh 前门 | 3080 | 200 |
| key-pool-proxy | **3101** | 挂在新 gen 内（AGENTSHELL 3 keys）|
| elv/dsh-hub | **15813**（pid 25184，"Agent-Shell Hub"前端）| 在跑；三脑全 `running:false`（未接真脑）|

**三脑现状**：hub 前端就绪 + gen.* 状态机已写 + mock(15817)全绿，但**三台真 dsh 没桥接**到 15813 → 用户看到的仍是"各用各的页面"。这是 elv/dsh-hub 侧 `dev-task-wire-three-brains-to-hub.md` 的核心任务。

## 5. 待办 / 下一步

1. **验证压缩修复**：对 462K 会话再发一句，看是否过 400。
2. **http://127.0.0.1:15813 三脑真接入**（给开发窗口）：T1 bridge 生产化（`elv/dsh-hub/scripts/hub-gen-bridge.mjs`）、T2 三实例分配、T3 前端统一管理视图。
3. **换代的 catchup 阻塞**（dsh-brain switchboard）：staging 追不平 active 的 `caughtUpSeq` → `b-catchup-failed`（gen-3090~92 屡败）；挡住真换代，需 dsh-brain 侧修。
4. 三脑 modelPolicies / 压缩参数可调（`C:\Users\Admin\.dsh\settings.yaml`）。

## 6. 复用手记

- **junction**：`~/.dsh/profiles/node_modules/@deepseek-ai/*` 是 junction 指向 `dsh-brain/node_modules/@deepseek-ai/*` —— **改后者即全局生效**。
- **重启 switchboard**（别用原 ps1，中文注释会让 PS5.1 解析崩）：用纯 ASCII launcher `C:\Users\Admin\AppData\Local\Temp\start-switchboard-ascii.ps1`（`powershell -NoProfile -ExecutionPolicy Bypass -File` 该路径，后台跑）。
- **fzstd**：`npm i fzstd --no-save` 后可解 `~/.dsh/sessions/*/session.jsonl.zstd`（多帧 zstd，用 `fz.decompress(全buffer)`）。
- 解 session：`C:\Users\Admin\AppData\Local\Temp\session-28f50f57.decoded.txt`（旧快照，如需最新用 fzstd 重解）。
- **设计画布**：`D:\project_develop\design-canvas`（已拉最新 + 编译）；MCP 走 `~/.dsh/profiles/web/cordis.patch.yml` 的 env（AGNES_UPSTREAM_BASE=3101）。