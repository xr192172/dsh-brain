# 现在到哪了 —— 一页给用户看的状态

> ★ **你只需要读这一页。** 技术细节在 `docs/skill-as-agent-spec.md` 与 `out/w*.md` 里，不用看。

---

## 一句话

★★ **Key 修好了、会话能正常跑了** —— 现在只差"换代释放那个索引句柄"和"开跑"。

## 刚做完的（key 那件事，已全部验证）

| 步骤 | 结果 |
|---|---|
| ① 定位 401 的根因 | ★ **那把 `DEEPSEEK_API_KEY`（尾 `c877`）其实是【AGNES 的 key】**（打 AGNES 200 / 打 DeepSeek 401）；而 DSH 配置里还写着不存在的 `deepseek-official` ⇒ 请求打到 DeepSeek 官方 ⇒ **必然 401** |
| ② 改 `~/.dsh/settings.yaml` | 默认模型 ⇒ **`agnes` / `agnes-2.5-flash`**（如实）；**备份 + 回退命令都在** |
| ③ 让池读到现有有效 key | 池的 fallback 里**加了 `DEEPSEEK_API_KEY`** ⇒ **不用你去设新环境变量** |
| ④ 你手动重启控制面 | ✅ 生效了 |
| ⑤ ★★ **真会话测试** | ✅ **完全跑通**：`Authentication Fails = 0`、`UNSUPPORTED_ = 0`、**`turn/end = completed`**、**它真的回了"收到收到"**；沙箱/审批事件也正常装载 |

★ 中途还修掉一处**我自己的疏漏**：`settings.yaml` 里的 **`reasoningEffort: high`** 是原来给 DeepSeek 写的，我改 provider 时没一并处理 ⇒ `agnes` 不支持 ⇒ 报错。**已去掉。**

## ⏸ 下一步（就 2–3 步）

1. ★ **换代**（释放那个索引句柄）—— 或按审计员的建议**把"两臂都冷"改成"起点一致"**（★ 因为跑会话本身就会建索引，"都冷"这个判据本来就会被破坏）
2. ★ **重跑"shell 能不能起"那条检查**（★ 现在会话能跑了，应该能过）
3. ★★ **开跑**：长程同族题 + 无泄漏 + 无混杂 ⇒ **拿第一次可信的读数**

## 📌 待做清单（按你说的"登记着，按紧要程度调"）

| # | 待做 | 紧要度 | 说明 |
|---|---|---|---|
| **格 ⑭** | ★★ **把控制面收窄成"纯启动器"** | **中**（不挡开跑） | 现在"模型接入 / key 池"装在**控制面与代【共用】的 profile** 里 ⇒ 改它要**重启控制面** ⇒ 这就是你说的"层级混乱/自指"。**正解**：控制面只做**交接**（spawn / 追平 / 冻结 / 翻转 / 验证 / 回滚 / 接手会话）；**模型接入、key 池属于"代的内容"** ⇒ 换代就能更新。★ 机制上已有口子（`?cmd=apply&profile=<名>`） |
| **O110 系列** | 让"提交前的门"从"约定"变成真约束 | 低 | 现在 `--no-verify` 能绕；台账是本地可写文件（门自己承认：判不了"是谁批的"） |
| **格 ⑫ 收尾** | 子代理的"进度文件"按 **ID 命名** | 低 | 现在是按任务名，同名会撞车 |
| **工具维修剩余** | 护栏判据改看"实际动作"（现在**只读也拦**） | 中 | 上次只做了"报错可读"那一半 |

## 已经做完的大件（背景）

| 大件 | 状态 |
|---|---|
| **长程同族题**（`cli-0006` 教模式 / `cli-0007` 只给增量） | ✅ 已入库（5→7 题），且审计员**双向验过**"增量是真的" |
| **臂注册化**（支持 N 个臂 / 配置型） | ✅ 已落地（还验过 3 臂） |
| **跨代存档 + 对比**（一代一目录 + "这代做了什么进化"） | ✅ 已落地 |
| **重跑前的五个坑**（捷径 / B 臂没锁 / 混杂经验 / 补丁不可复现 / 题面不一） | ✅ 全填了 |
| **提交前的门**（未批的 R1 改动不许提交） | ✅ 已装（且今天真实拦住过我一次） |

## 术语对照

| 我会说的 | 意思 |
|---|---|
| 票 / 单子 | 一张"请批准"的申请（**另一个 AI** 来批，不是你） |
| 审计员 / 见证者 | **另一个 AI**（新起一个、不共享上下文、要它自己算） |
| 换代 | **热更新**（把自己的新版本换上继续干活，带验证 + 失败回滚） |
| 控制面 / 启动器 | 管"换不换、怎么换"的那一层 |
| 代 | 真正干活的那份"大脑"（可被换掉） |
| 地板 | "不许变差"这条底线（与"有没有变好"是两件事） |
