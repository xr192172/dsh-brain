# 现在到哪了 —— 一页给用户看的状态

> ★ **你只需要读这一页。** 技术细节在 `docs/skill-as-agent-spec.md` 与 `out/w*.md` 里，不用看。

---

## 一句话

**"重跑前的五个坑"已经全部填好了（我核验过关键三条）**，现在**只等审计员签字** → 签完就**重跑实验**。

## 刚做完的（重跑前的五个前置，全部来自审计员的实测）

| # | 之前的问题 | 现在 |
|---|---|---|
| **①** | ★★ 工作树里有个"**快捷方式**"（`node_modules`）**指回判据根** ⇒ 顺着 `..` 就能读到判据脚本**和全量题面** | ✅ **堵上了**（两臂都访问不到） |
| **②** | ★★ **B 臂的代码仓库根本没锁**（留着 333 条历史）⇒ 一条 `git show` 就能拿出判据脚本；**而 A 臂是锁的** ⇒ 不仅泄漏，**两臂还不对等** | ✅ **两臂都锁上了**（历史都只剩 1 条） |
| **③** | ★★★ 两臂的 `AGENTS.md` **不一样** —— **B 臂多了两条我们【预先写好】的"经验"，其中一条恰好贴着第二题的约束** | ✅ **已清掉**（现在两臂逐字相同）⇒ **B 臂的"记忆"只能来自它自己做第一题的写入** |
| **④** | 工具维修的补丁**没被登记**，工作树一重建就丢 | ✅ 变成**可复现的一步** |
| **⑤** | 两臂题面可能不一样 | ✅ **统一用新题面**（带"说说工具体验"那句） |

★ **③ 这条最要紧**：它不清掉的话，我们测的其实是"**我们塞给它的东西**"，而不是"**它自己学到的**"。

## 题目也加好了（按你的主张）

- ★★ **新增长程同族两题**：`cli-0006`（**教一个模式**）／`cli-0007`（**只给增量**）⇒ 题库 **5 → 7 题**（**只加不删**）
- ★ 审计员**自己验了"增量点是真的"**：把第一题的答案**照抄**到第二题 ⇒ **判据红 2 处**（正好是那题多加的约束）
- ★★ 按你的意见：**基础题不再用于"测记忆效果"**，**改作"地板：换代后不许变差"**；**测记忆只用长程同族题**
  ⇒ ★ 理由有数据：上一轮 **`k=1/2/3` 两臂结果逐题相同（3/3）** ⇒ **简单题有"天花板"，两臂都满分，本来就测不出差别**

## 你的另一条建议也采纳了（子代理的"中断继续"）

- **你猜对了**：子代理**一被关闭就不返回结论**（实证：两份报告都没写出来）。
- ★★ **但工作没丢** —— 它们**每步的产物都在**（分步证据、扫描结果）⇒ **丢的只是最后那份报告**。
- ⇒ ★ **已落地**：现在要求子代理**开跑先写"进度文件"**（我到哪步了／还没做什么／证据在哪），**每完成一步就更新** ⇒ **被打断后新子代理读它就能接着跑**。
- ★ 你问的"**用 UUID / 会话 ID 找文件**" —— **对，就该这样**（现在我用的是"任务名"，下一步改成**按 ID 命名**，避免撞车）。

## 下一步（只差一步）

**审计员签字** → **提交** → ★★ **重跑实验（这次：长程题 + 泄漏堵上 + 混杂消除）** ⇒ **拿第一份真正可信的读数**。

★ **现在有 5 张票在等签字**（题面 / 长程题 / 工具维修 / 五个前置）。**按规矩我不自己批**，继续派审计员。

## 术语对照

| 我会说的 | 意思 |
|---|---|
| 票 | 一张"请批准"的申请 |
| 审计员 / 见证者 | **另一个 AI**（新起一个、不共享上下文、要它自己算）—— **不是让你审** |
| 换代 | **热更新**（AI 把自己的新版本换上继续干活） |
| 主线 | 那件正事：**让实验跑出可信的读数** |
| 地板 | **"不许变差"**这条底线（与"有没有变好"是两件事） |
