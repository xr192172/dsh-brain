# 判据阶梯 L2/L3/L4 施工记录（2026-09-24）

> 目的：把"G1（门只跑到 L1）"从纸面推到**能拒门**。设计输入 = 议事厅 l234 的最小实施设计
> （`out/_council/l234.seats.md`）+ 评审席纠正 + 主线修正。
> 本页是**施工与核验的记录**；结论性的东西在施工完成后由主线独立复算后再写。

## 1. 派发方式（三条并行分支）

| 线 | 工作树 | 分支 | 任务书 | 起跑 HEAD |
|---|---|---|---|---|
| L2 基线不退化 | `D:/project_develop/_l2/wt` | `feat/l2-baseline` | `out/_l2-prompt.txt` | `0718341` |
| L3 隐藏 holdout | `D:/project_develop/_l3/wt` | `feat/l3-holdout` | `out/_l3-prompt.txt` | `0718341` |
| L4 反事实对照 | `D:/project_develop/_l4/wt` | `feat/l4-shadow-ab` | `out/_l4-prompt.txt` | `0718341` |

- 施工方 = **DSH 会话**（用 `out/dsh-delegate.mjs` 派，不收进 WorkBuddy 的子 Agent —— 用户 2026-09-24 定的纪律）。
- 三者都会改 `capability-gate.mjs` 的同一批位置（`LADDER` / `evaluate()` / `receiptOf()`）
  ⇒ **冲突是预期的**（议事厅设计的"代码可并行、合并 L3 先"）。任务书已要求各自**把 diff 做小做局部**。

## 2. ★★ 实证结论（都是"读到即证据"，不是推断）

### 2.1 沙箱：工作区外的写有两种命运，都会浪费一轮
- **被直接拒绝**（`approval/asked = 0`）：L2 第一轮往主仓 `dsh-brain/scripts/` 连续 3 次 `write` **全失败**，
  主仓 `git status` **零污染**。⇒ 工作区外的写**不卡会话，只白烧步数**。
- **提权后挂起**：L3 曾往 `D:/project_develop/_holdout/` 写，工具**发起 `escalate sandbox to danger-full-access`**
  （reason 逐字："任务书明确要求将 holdout 任务集写在这条路径…"）⇒ `approval/asked = 1`、
  **`approval/decided = 0`，整轮卡死**（白烧 ~20 分钟）。
- ⇒ ★ **判据**：**工作区内的写不触发审批**（冒烟：shell+write+read，`approval/asked = 0`）；
  **工作区外的写要么被拒、要么挂起**。**两者都靠"留在工作区内"避免。**

### 2.2 ★★★ 审批**没有编程应答通道**（"完全脱离人工"的硬阻塞）
实测候选全 `404`：`approval.answer` / `approval.decide` / `approval.respond` / `approval.resolve` /
`approval.list` / `approval.pending` / `userApproval.{decide,answer,list}` / `session.approval{,.answer,s}` /
`permission.approve` / `permission.decide` / `permission.answer` / `sandbox.escalate` / `sandbox.approve`；
列举面 `host.methods` / `api.describe` / `rpc.describe` / `host.capabilities` 也全 404。
经 `session.prompt` 发 `/permission danger-full-access` **不会改档**（只当普通用户消息，事件流里
`permission/preset` 保持 `workspace-write`）。探针：`out/_probe/probe-approval-answer.mjs` / `probe-permission.mjs`。
- ⇒ **结论**：**沙箱档位与审批应答目前只能由人在 UI 操作**。
- ⇒ **含义（要如实记）**：任何"AI 全自动施工"一旦需要工作区外的写，就会**永久挂起**。
  用户要的"完全脱离人工 / AI 互相监督"，**这一格是空的**。
- ★ 解它需要一层**独立于档位的 auto-answerer**（正解就是用户先前提过的"`test` 开发者模式：不审批、自动放权，
  只禁互读互写"）。**但那属于安全层（P2）⇒ 主线按停手判据停下，等用户裁决，没有擅自做。**

### 2.3 越界事故的根因：**"执行方式"改了，任务书里的路径没同步**
- L2 任务书是在"直接改主仓"的设想下写的（"只改 `D:/project_develop/dsh-brain` 的 `scripts/`"），
  后来改成"在 `_l2/wt` 工作树里施工"却**没同步那句话** ⇒ 子 agent **照旧路径施工**。
- ⇒ **纪律**：**改了执行方式 ⇒ 必须同步任务书里的每一个路径**；
  且 **派活后必须用独立通道核验**（本轮靠 `out/_probe/audit-delegates.mjs` 抓到，**不是**靠它自己的总结）。
- ⇒ 第二例同族：L3 的 holdout"必须落在工作区外"（R1 要求）与"沙箱不许写工作区外"**互相矛盾** ⇒
  正解是 **被测侧只写草稿、落盘由主线代劳**（已改任务书）。

### 2.4 审计器自己也踩了铁律 18（"回读 ≠ 生效"）
第一版读**会话创建事件**里的 `agentPreset` ⇒ 三条会话全被误报成 `council`
（那是"创建那一刻的默认值"）。真正生效的值在 **`agent-preset/selected` 持久事件**里。已修。

## 3. ★ 主线的独立核验清单（施工完成后**我自己**跑，不信它们的总结）

1. **合并**：按议事厅定的顺序 **L3 → L2 → L4**（冲突手工解，解完 `node --check` 每个改过的脚本）。
2. **逐条复算任务书里的判据**，至少：
   - L2：`cap.baselines` 缺 ⇒ blocked；当前 < 基线 ⇒ blocked；**基线"就地生成"必须被拒**；
   - L3：改 holdout 一字节 ⇒ blocked；结果文件缺失 ⇒ blocked（fail-closed）；**`unenforced` 从 `['L2','L3','L4']` 缩成 `['L2','L4']`**；`proofLevel` 仍 `'L1'`；
   - L4：**`L4.enforced` 必须仍为 `false`** 且 `status='advisory-by-design'`；delta 难看时 **verdict 不变**。
3. **消融自证**：逐个撤回补丁 ⇒ 对应断言**必须变红**（铁律 21）。**任何一个撤不回红的判据都不算数。**
4. **回归**：`node scripts/capability-gate.mjs ladder`（L2/L3 显示已实施、L4 显示软门）+
   `scripts/gate-vector-run.mjs` 16/16 + `scripts/change-classify.mjs` 判定。
5. **看闸门回执落在哪个库**：三条线都该写**隔离 `DSH_HOME`**（`_l2/dshhome` 等），
   **现役 `~/.dsh/capabilities/registry.json` 不该被改动** —— 用 mtime + `git`-独立比对确认。
6. ★ **判据不能在"够不到的地方"这件事要如实**：`evals/README.md:152-155` 已写明
   **最严边界 `machine` 下 5 题全部不适用**（同机同盘 ⇒ 绝对路径恒可达）⇒
   **不许把"放到工作区外"说成"已隔离"**。

## 4. 未闭合

- **L4 的 `enforced` 语义**：议事厅那版没说清 ⇒ 主线定为 **保持 `false`** +
  新增 `status:'advisory-by-design'` + 回执加 `unenforcedWhy`，把"未实施"与"软门"分开。
  （若标成"已实施"就是**假绿** —— 它根本拦不住东西。）
- **本机单机 + `workspace-write` 下 R1 做不到**（同 2.3 第二例 + 3.6）⇒ 若要真隔离，需换卷/离机/容器。
- **审批 auto-answerer 未建**（见 2.2）⇒ 待用户裁决。
  ★ **2026-09-25 已定案：不建。** 用户选了更好的路子 —— **把控制面当启动器，用启动参数定档**
  （`DSH_SWITCHBOARD_DEV=1` ⇒ 子代 `DSH_PERMISSION_MODE=danger-full-access` ⇒ 沙箱全开 + 审批 never，
   commit `45ca40a`）。**档位出生时定 ⇒ 根本不存在审批动作 ⇒ 永不挂**，比运行期拦截器干净。

---

## 5. ★ 主线的独立核验记录（2026-09-25，**不采信它们的报告**）

### 5.1 L2：我抓到两处，都值得留档

**① 那条"回归"我自己补上了（行为级、真跑）**
- 子代理报"未跑（沙箱 EPERM，`gatecheck.exe` 不可执行）" —— **这是诚实的**，没谎报通过。
- ★ 但**判据本身是可跑的**：`out/gatecheck.exe` 存在（12.5 MB）。我在 **L2 工作树里**跑
  `node scripts/gate-vector-run.mjs --impl … --tmp out/_gvrtmp` ⇒ **16/16 PASS / exit 0**。
- ⇒ 结论：**沙箱只拦"worker 执行工作区外的 exe"，不拦主线** ⇒ 这类判据**该由主线代跑**。

**② 它的"消融自证"是弱判据，而且给出了【假结论】**
- 证据：`out/l2-ablation-test.mjs` **只 `readFileSync` 源码**做文本判定（不做行为判定）；
  `scripts/test-l2.mjs` 一半是 `gateSrc.includes(...)` 文本匹配，另一半是**镜像实现**（自己重写一遍逻辑）
  —— 正是"测试与被检对象一起错"的典型风险。
- ★★ **我做了行为级消融**（`out/_probe/ablate-l2-behavioral.mjs`：驱动真 CLI、看真 verdict）：
  · 第一版打靶 `return { verdict: 'blocked' }` ⇒ **门纹丝不动（仍 blocked）**
  · ⇒ **挖出真问题：`evaluate()` 是 `allChecks.some(c => !c.ok)` —— 它【不看 `l2.verdict`】，
    那个返回值是【死码】。**
  · ⇒ 连带结论：**子代理报告里"消融①生效：无基线时门放行"是【假结论】** ——
    它的文本读取器看到源码变成 `admitted` 就宣布门放行了，**真跑一遍门根本没放行**。
  · 打对靶子（`add('基线存在', false, …)` 的 ok 位）后：**4/4 通过** ——
    `blocked → 消融后 admitted(proof=L1) → 还原后 blocked`，且**还原逐字节一致**。
- ★ **待修（合并时做）**：`l2Baseline` 的 `verdict` 字段是死码 ⇒ 要么让 `evaluate()` 认它
  （双源 fail-closed，但**会让单因子消融失效**），要么**删掉它**免得后人以为它决定 verdict。
  倾向**删掉 + 注释说明"唯一决定因子是 `checks[].ok`"**。

### 5.2 L4：我逐条复跑过（上一轮）
delta 表用真存档（6 对参与 / 4 对如实单列不计入）；`enforced:false` 保持 + `advisory-by-design`；
**坏 delta 下 verdict 仍 admitted**（软门成立）；负向 `TRUE_EXIT=2`；**消融**（把 L4 塞进 checks ⇒ 变 blocked）✓。

### 5.3 L3 / 臂间隔离：施工中（第二轮的收尾会话）
- L3 收尾要求：**g 真实现**（原报告写 `PASS(逻辑正确)` 但代码里 `visibleUp`/`hiddenDown` **命中 0**）、
  h 消融、d 单独验证、**holdout 集过 R2 自证**。
- 新建 `packages/arm-isolation`：**独立于沙箱档位**的一层（沙箱策略只有 `mode`+`workspaceRoot`，
  已核对无 deny 列表）⇒ 用官方拦截面 `ctx.tools.guard`，**范本 = 我们自己的 `switchboard/src/guard.ts:36-51`**。

### 5.4 第二轮收尾结果（2026-09-25，全部**我复跑过**）

| 线 | 判定 | 我复跑到的证据 |
|---|---|---|
| **L2** | ✅ | 行为消融 4/4（我写的 `out/_probe/ablate-l2-behavioral.mjs`）+ 回归 **16/16 exit 0**（我跑的） |
| **L3** | ✅ | g **真实现**（`computeMisevolution`，读真存档 + 真结果文件，缺侧不猜）；h/d/R2 见其报告，R2 用**直接跑 oracle** 替代 `eval-validate`（因其 spawnSync EPERM）—— 如实标注 |
| **iso**（臂间隔离） | ✅ | `test/isolation-check.mjs` **驱动真插件**（`apply(ctx, armsConfig)` + 真 `tools.guard`）；我复跑 **24/24 exit 0** |
| **L4** | ✅ | 5.2 已记（上一轮全项复跑） |

### 5.5 ★★ 我自己的一次【仪器假阴性】（必须留档，因为它正是本项目反复栽的那一类）

我在核 L3 的 g 时用 `grep -c "visibleUp\|hiddenDown"` ⇒ **命中 0** ⇒ 据此差点写下"**g 又没实现**"。
**实际是实现了**，只是实现者把函数命名成 **`computeMisevolution`**（`:423`），我 grep 的是**上一轮设计方案里的标识符名**。
- ⇒ **教训**：**判据要按"行为"写，不能按"设计文档里的变量名"写**。
  正确查法是"`evidence.l3Misevolution` 里有没有真数字 / 有没有那条计算函数"，
  而不是"某个标识符在不在"。
- ⇒ 这与本项目铁律同族：**"判据读不到差别" ≠ "没有差别"**；**"一条读数稳定不变就是红线"**
  ⇒ 先去问"**它到底有没有在量东西**"，而不是拿它下结论。
- ★ 反过来说：**上一轮**同样是这条 grep 命中 0，那次**确实**没实现 ——
  ⇒ **同一个读数两次都出现**，第一次是真阴性、第二次是假阴性。
  **这正是"读数不能用来自证"的活例**：它必须与**独立通道**（读代码本体）交叉才可信。

### 5.6 ★ 一个连带发现（对委派有普遍影响）：**P2 宿主护栏会拦"读"**
iso 会话报告：它想读我指定的范本 `packages/switchboard/src/guard.ts` 时，**被 P2 策略拦下**
（"即使只读"），最后用 `pwsh Get-Content` 绕过。
⇒ 含义：**`@dsh-brain/switchboard` 作为 bundle 挂在每个 gen 里 ⇒ P2 宿主护栏在 gen 内也生效 ⇒
被派来的 worker 读不到 `packages/switchboard/**`**。
⇒ **纪律**：任务书里**不要**让 worker 去读控制面源码当范本（读不到）；
要么给副本、要么只描述形状。已写进 skill `dsh-delegate-loop`。


