# 主线账本（每轮从这份开始读，别从漂移的上下文开始）

> **用途**：防"长上下文漂移"。★ **主线只保留：当前链的骨架 + 到哪一格 + 每格的判据 + 下一步。**
> 细节一律在 `docs/skill-as-agent-spec.md`（分节）与 `out/wNN-*.md`（子代理报告）里，**不要往主线搬**。
> **维护规则**：每完成一格，只改这份文件的 3 行（进度 / 判据 / 下一步），**不写过程**。

---

## 1. 当前链（唯一的链）：**"让 A/B 的读数可信"**

★ 我们不是"跳到别的项"，是**同一条链**上一格一格补。链的终点 = **拿到第一次可信的 A/B 读数**。

```
① 判据机器（三断言 + 四态 + 反自欺守卫）        ✅ 已成（骨架期 README 那句"不构成结论"）
② 记忆面接进来（--face 指向真面）               ✅ 已成（memface.exe；A=0/B=1）
③ 随机对照三条断言拿到真读数                    ✅ 已成（A2/A3 真值）
④ 第一次真 A/B                                  ✅ 跑过（9/10）★ 区分度 0
⑤ 但"读数可信"被三个假绿源破坏 ⇒ 逐个补：
     ⑤a R1 判据可被 agent 读到                  ✅ 已修（判据移出 + --repo 指向指定树）
     ⑤b 工作树 CRLF ⇒ seed 打不上                ✅ 已修（+ R2 变成可执行门）
     ⑤c 沙箱 seam 没覆盖 MCP / bridge 旁路       ✅ 已修（+ 换代生效 + 行为级复验）
     ⑤d 换代失败路径不 fail-safe（产孤儿代）      ✅ 已修（O100）
⑥ ★ **待办：自举层的"待批记录 + 一句确认"**      ✅ **已成**（O106/O107；`change-classify` + `pending-approval`）
   └ ★ **悬着的票**：`pa-20260923-627ca5`（"要不要把判层器/待批器升为 R1"）—— **等外部一句话，我不代批**
⑦ ★ **待办：记忆的【写】侧**（现在只有"预置常驻"）  ✅ **已成**（`scripts/mem-seq.mjs`；★ **Go 侧 0 行改动** —— 缺的不是 API 是接线）
   └ ★ 权威读数（我复核）：**B 臂 `count:4`** / **A 臂 `count:0`**（注入时 `[1,2,3]` → 题后 `[2,3,4]`）
   └ 两臂同脸仍成立（102 / `0339ee00` / 6459）；写入内容 3 条**全 L1**（L2/L3 特征扫描 0 命中）
⑧ 重跑 A/B，拿可信读数                          ⬅ **下一步**；★ 前置两件：
   └ (a) **复位实验场地**（B 的 store 已被本次实验改成 4 条；两臂 wt 多了 3 个微任务产物）⇒ 决定：**复位**
   └ (b) ★★ **加"只读 count 入口"**：判据机器**读不到外部真实 store 的 count**（带 `--face` 会 `reset` 毁库、不带就用桩）
        ⇒ 现在 A2/A3 的真读数是脚本重算的，不是判据机器产的 ⇒ **这是 ⑧ 的前置缺口**
```

## 2. 每格的判据（**判据必须可执行，不许是"看着像"**）

| 格 | 判据（可执行） |
|---|---|
| ① | `memory-effect-judge.mjs --selftest` ⇒ 四态齐 + 故意破坏 ⇒ `BATCH-INVALID`/`CONTROL-FAILED` |
| ② | `--face <exe>` ⇒ 三断言真执行；`count` 权威值 A=0/B=1 |
| ③ | A2 控制臂恒 0 / A3 处理臂严格递增 / A1 两臂指纹（★ 注意 **venue 归一化**） |
| ④ | 10 次真会话 + 三条对照断言 + **`NEEDS-EVIDENCE` 不填"通过"** |
| ⑤a | `poison-check --wt <wt>` ⇒ `isolation=OK`；带毒 ⇒ `BROKEN` 点名；判据 8/8 不在被测树 |
| ⑤b | `eval-signal-check --task cli-0001 --repo <wt>` ⇒ **三段全过**（干净绿/seed 红/还原绿）；坏 seed ⇒ `SEED-NOT-EFFECTIVE` |
| ⑤c | 真会话里 `safe_rename`/MCP 工具写判据根 ⇒ **被拒**；**工作树内写 ⇒ 成功**（阳性对照） |
| ⑤d | verify 传非法路径 ⇒ **有记录 + 非破坏回滚 + 不产孤儿** |
| ⑥ | R1/R0 类改动 ⇒ **留待批记录 + 要外部一句确认**（不自动续跑）；R2 类 ⇒ 自动 |
| ⑦ | 记忆"写"侧可用 ⇒ 处理臂 `count` 随 k **增长**（不是常驻 1） |
| ⑧ | 两臂 oracle 出现**可解释的差**，或**明确记为"未观察到差别"**（n=5 不声称显著） |

## 3. 我（主线）该做的与子代理该做的（**用户定的分工**）

- **主线（我）**：**只做"派活 + 核验 + 维护本账本"**。★ **判据是"我这一轮有没有把细节搬进主线"**。
- **子代理**：执行类（跑实验、改代码、写报告、查证）**一律派**；**验收门由我定、结果由我独立核验**（不许只信它的总结）。
- ★ **今天漂移的原因**：后半段我大量自己在做（findings/诊断/验证），主线上下文被细节淹没 ⇒ **违反了这条分工**。

## 4. 用户的两条设计洞察（**要长期记住**）

1. ★★ **"是不是进化"的判据可以锚在【外部世界】**：只要互联网没烂到全世界互相投毒，**GitHub 上总能找到真正进化方向的证据**
   ⇒ 这与"R1 必须外部见证"一致，而且给出了**具体的外部见证物**（star / 采纳 / 复现 / 上游实现）。
   ⇒ ★ **远景**：候选由 agent 提，**判定逐步从"人一句话"过渡到"外部基准"**（但这仍属 R1，**不能由被判定者自己定标准**）。
2. ★ **换代 = 自进化的正式通道**（不是"人操作自己"的禁区）：P2 禁的是"**没做好准备就就地换血**"，
   **agent 自己走 `tool_apply`（带 verify）换代是设计意图**。

---

## 5. 第 ⑧ 格状态（2026-09-23 10:2x）：**读数已就位，但改动待批**

- ✅ **前置 A**（只读真实 count 入口）：已实现 **`--face-readonly`**；真 store **连读 3 次 `unchanged=true`**；同命令在两版判据下**输出逐字相同** ⇒ **口径未改**
- ✅ **前置 B**（复位）：两臂复位，**备份 hash 已记档**
- ✅ **k=1..5 逐题 JSON 落盘**；三条断言逐条机器验证：
  - **A1** venue 归一后同为 **`e7b38b40`**
  - **A2** 逐 k **全 0**
  - ★ **A3** 序列 **`[1,2,3,4,5]` 严格递增**（**(甲) 序列型核心断言成立**）
  - **两臂 oracle**：**「未观察到差别」**（★ n 小，**不声称显著**）
- ⏸ ★★★ **但改动被 `change-classify` 判 R1**（命中 `R1-MEMORY-FAMILY`："**改器具 = 改读数 ⇒ R1**"）
  ⇒ **它按规则停下、未提交** ⇒ ★ **待批票 `pa-20260923-206326`** ⇒ **等你一句话**（不批则 ⑧ 格停在"待见证"）
- ★ **当前两张票**：`pa-20260923-627ca5`（机制自身是否升 R1）+ `pa-20260923-206326`（⑧ 格前置 A）

---

## 6. ★★★★ 新增格 ⑨（**在做 ⑧ 的重跑时暴露**）：**题面与隔离布置自相矛盾**

**用户问**："三个会话卡在沙箱审批，我改成了 full access —— **会影响臂的可见性吗？**"

**答复（分两条，别混）**：
- ✅ **"可见性/同脸"不受影响**：档位**不进 `system`/`tools`**，走的是 `user/message` 的 runtime-context；
  被改档的 A 会话与未改档的 B 会话 **venue 归一后 `system` 逐字节相同**（`diff` 空 / 6459 / 102 / 名字 hash 同）。
- ❌ **"隔离"被击穿，但方向是【臂 → 判据根】（**两臂互看仍完好**）**：
  A 臂写了判据根里 **B 的 oracle 依赖的同一份文件** ⇒ B 的题 **"没有信号"被弃跑**。
  ★ 两臂 store/wt 互不可见**仍成立**（A store 一整天未动 19B/00:05；B 3265B/11:11）。

**三处前提修正（它纠正我）**：
1. **不是 3 条，是 2 条持久改档，且都在 A 臂**（`/permission danger-full-access` @ 11:08:20 `dbc87502`、11:08:27 `65fdca09`）；
   ★ **B 的 `e8279460` 是 8 次 `allowed-once`（单次放行）**，档位始终 `workspace-write` ⇒ **性质不同**。
2. **不致命**：改档**只写进该会话自己的日志**（`dsh-sandbox-policy` 源码：session log as the store，**two sessions can never see each other's state, and there is no external config store**）；
   ★ 默认档位 = **`workspace-write` + `ask`**（我核验：`dsh-base/cordis.patch.yml:175` `mode: … ?? 'workspace-write'`、`:191` `policy: … 'never' : 'ask'`）⇒ **后续新会话不受影响**。
   ★ 缺口（如实）：11:08 之后**没有新建会话** ⇒ 这条是**源码判定、无实测**。
3. ★★★ **真污染是判据根，且已还原**（`git diff` 空、mtime 11:12:37、补丁存 `w41-main-repo-pollution-cli-0004.patch`）。
   **关键因果**：**"写判据根"从 10:50/11:06 就开始了，走的是用户点的 `allowed-once`；`full-access` 只是把后面的刹车拆了**（改档后 11:08:25–11:11:19 连写 15+ 次，再无审批）。

**★★ 读数影响**：**k=4（cli-0005）两臂都被审批卡住**（A 31.5min / B 16.6min）⇒ `over-budget` ⇒ **作废/待见证**；
★ **k=5（cli-0004）A 的 `oracle=pass` 是【自证】**（它改的就是 oracle 执行的文件）⇒ **无效**；
★ **且 A1 不成立的原因是"B 没发题"，不是"记忆改了脸"**（别误读）；★ **k=1..3 干净可用**。

### ★★★ 结构性根因（**这条最重要，且修正了我 §50.3 的定性**）
> **两臂 wt 都【不含 `evals/` 和 `scripts/`】**（sparse-checkout 排除了它们），**而这两题的靶文件正在那两处**
> ⇒ **"去判据根找文件"是被题面逼出来的必然行为。**

⇒ ★★ **所以不是"agent 主动越界"（我 §50.3 的定性偏了），而是【题面与隔离布置自相矛盾】。**
⇒ **⇒ ⑨ 的处置（未执行，先记录）**：① k=4/k=5 标"作废/待见证"；② **改题 or 改布置**（把靶文件移出判据根区，或判该题"不适用于隔离布置"）；③ 档位无需复位（per-session，旧会话已结束；新会话默认 `workspace-write`）。

---

## 7. ★★★★ 用户澄清与方向修正（2026-09-23 12:0x）—— **"待批"是替代他审批；目标是"完全脱离人工 + AI 互监"**

**澄清**：前一句"待批"被语音识别错，他的意思是 —— ★★ **待批机制应当【替代】他审批**（不是把票还给他）。

**方向（他明确要求）**：
> "我希望完全脱离人工，**AI 可以互相监督**。你做开发时是能监督自己做了什么的……**我的水平甚至没有你高**，你能做的判断肯定比我准确。"

⇒ ★★ **我的更正**：我早先说"R1 必须【人】见证"**说过头了**。无环原则要防的是"**被判定者自己批自己**"（回路 = 漏洞），
**而"另一个【独立】的 AI 见证者"完全满足它** ⇒ 正解：
> **把"外部见证"从【人】改成【独立 AI 见证者】**：★ **独立性判据** = **不共享被判定者的上下文** + **输入只有证据** + **必须独立复算**（不是复述）。
> ★ **人只在两端**：定标准（一次）+ 极少数冲突裁决 ⇒ **中间全自动**（= 文档 §6.2 的"契约式授权"）。
⇒ ★ **载体已存在**：把 `pending-approval` 的 `approve --by` 从"人"扩成"**独立 AI 见证者 + 独立复算证据**" ⇒ **O113**。

**他的 `test`/开发者模式提议**：
> "搞一个启动参数，比如 `test` 进开发者模式：**换代不需审批自动走**；**沙箱自动给权限**；**不设危险操作拦截**；★ **只是不允许互读互写**。"
- ★ **我赞成方向**（= "人批一次标准+预算，之后全自动"），**但指出一处技术问题**：
  **`danger-full-access` 是全开，做不到"只禁那两处"** ⇒ "沙箱全开 + 只禁互读互写"**不能靠沙箱档位实现**。
- ★★ **更干净的实现（也正是格 ⑨ 的正解）**：
  > **让【题面自洽】—— 靶文件在 wt 里，而答案（oracle / 期望值 / holdout）不在可达处。**
  > 那样 **沙箱全开也无所谓**（"答案不可得"靠**布置**保证，不靠沙箱拦）。
⇒ **⇒ 所以格 ⑨ 与 dev 模式是同一件事的两面** ⇒ 合并处理。
