# 主线账本（每轮从这份开始读，别从漂移的上下文开始）

> **用途**：防"长上下文漂移"。★ **主线只保留：当前链的骨架 + 到哪一格 + 每格的判据 + 下一步。**
> 细节一律在 `docs/skill-as-agent-spec.md`（分节）与 `out/wNN-*.md`（子代理报告）里，**不要往主线搬**。
> **维护规则**：每完成一格，只改这份文件的 3 行（进度 / 判据 / 下一步），**不写过程**。

---

## 1. 当前链（唯一的链）：**"让 A/B 的读数可信"**

★ 我们不是"跳到别的项"，是**同一条链**上一格一格补。链的终点 = **拿到第一次可信的 A/B 读数**。

```
① 判据机器（三断言 + 四态 + 反自欺守卫）        ✅ 已成（骨架期 README 那句"不构成结论"）
② 记忆面接进来（--face 指向真面）               ✅ 已成（memface.exe；A=0/B=1）
③ 随机对照三条断言拿到真读数                    ✅ 已成（A2/A3 真值）
④ 第一次真 A/B                                  ✅ 跑过（9/10）★ 区分度 0
⑤ 但"读数可信"被三个假绿源破坏 ⇒ 逐个补：
     ⑤a R1 判据可被 agent 读到                  ✅ 已修（判据移出 + --repo 指向指定树）
     ⑤b 工作树 CRLF ⇒ seed 打不上                ✅ 已修（+ R2 变成可执行门）
     ⑤c 沙箱 seam 没覆盖 MCP / bridge 旁路       ✅ 已修（+ 换代生效 + 行为级复验）
     ⑤d 换代失败路径不 fail-safe（产孤儿代）      ✅ 已修（O100）
⑥ ★ **待办：自举层的"待批记录 + 一句确认"**      ⬅ **O106/O107（本轮做）**
⑦ ★ **待办：记忆的【写】侧**（现在只有"预置常驻"）  ⬅ 所以 (甲) 学习曲线型**还没跑**
⑧ 重跑 A/B，拿可信读数                          ⬅ 链的终点
```

## 2. 每格的判据（**判据必须可执行，不许是"看着像"**）

| 格 | 判据（可执行） |
|---|---|
| ① | `memory-effect-judge.mjs --selftest` ⇒ 四态齐 + 故意破坏 ⇒ `BATCH-INVALID`/`CONTROL-FAILED` |
| ② | `--face <exe>` ⇒ 三断言真执行；`count` 权威值 A=0/B=1 |
| ③ | A2 控制臂恒 0 / A3 处理臂严格递增 / A1 两臂指纹（★ 注意 **venue 归一化**） |
| ④ | 10 次真会话 + 三条对照断言 + **`NEEDS-EVIDENCE` 不填"通过"** |
| ⑤a | `poison-check --wt <wt>` ⇒ `isolation=OK`；带毒 ⇒ `BROKEN` 点名；判据 8/8 不在被测树 |
| ⑤b | `eval-signal-check --task cli-0001 --repo <wt>` ⇒ **三段全过**（干净绿/seed 红/还原绿）；坏 seed ⇒ `SEED-NOT-EFFECTIVE` |
| ⑤c | 真会话里 `safe_rename`/MCP 工具写判据根 ⇒ **被拒**；**工作树内写 ⇒ 成功**（阳性对照） |
| ⑤d | verify 传非法路径 ⇒ **有记录 + 非破坏回滚 + 不产孤儿** |
| ⑥ | R1/R0 类改动 ⇒ **留待批记录 + 要外部一句确认**（不自动续跑）；R2 类 ⇒ 自动 |
| ⑦ | 记忆"写"侧可用 ⇒ 处理臂 `count` 随 k **增长**（不是常驻 1） |
| ⑧ | 两臂 oracle 出现**可解释的差**，或**明确记为"未观察到差别"**（n=5 不声称显著） |

## 3. 我（主线）该做的与子代理该做的（**用户定的分工**）

- **主线（我）**：**只做"派活 + 核验 + 维护本账本"**。★ **判据是"我这一轮有没有把细节搬进主线"**。
- **子代理**：执行类（跑实验、改代码、写报告、查证）**一律派**；**验收门由我定、结果由我独立核验**（不许只信它的总结）。
- ★ **今天漂移的原因**：后半段我大量自己在做（findings/诊断/验证），主线上下文被细节淹没 ⇒ **违反了这条分工**。

## 4. 用户的两条设计洞察（**要长期记住**）

1. ★★ **"是不是进化"的判据可以锚在【外部世界】**：只要互联网没烂到全世界互相投毒，**GitHub 上总能找到真正进化方向的证据**
   ⇒ 这与"R1 必须外部见证"一致，而且给出了**具体的外部见证物**（star / 采纳 / 复现 / 上游实现）。
   ⇒ ★ **远景**：候选由 agent 提，**判定逐步从"人一句话"过渡到"外部基准"**（但这仍属 R1，**不能由被判定者自己定标准**）。
2. ★ **换代 = 自进化的正式通道**（不是"人操作自己"的禁区）：P2 禁的是"**没做好准备就就地换血**"，
   **agent 自己走 `tool_apply`（带 verify）换代是设计意图**。
