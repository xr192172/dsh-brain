# 任务 T3：`ToolDef` 到底怎么才能"真的被注册"——先给**可行性与最小方案**（缺口③）

## 背景（确凿事实）
端到端演练里，工厂产出的规格是 `toolFilter: ["demo_first_line"]`；桥**如实标注**了这个工具
**在 DSH 里并不存在**（"需先把 `ToolDef` 注册进 ToolRegistry"）。
⇒ 也就是说：**目前没有任何东西把 skill 声明的 `ToolDef` 变成运行时可调用的工具**，
所以"窄工具面"这条设计**还没落地**。

## 要做什么（**这一轮只要"读源码 + 给结论"，不要动手改产品代码**）
1. **读清楚**（列出你读过的确切文件与行号）：
   - `@deepseek-ai/dsh-tools` 的 **ToolRegistry / ToolGuard** API：
     工具是怎么注册的？注册一个"给定名字 + JSON Schema + 执行体"的工具需要什么？
     （找 `node_modules/@deepseek-ai/dsh-tools/` 与其 README/类型定义）
   - `packages/tool-evolution/`（我们**已经有**这个包）——它现在做什么？**能不能承接"注册 ToolDef"这件事**？
   - `packages/capability-bridge/` 与 `packages/arm-isolation/`（后者**今天刚改过**）：
     它们是怎么在 `apply(ctx)` 里挂到工具面的？照哪个形状抄最省？
2. **给出结论**（这是本轮的主交付）：
   - 「把 `ToolDef{Name, Description, Kind, Entry, Fn, Schema, ReadOnly}` 注册成运行时工具」
     **最小可行路径是什么**？写成一个插件，还是挂到已有插件里？
   - **执行体怎么落地**：`Kind: shell` + `Entry: first_line.sh` + `Fn: first_line` 这种，
     在**当前沙箱**下(win32)能不能跑？如果不能，**替代方案**是什么（Node 内联 / 预编译 / 只读在沙箱外的可执行）？
   - **风险与边界**：注册工具会不会影响**所有** agent 的工具面？（`Registry` 是进程单例吗？）
     会不会和"窄脸（toolFilter）"设计冲突？
   - **落地清单**：如果要做，分几步？每步的验收判据是什么？
3. ★★ **证据分级**（必须逐条标注）：哪些是你**直接读到的**（给文件+行号）、哪些是**二手**（README/注释说的）、
   哪些是**你的推断**。**二手与推断不许当结论。**

## 可判定的验收
- 产出一份 Markdown（建议 `docs/tooldef-registration-plan.md`），其中：
  1. 「读过的文件/行号」清单（**至少 5 条**，行号要真）
  2. 最小方案（含"挂哪里、什么形状、执行体怎么跑"）
  3. 风险清单（至少 3 条，且每条说明**怎么验证**）
  4. 落地清单（分步 + 每步判据）
  5. **证据分级表**
- ★ 要求：**任何"我读了 X 的第 N 行"都要能被复核**（贴出你看到的关键几行原文片段）。

## 硬约束
- ★ **只写 `docs/tooldef-registration-plan.md` 这一个文件**；**不要**改 `packages/`、不要改别的脚本。
- ★ **不要**尝试真的注册工具（那要换代，影响面大）——本轮**只出方案**。
- 不许 `git commit`、不许 `git push`、不许起服务、不许杀进程。
- ★ **不确定就写「不确定」**；**禁止编造** API、路径、行号。

## 诚实清单（最后必须写）
- 哪些结论是**直接读到**的、哪些是**推断**、哪些**没验证**
- 你有没有**没能读到**的关键文件（例如某个包没有 README / 只有编译产物）
