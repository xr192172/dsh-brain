# 2026-09-22 · dsh-brain

> **承接**：本会话从 2026-09-21 深夜连续工作到现在，**§1–§26 记在 `2026-09-21.md`**（同一会话的流水，按 §编号连续）。
> 本文件从 **§27** 起记 09-22 白天的内容。**找上下文：先读 `docs/skill-as-agent-spec.md`（施工规格），再看这两份日志。**

---

## 二十七、★★★★ 官方文档核证：用户关于「缓存时序 / 多子代共存」的判断成立 + 我 TTL 警告过度

**用户原话要点**："静置以后肯定不能用了呀，但是**父代都已经准备动了，那说明父代这一轮调用工具的那一次，百分百是建立了缓存的**。
只是问题在于父代很久没用过，你再让它做某个任务时，**这一波会在父代时重建一次缓存、要吃一波钱**；
但父代动起来以后缓存就建好了，**子代依旧可以直接[复用]**。
**除非多个子代从多个起点开始，后一个子代会影响另一个子代的缓存，但这个按理来说不会。**"
（并提示我去官网查证）

**查证来源**：`https://api-docs.deepseek.com/guides/kv_cache`（Context Caching，逐字引用）

### ✅ 用户两点判断都成立

| 用户判断 | 官方原文 |
|---|---|
| 父代调用工具那一次**必然已建缓存** | "**Each user request will trigger the construction of a hard disk cache.**" |
| 多子代**不会互相影响** | "**When the system detects a common prefix across multiple requests, it will persist that common prefix as an independent cache prefix unit.**" ⇒ 同一父代、同一 fork 点的多个子代**共享同一条公共前缀 ⇒ 共享一个单元，而不是互相驱逐** |

### ★★ 我上一轮的"TTL 是最大适用边界"**过度了**

官方：**"Once the cache is no longer in use, it will be automatically cleared, usually within a few hours to a few days."**
⇒ **窗口是数小时~数天**，不是我暗示的"同秒级脆弱"。叠加"每个请求都触发缓存构建" ⇒
**"父代刚做完事就 fork"这个用法下 TTL 根本不是问题**；
只有**父代长期静置后第一次动**才付一次重建钱，**而那是父代自己的成本，它本来也要付**。
⇒ **规格 §13.5 第 1 条（"TTL 是最大的适用边界"）作废。**

### ★★★ 官方机制比我说的更精确（本轮真正的收获）

> "**A cache hit requires that the corresponding prefix has already been 'persisted' … Each cached prefix is an independent, complete unit.
> A subsequent request can only hit the cache if it【fully matches】a cache prefix unit.**"

**单元持久化的三个时机**（逐字）：
1. **请求边界**：每个请求产生**两个单元** —— **用户输入末尾** 与 **模型输出末尾**；
2. **公共前缀检测**：检测到多请求公共前缀 ⇒ 把它持久化为**独立单元**；
3. **固定 token 间隔**（长输入/长输出）："to avoid long prefixes from being completely uncacheable due to never reaching an end position"。

⇒ ★★ **所以"能复用多少"不是简单"等于种子长度"，而是：子代前缀必须【完整匹配】父代留下的某个单元边界。**
⇒ **与我们实测精确吻合**：子代首请求 `cr=133,248`，而**父代自己那次请求命中的也是 `133,248`**（父代 `seq=495`）
⇒ **子代命中的就是父代留下的同一个单元。**
⇒ **fork 之所以结构性几乎总能命中**：子代种子 = 到最后一个 `turn/end` = 父代**模型输出末尾**
⇒ **正好落在规则 1 的边界上**；规则 3 的长上下文固定间隔再兜一层底。

### ⚠️ 由此得到一条**可证伪的预测**（O25）

若某次 fork 的**种子边界不落在任何单元边界上**，该子代首请求命中应当**显著下降**（而非仍为"脸 + 种子"）。
但 `completedTurnPrefix` 只取**完整回合** ⇒ **默认必然落在"模型输出末尾"** ⇒ 这条预测的**更可测形态**是：
**种子长度取不同值（尤其卡在两个固定间隔之间）时，`cr` 是否出现台阶。** **留待下一批实验。**

### 其它要记账的官方约束

- "**best-effort**…**does not guarantee a 100% cache hit rate**" ⇒ **不可把"命中"当保证**。
- "**The hard disk cache only matches the prefix part of the user's input**；output 仍要计算" ⇒ **命中 ≠ 这一步免费**。
- 官方命中读数 = `usage.prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`
  ⇒ 与我们一直用的 `cacheReadTokens` / `inputTokens` **口径一致** ✓

---

## 二十八、★★ O24 / O22 已修（度量工具的口径 bug）—— 子代理执行 + 我独立复核

**改动**（只改 `scripts/measure-delegation-reuse.mjs`；报告 `out/w11-o24-o22-fix.md`）：

### O24：主表"首请求"改为取【`seedLength` 之后】第一条
**旧行为是 bug**：取"日志里第一条带 usage 的 `assistant/message`" ⇒ 带种子的子代会**取到拷贝来的父代消息**。
**新**：第一条 `seq > seedLength`；无种子则不变；**旧口径并列保留并标 `Δ`**。

**我独立复核的硬数值（exit 0）**：

| 子代 | 新口径（**权威**） | 旧口径（并列） | 与我的真值核对 |
|---|---|---|---|
| `a04d96f5`（seedLength=22） | **98.9% / 474 / 40832** | 0.3% / 40803 / 128 | ✅ 一致 |
| `bd7d6e6d`（seedLength=381） | **99.7% / 413 / 133248** | 98.1% / 767 / 40448 | ✅ 一致 |

⇒ **两口径只有 3/185 个会话不同** ⇒ **修正只影响带种子样本，不影响既有结论**（空种子本来无 bug）。
⇒ 脚本内建了**期望值自检**（`⇒ ✅ MATCH`）——比自己报数更可信。
⇒ 顺带印证 §12.4：`bd7d6e6d` **会话累计 81.3%** vs **单次首请求 99.7%** ⇒ "累计 vs 单次"差异真实存在。

### O22：负对照改为**按 `seedLength` 分臂**
旧判据要求"**fork** 子代种子份全部 >0"，而样本里有 **4 个历史空种子 fork** ⇒ **永远不可能通过**（**判据形状错**）。
新：**臂 A（有 seedLength）⇒ 种子份 > 0**；**臂 B（无）⇒ == 0**。
实测：A `n=3` 全 >0、B `n=182` 全 ==0 ⇒ **`negative control passed = true`**。
provider 分组保留为信息列 ⇒ **没有为了让门变绿改样本/改判据** ✓

### ⚠️ 两处残余弱点（别把它当强证据）

1. ★ **臂 B 是"结构性恒真"**：切分只在 `seedLength > 0` 时执行 ⇒ 臂 B 必然全 0 ⇒ **该断言不可能失败 ⇒ 鉴别力有限**。
   **真正有分辨力的是臂 A。** ⇒ 与铁律 #11 同族反面：那里是"**从未为真**=假绿"，这里是"**必然为真**=弱证据"。
2. ★ **臂 A 的 n=3 里有 1 个不是子代理**：`session-432f6207` 有 `seedLength=6989` 但**无 `subagent/descriptor`**
   ⇒ **真·子代理样本只有 2 个**。若要更严，臂 A 应额外要求"有 descriptor 且 `delegationDepth ≥ 1`"（**未做**，子代理如实标为待拍板）。

### 未受牵连
usage 两源归并、种子三列切分、切分标记普查、A/B 并列、机制正对照、全量一览、交叉表**全部仍在**；
且 **182/185 个会话两口径逐字相同** ⇒ 本次修改**增量、可审计**。

⇒ **度量工具至此可信** ⇒ 后面所有实验的读数才站得住。下一步就是 **S1 的代码**（前置 O3 已解），
但**先需要你拍板 D2′（声明面 ⊊ 可调面 是否接受）与"要不要 outputSchema"**。

---

## 二十九、★★★ 用户裁定 D2′ 接受 + 两个新问题的答复（含"孙代会不会累积能力"的机制+实测）

### D2′ ✅ 已接受，且用户的理由比我的**更根本**

> "**我接受啊，这就是我自己提出来的呀。声明面就是要小于可调面，或等于可调面，
> 因为只有这样，我们才可以通过后续追加的方式使子代理的能力大于主代理，然后专门去做专项功能。**"

⇒ ★ **"声明面 ≤ 可调面"不是"一致性妥协"，而是【功能前提】**：只有允许"可调多于所宣告"，
才能**在不改主代脸的前提下**让子代通过追加获得**超过主代的能力**。**D2′ 状态：✅ 定**。

### ★★ 问题 1：子代再调子代，会不会把专有能力带下去？⇒ **不会（既不串台，也不累积）**

**机制**：`composeFrom` 把子代的 scope key **`bindScopeParent` 到 preset 的 standing scope**，
**不是绑到父代 agent 的 scope** ⇒ 子代链 = `[global, standing, 自己]` ⇒ **父代 agent scope 上的注册不在链上**
（`dsh-scope/lib/index.js:162-181` `chainLayers` 逐层合并、**最近者胜**；`:256-262` `bindScopeParent`）。

**实测印证（`out/delegation-reuse.txt:60/82`）**：

| 层 | preset | `tools` 数 | `system` 公共前缀 |
|---|---|---|---|
| depth 1 `436fde97` | council | **103** | — |
| depth 2 `8ac9ffef` ← depth 1 | council | 子 **103** / 父 **103** | **6880/6880 = 100%** |
| depth 3 `8314ffa9` ← depth 2 | council | 子 **103** / 父 **103** | **6880/6880 = 100%** |

⇒ **层层同一张 preset 脸，深度增加不带来任何工具累积。**

**⇒ 两个后果必须同时记住**：
- ✅ **不会串台/膨胀**（能力是显式给的，不是继承累加）
- ⚠️ **也不会自动传承** ⇒ **A 的专有能力 B 也需要时，必须【显式给 B】**（我们控制创建钩子 ⇒ 可做）
  ⇒ **新增 O26：分身的"能力传承"要显式设计。**

### ★ 问题 2："结构化回执有更好还是没有更好？你觉得哪个更好？"（用户要我以"模型更适应什么"回答）

**我倾向【默认不用 `outputSchema`，用固定小标题的文本回执】**，三条理由：
1. **文本能承载"不确定/部分结论/风险"**；schema 会逼着把丰富的东西压进字段 ⇒ **更容易丢掉该说的**。
2. ★ **schema 是"终局约束"**：`attachStructuredRuntime` 要求"结构化输出即终局"，**捕获后单调守卫阻断后续调用**；
   **干净一轮从未提交结构化值会被 driver 改写成 `error`**（§O10 实测）⇒ 对"边探索边回话"的分身是**负担**。
3. **父代是【读】回执，不是【解析】它** ⇒ schema 的主要收益（机器可解析）用不上。

**但两种情形该上 schema**：① 父代要**按字段分支**（"状态=失败 ⇒ 改派"）；② 要**机器校验/统计**（S6 的错误率/返工要自动归类）。

★ **但这事不该靠我表态定** ⇒ **可测判据：① 父代解析失败率 ② 子代因回执约束返工的次数 ③ 回执字段缺失率**。
⇒ 建议把"要不要 schema"做成 **S6 里一个可切换的臂**，用这三列数据决定。**状态：🟡 倾向已给（默认文本），最终由数据定。**

---

## 三十、★★★★★ 「孙代继承了子代的工具面**宣称**、而它其实没有那些工具」—— 上游已有现成机制（逐字）

**用户提的问题**（成立，真缺口）：子代中途加能力 ⇒ 它的上下文里多了"你具备 X"的声明 ⇒
**此时 fork 出孙代，孙代就继承了这条声明**，但 §16.2 已证**孙代的脸 = preset 脸（不带子代追加）** ⇒
**孙代继承了一条【对它不成立】的宣称** ⇒ 它会去调它没有的工具。

### ★★ 上游已有这套机制，语义正好是我们要的

| 证据 | 原文 |
|---|---|
| **新快照显式推翻更早的** | `dsh-system-prompt/lib/index.js:87`：<br>`return \`Current runtime context. This snapshot supersedes earlier runtime-context snapshots.\n\n${body}\`;` |
| **整段失效有专门措辞** | `dsh-agent-loop/lib/index.js:17`：<br>`const CLEARED = "Current runtime context: none. Earlier runtime-context snapshots no longer apply.";` |
| **快照"有主"可识别/替换** | 同文件 `:16,18-20`：`SOURCE="@deepseek-ai/dsh-system-prompt"` + `isOwned(message)` |
| **可跨 resume 恢复** | `:25-47`：`RuntimeContextProjection` 倒扫 `session.events` 取最近一条 surface 可见的 owned 消息 ⇒ `retained {seq,text}` |
| **随事件跟进（⇒可判重）** | `:48-51`：`ctx.on("session/event", …)` 更新 `retained` |
| **provider 每次装配求值** | `dsh-system-prompt/README.md:21`："evaluated for **each eligible assembly**"；产出落**消息面** |

⇒ ★ **"直接告诉他工具面变了"是【有效】的，且不是我们发明的。**
**要害：必须是【runtime-context（每次装配重算、可被推翻）】，而不是【写进历史的一次性声明】。**

### 回答"需不需要在子代时就标记好'这是某工具面'？"

**需要，但用途是【审计与判据】，不是替代快照机制。**
- **标记这半采纳**：给工具面一个**短指纹**写进快照（`当前工具面 face=<指纹>（N 个工具）：…`）
  ⇒ ★ 换来**可机验判据**：比对"日志里模型看到的 face 指纹" vs "当时真实注册表指纹" ⇒ **自动检出陈旧宣称**。
- **失效这半不用我们发明**：新快照自带 supersedes；清空时上游发 `CLEARED`。

### 落地五步（这就是"怎么处理"的答案）

① 分身创建时注册 **child-scoped `systemPrompt.context()`**，内容 = 当前工具面（+自带 supersedes 前缀）；
② 能力集变化 ⇒ provider 重算 ⇒ **新快照追加** ⇒ 旧的被**显式推翻**；
③ 孙代 fork 时，种子里那条旧快照**仍在**，但孙代**自己装配会注入它自己的快照** ⇒ 旧宣称被推翻 ⇒ 不会乱调；
④ 某层 face 变空 ⇒ 上游发 `CLEARED`；
⑤ 审计：face 指纹比对。

★ **全部落在消息面 ⇒ 不改 system ⇒ 不砸前缀**（与 D2′/D3 一致）。

### ⚠️ 残留代价 + 顺势回到用户最早的"压缩点"直觉

**孙代【仍会看到】那条已作废的旧快照** —— 历史只能追加，**推翻 ≠ 删除** ⇒ 历史多一段无效文本，**随深度累积**。
⇒ ★ **它的正确清理位置就是【压缩点】**（压缩本来会把那段消息吞进摘要 ⇒ 唯一免费的时刻）。
⇒ **⇒ 用户最早说的"压缩时再清理"在这里得到了它【正确且唯一】的位置：
不是清理 `system`（那里没东西可清），而是让压缩把"已作废的运行时快照"吞掉。**

### 新增未闭合

**O26**：分身"能力传承"要显式设计（能力不自动累积 ⇒ A 的专有能力 B 拿不到）；
**O27**：face 指纹取法 + 审计脚本；
**O28**：快照"仅在变化时注入"是否真去重（我只读到 `retained` 状态跟踪，**未读到判重那一段**）。

---

## 三十一、★★★★ 机制确认 + 更省的改法 + 「上游更新了我会不会白做」的诚实答案

### 1. 用户的机制描述成立

"这套机制就是**让 LLM 清楚自己现在有什么**：子代时宣告进入新工具面、有了新工具；孙代时告诉它上一个工具面失效了" ⇒ ✅ **成立**，
而且**这正是上游维持正确性的唯一手段**（§17.2 的 `supersedes` 那句话就是干这个的）。

### 2. ★★ 但有一个**更省、更不易过期**的改法（第一原理推出来的）

**关键认识：模型本来就看得到"当前真脸"** —— native 模式 schema 在本次请求的 `tools` 里；code 模式 SDK 段在 `system` 里。
⇒ **runtime-context 那句的价值不是"提供信息"，而是"消解矛盾"**：种子里旧声明说"你有 X"，而本次请求没有 X
⇒ 同一上下文里两条矛盾信息，且**旧的那条当时是真的** ⇒ 模型可能继续信旧的。
⇒ ★ **所以快照应当【强调作废】而不是【重述清单】**：

- 推荐：`工具面已变更为 face=<指纹>；更早关于工具面的声明不再适用。当前可用工具以本次请求的工具列表为准。`
- 不推荐：把工具名再列一遍（与 schema/SDK 段重复、白花 token、自己也会过期）

⇒ 好处：更短 / **指向权威来源 ⇒ 自身永不过期** / 与 face 指纹审计兼容（指纹机验、句子告知）。

### 3. ★★★ 战略问题："上游这么深入，上游更新了，我是不是白做？"

**分三档，风险差别极大：**

**(a) 风险最高：重写上游已有的机制。** 上游手里已有三块料（`presentAs` / `context()` / `tools.register`）+ fork/spawn/compaction，
**我们若去写"脸冻结 + 钉住 SDK 段 + 尾部目录"，上游完全可能做个更好的版本** ⇒ 这正是**铁律 #17 那条红线**。**⇒ 别在这条路上投重兵。**

**(b) 风险极低：我们真正产出的东西 —— 它们不是"功能"，是"判据与量测"。**
本机 `chars/token≈2.87`、脸 40.2K（tools 94%）、fork 种子复用 **1:1**、`end-seed` 与 mode 的**伪**相关、
`complete` 段**静默作废**钉住、压缩信号历史为真 **202 次**、`node -e` 被 shell 吃引号、`chars/4` 估错 **40%**、
主表首请求取错事件、**用小种子否定长上下文主张**……
⇒ ★ **这些是"这台机器 + 这套配置下的物理事实"与"我们踩过的坑"。上游更新不会让它们失效，只会让我们重新量一遍；
而它们的功能恰恰是【测出这次更新有没有把我们的假设弄坏】。** ⇒ **上游永远不回答"我们的不变量是否被破坏"。门是长期资产。**

**(c) 上游不会替我们做的（它的默认组合不需要）：**
1. **能力传承（O26）** —— 上游子代**刻意绑到 preset 的 standing scope、不继承父代 agent 的注册**（§16.2）⇒ "A 的专有能力怎么给 B"**它不解决**。
2. **压缩点吞掉作废快照（§17.5）** —— 上游压缩是**通用区域压缩**，不专门处理我们注入的快照。
3. **本机复合体**：council preset / 议事厅席位 / switchboard / eval 靶场。

**(d) 策略（可执行）**

| 做什么 | 为什么 |
|---|---|
| ★ 重心从"造机制"移到"**定判据 + 量证据**" | 判据在上游更新后仍有用（它就是用来测更新的） |
| ★ **把本轮判据接进漂移体检** | 已有 `scripts/check-upstream-drift.mjs` + `npm run check:upstream`（铁律 #10）⇒ 接入"脸指纹 / 前缀命中 / 复用比 / 钉住是否被 complete 作废 / 压缩计数" ⇒ 上游一更新**先知道哪条假设坏了** |
| ★ S1–S5 先做"**能被上游变化测到**"的最小实现（观测/门），再做会随上游变动的机制代码 | 让返工面最小 |
| ★ 优先做上游明确没做的两条：**O26** 与 **§17.5** | 由我们特有需求驱动 ⇒ 上游不会替我们做 |

**⇒ 一句话：造通用机制很可能白做；定判据 + 量证据 + 做上游没需求做的事不会白做。**
且用户早就定过这条纪律（铁律 #10：**不追上游版本，只按需合并**）⇒ **收益来自"按需合并"，不来自"抢先实现"。**

### 新增未闭合
**O29**：把本轮判据接进 `check-upstream-drift.mjs`（脸指纹 / 复用比 / 钉住有效性 / 压缩计数）。

---

## 三十二、★★★★ 用户提出「材料树 / 成品树」二分 —— 评估：二分对，但不该是两棵独立的树

**用户提法**：要管理很多不同子 agent，而这个"子"由 skill 树展现 ⇒ **skill 树是不是要两个：材料树（外部 skill 的归一化）+ 成品树（sub agent）？**

### ✅ 二分成立，判据一句话：**"这个东西需要被【执行/委派】吗？"**

| | 材料 | 成品 |
|---|---|---|
| 需要什么 | **被读/被融合/被溯源**（外部来源归一化，要探测上游变更） | **被委派/被评分/被淘汰**（身份、能力面、回执约定、预算） |
| 生命周期 | 会被 absorb 掉 | 会被使用、评分、提升、归档 |

### ★★ 但现有 schema 已经把"账本"和"升格门"建好了（核过 `packages/skill-tree/src/index.ts`）

| 需要的能力 | **现有字段** |
|---|---|
| 材料来源与版本 | `Source` / `SourceFile` / **`SourceHash`**（注释原文："探测上游变更 → `update_pending`"）/ `ImportVersion` |
| ★ **材料→成品的升格门** | **`Tools: ToolDef[]`**，注释原文：**"有 Script/Tools ⇒ 可升格 sub agent"** |
| **多对多映射账本** | `MergedFrom` / **`AbsorbedBy`** / `Status=absorbed`（`isAbsorbed()`） |
| 成品间继承/组合 | `Extends` / `Requires`（一层深） |
| 成熟度与淘汰 | `Score` / `UseCount` / `SuccessRate` / `Level`（L3 = score≥0.7 ∧ useCount≥10） |

⇒ ★ **"成品树"就是这棵树里 `Tools.length>0` 且状态可用的节点的【筛选投影】，不是第二棵树。**
⇒ 与本项目一贯原则一致（权威 vs 投影）⇒ **不能做成第二个真相源，否则两份数据必然漂移。**

### ★★ 真正缺的三件（重点：不是"再加一棵树"）

**(a) 成品没有 sub-agent 实体字段** —— 已核：`skill-tree/src/*.ts` 里 `Agent/Subagent/Preset/Face/Recipe` **零命中**。
缺：席位/身份、**脸指纹**、**回执约定**、**深度预算**、**它能委派给谁（编排）**。

**(b) `Tools` → "脸" 的映射未定义** —— 要变成脸必须有：①注册走管线（D2′）②定 presentation（native/code）③定钉住/刷新时机（D1/D2′）。没这层，`Tools` 只是注释。

**(c) ★★ 最硬的前置：能力不自动传承 + per-child preset 无公开入口** —— §16.2 已实测子代的脸 = preset 脸、不继承父代 agent 注册；O7 同样未解
⇒ **"成品"目前无法真正持有专属能力面** ⇒ **成品树的落地前置 = O7 + O26，没这两个成品树只是名字。**

### 命名建议（"树"会误导）

"树"暗示两个独立数据结构 ⇒ 诱导"两份真相源" ⇒ 必然漂移。建议按**功能**命名：
材料 → **`Sources` / `Shelf`**；成品 → **`Lineup` / `Agents`**；**共用 `Node` schema + 阶段字段**（现有 `Status` + `Tools.length>0`）。

### 落地顺序（不能跳）

1. **先解 O7**（per-child preset 入口）—— 否则成品没有"自己的脸"
2. **再解 O26**（能力传承：显式给 B）
3. **给 `SkillNode` 补成品实体字段**（身份/脸指纹/回执约定/预算/编排），**保持它是投影**
4. 最后才是"管理很多子 agent"的**名册与预算层**（`skill-tree` **刻意不含执行器**，这是新的一层）

**一句话**：二分对、判据已在 schema 里（`Tools` 非空）；**别做两棵独立的树，做"一棵真相源 + 一个可执行投影 + 一份谱系账本"**；
缺的不是第二棵树，而是 **O7 / O26 两个前置 + 成品实体字段**。
