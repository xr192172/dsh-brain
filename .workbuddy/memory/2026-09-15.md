# 2026-09-15（01:00–03:00）一次排查修掉 6 个既有测试红 + 写闸收编

## 用户问题与答复要点

- 「边界扩展：意思是会有文件被遗漏吗？像拼图先定好整体形状、内部用洪水模型填充，为什么还会有缝隙？」
- 「能力自述没看懂」
- 「写闸收编也可以。顺序你自己定」
- 「验收一定要验收通过」
- 「之前有两个失败，那个东西是什么？根因是什么？修好了没有？」

### 答「洪水填充为什么还有缝隙」

用户的模型**对了一半**：拼图的"形状"确实是一次划定的，但拼图存的是**内容**（符号/边），
而内容的**新鲜度**会漂 —— 缝隙不是"形状没画对"，而是三类**时间性**问题：

1. **圈定之后才出现的文件**（新文件不在任何块里）；
2. **上次填充被预算截停**（`maxFiles`/`maxMs` 撞上限 ⇒ `noExpand` 终点）；
3. **块内内容过期**（文件被改，块的"形状"没变但"内容"旧了）—— 这才是最主要的，
   也是五层保障（L1a/L1b/L2/L3①②/L4）在解的事。
⇒ 所以"缝隙"分两种：**覆盖缝**（边界扩展要解）与**时效缝**（保鲜五层要解）。别混。

### 答「能力自述」

= P10：工具要**如实说清自己对每种语言能解析到什么粒度**。
例：TS/JS 能给出"函数/类/方法/import/call"级；Go/Python 有 import 边 + 调用；
C/C++/C# 刚进 AST 闭包与 include/using 解析；某些语言只能到"文件级"。
若不说，LLM 会把"**解析不到**"当成"**不存在**"（漏报），或把"解析到但粒度粗"当成精确结论。
落地形态：按语言给一张"可解析粒度表"，并让结果里带 `解析粒度: xx` 标注。**未做**。

### 顺序（我自己定的，理由：先让基线绿，再长功能）

1. ✅ **修那 6 个既有测试红**（"验收通过"的前提：基线不绿就没法宣称验收）
2. ✅ **写闸收编**（正确性，封闭面小）
3. ⏳ **L3② 精确化**（11 个同步读工具要改 async，波及面大；本轮改用"先把召回回归修掉"）
4. ⏳ **边界扩展**（需要持久化拼图边界/`noExpand` 终点，是设计级改动）
5. ⏳ **能力自述**

## ★★ 本轮最重要的发现：一次排查修掉 6 个既有测试红

### 排查方法（可复用）

**逐工具计时 + 边跑边落盘 + 单工具超时**：
之前一把梭跑全工具循环，一挂就什么都看不到；改成每跑一个就 append 到 jsonl，
被 kill 也留证据。这一步直接把"神秘 5 秒"定位到具体工具。

### 根因 ①：Windows 上 spawn **不在 PATH 上**的命令 = 白等 ~5.1 秒（不是"立刻失败"）

实测（逐工具）：`run_tests` 120103ms（无参=跑全量套件，工具自身职责）/
`harvest_from_url` 5183 / `harvest_decisions` 5169 / `find_references` 5189 / `move_symbol` 5171。
后四者共同点：`resolveProjectRoot` → `gitRootOf` → `execSync('git rev-parse ...')`。
单独测 `resolveProjectRoot(cwd)` = **5112ms**，其余原语 10~35ms ⇒ 铁证。

**修法**：新增 `src/tools/exec_guard.ts`
- `findOnPath(cmd)`：**纯 fs 查 PATH**（微秒级，一次 spawn 都不发）
- `commandAvailable(cmd)`：确认存在才 spawn 一次 `--version` 复核；结论**每进程记忆化**（含否定结论）
- `gitRootOf` 改 **① 向上找 `.git`（目录或文件，O(深度) existsSync）→ ② 走查落空才 spawn**
- `harvest_decisions.gitLogEntries` / `harvest_from_url.gitOk` 同样过闸
实测：`resolveProjectRoot` **5112ms → 1ms**；三个工具 ~5.2s → ~0ms。
★ 踩坑：`exeExts()` 在 Windows 上多了个空扩展名 ⇒ `path.join(dir,'')` === dir 本身
⇒ `findOnPath('')` 把**目录**当成"命令存在"。空串只能给 POSIX 用。

### 根因 ②：`find_references` 的**召回回归**（跨语言引用静默漏报）

`indexCandidateFiles` 只走 import 反向闭包；"没有 import 边的引用"——最典型是**跨语言**
（Go/Python 直接调 TS 导出函数名，磁盘上不存在任何 TS import 边）——**永远进不了候选集**。
零前置冷启让索引从"空"变"非空"之后，快路径接管、原先的"全闭包回退"被绕过 ⇒ 漏报。
代码注释还写着"绝不再回退全闭包逐文件即时解析"（性能取舍），**不该以静默漏报为代价**。

**修法**（正是已设计好的"两级索引"）：
候选集 = import 反向闭包 **∪ 文本提及补召回**（`refs_text.scanTextMentions`，
有界 `TEXT_SCAN_MAX_FILES=3000`）；真引用仍由细层（AST/文本探针）判定；
**召回边界如实上报**：`candidateScan = {closureFiles, textAdded, textScanned, textBounded}`，
截断时绝不假装完整（对应"不撒谎"不变量的"标注"半边）。

### 结果

- 21 文件 **250/250 全绿**、tsc 干净（35s）。比基线多绿 **6 项**：
  find_references 3、stale_build 1、project_root 2。
- 仍红 1 项：`harvest_from_url` 的 git URL e2e —— `git clone` 对 `file:///C:/...` 生成
  `'/C:/...'` **前导斜杠**路径（Windows 路径格式），**既有问题、与本轮无关**，待单独修。
- 提交：design-canvas `e10b379`。

## 写闸收编（edit_code / rename_file / symbol_move）

这三个工具此前**自己 syncFile，但从不重开引用方** ⇒ 被改符号的引用方边被 FK 级联删掉后
不会重建 ⇒ `find_references` / `impact` 静默漏报（与 §8.2 同根）。
新增 `write_gate.reopenAndResolveAfterWrite`（绝不抛错，失败写 `error`，调用方必须带进结果）
+ `reopenNote`（人读短注）。edit_code 5 处、rename_file 1 处、symbol_move 1 处已接。

## 教训（可复用）

1. **"测试红"必须先定位根因再动手** —— 两个都是真 bug（性能/召回），不是测试坏。
   若当初按"环境问题"忽略，就一直带着 5 秒/次的隐性成本和跨语言漏报。
2. **逐工具计时 + 边跑边落盘** 是定位"哪个调用慢"的唯一可靠手段（一把梭看不到）。
3. **Windows spawn 不存在命令 ≈ 5 秒**：所有 `execSync` 都要过 `exec_guard`。
4. **测"注入型逻辑"必须给它单测**（exec_guard 的 `findOnPath('')` bug 就是测试抓出来的）。
5. **改测试超时/排除表要写明理由**（run_tests 无参=跑全量是它的职责，不该由注入用例驱动；
   排除表还加了"必须真实存在"的断言，防用它掩盖坏工具）。

## 03:00–04:20 L3① 结构性精确化（用户选了先做三）

### 方案：不在 11 个同步入口逐个改 async，而是**结构性**解决

原计划是给 17/11 个"直接开库没保鲜"的读工具逐个加 ensureProjectIndex —— 但它们多数是**同步签名**
（diff_impact / extract_contracts / harvest_closure / language_concepts / query_feature / function_outline /
analyze_monolith / derive_mind_map），改成 async 会波及大量直接调用的测试。

**改法**：在 `registerAllTools` 的**唯一入口**，调 handler 之前：
若入参带项目根 && `hasLiveIndex(root)` && `!def.noAutoFresh` ⇒ `await ensureProjectIndex(root, {bootstrap:false})`
全部 60 个工具自动精确，**以后新增工具也不用记得**（结构保证，不是自觉）。ready 态实测 ~35ms/次（390 文件）。

三条纪律（写进注释）：
- bootstrap:false —— 绝不因一次调用就冷启建索引（hasLiveIndex 双保险）
- 失败静默不阻断 —— staleIndexWarning 仍兜底标注
- **noAutoFresh 逃生口**（ToolDef 新字段）：
  - index_integrity：refresh:false 必须纯只读 —— 若被自动保鲜，它报告的是"修完之后"而非
    "LLM 马上要读到的"现状，**那是另一种撒谎**
  - import_project：自己做全量导入，前置保鲜纯属浪费

### 测试（3 项，tests/server_registry.auto_fresh.test.ts）

- 绕过写闸改文件 ⇒ 调**任意**带 project_dir 的工具 ⇒ 索引自动跟上（断言 db 的 size === 磁盘 size
  —— 这是"结果精确"，不再只是"标注可能旧"）
- noAutoFresh：index_integrity({refresh:false}) 报告的是**调用前**现状（索引没被动过）；refresh:true 才修
- 没索引 ⇒ 不冷启（绝不因一次调用凭空建索引）

★ 踩坑：连接池持有 cache.db 句柄 ⇒ Windows rmSync 报 **EBUSY** ⇒ 清理前必须 `closeAllProjectCacheDbs()`

### 验收

24 文件 **289/289 全绿**、tsc 干净（27.3s）。提交 design-canvas `69d0c9c`。

### 至此 L3 两条路

① 自动保鲜（**精确**，本提交）② staleIndexWarning（**标注**，兜底 —— 保鲜失败/无索引时仍能看到提示）
⇒ "不撒谎"不变量的两半都有了**结构性**实现：结果精确（L3①）+ 兜底标注（L3②）。

### 仍缺（extreme 文档 §5 已更新）

② 可信度自动附到 impact_analysis / rename_* / diff_impact 输出
③ 边界扩展（需持久化拼图边界 / noExpand 终点 —— 设计级）
④ 能力自述（P10）
⑤ 同步工具直连 L1a（预热 parseFileFull）
⑥ code_workbench / refactor_pipeline / scaffold 接闸
⑦ harvest_from_url 的 git clone 把 file:///C:/... 生成 '/C:/...'（前导斜杠）⇒ Windows clone 失败（1 项既有红）

---

# 2026-09-15（04:40–05:00）追加：L0「首次接触 ⇒ 后台建索引」落地

## 背景与拍板

- 用户问 `bootstrap:false`（"绝不因为一次调用就冷启建索引"）的含义 → 厘清：纪律管的是**起点**，
  不是"要不要建"。用户模型：创建工作区就建索引；急用时拼图兜底。
- 用户拍板："白跑一轮索引对 LLM 是免费的，只是机器后台跑一轮任务" → **首次接触就该起后台建**。
  真相：之前**没有**这个机制（索引只能从"第一次读"开始；"工作区创建"没有钩子）。

## 实现（design-canvas）

- `server_registry.ts`：
  - 新增 `firstContactBackfill(root)`（导出，可测）：带 `project_root` 的调用若 `!hasLiveIndex(root)`
    且根是真实目录 ⇒ `scheduleBackfill`（幂等单飞、分小批、unref、**不阻塞本次调用**）；
    kill-switch `DC_AUTO_BACKFILL=0`；`noAutoFresh` 工具不触发（index_integrity 纯只读纪律）。
  - `registerAllTools` 包装器：handler 前先 `firstContactNote = firstContactBackfill(rootArg)`，
    注入响应（在 staleIndexWarning 之前）。
  - **诚实标注** `backfillProgressNote`：在建中每次调用都标"后台建索引进行中 (done/total)，
    本轮结果可能不全" —— 这是**空缺型不全**（查不到 ≠ 不存在），staleIndexWarning（落后型）覆盖不了。
  - L3① 自动保鲜加闸：`isIndexIncomplete(root)` 时**跳过**逐调用保鲜 —— 后台循环本来就在持续同步，
    跳过省掉每次全盘走查 + 避免 `MAX_ADDS_PER_REFRESH` 噪音。
- 副产品：冷启 2000 文件上限截断的大仓，后台续建会自动把剩余部分补完。

## 验收（全部绿）

- 新测试 `tests/server_registry.first_contact.test.ts` 5 项：入口触发+标注 / 30 文件后台真补完 /
  建中持续标注 / kill-switch / 幻觉路径。
- 回归：auto_fresh(3)+stale_index_warning(7)+write_gate(9)+index_integrity(7)+stale_build(4) = 30 全过；tsc 0 错。
- 全量 2071 项：2032 过 / 11 失败（5 文件）—— **低于**既有基线（18/9），失败全是 git/网络环境类
  （本次 shell 的 PATH 没带 git，`git init` 直接报"不是内部或外部命令"）。与本次改动无关。
- 文档 `docs/index-freshness-extreme.md`：五层 → **六层保障**（新增 L0 行）、L3① 行补"在建时跳过"、
  §5 加第 0 条（已落地）、§6 验收表加 first_contact 行。

## 下一步（继承既有清单）

② 可信度自动附注（impact_analysis/rename_*/diff_impact）｜④ 边界扩展｜能力自述 P10｜
写闸收编剩余 3 工具｜harvest_from_url 前导斜杠｜open question：watch_project 的 scopeToIndex 默认值。

---

# 2026-09-15（05:05–05:15）追加：② 行动工具可信度自动附注落地

## 实现（design-canvas，`server_registry.ts`）

- `ToolDef.trustAnnotated` 标在 4 个"结论会被拿去行动"的工具：`find_references` / `impact_analysis`
  / `rename_symbols` / `rename_files`。
- `trustNoteFor(root)`（导出可测）：陈旧断言（`resolved` 但 `reference_name` 已不在 `nodes`）> 0 ⇒
  响应附 `⚠️ TRUST：N 条陈旧断言 ⇒ 结论可能静默漏报 + index_integrity({refresh:true}) 修复`；
  健康/无索引 ⇒ 空串。**纯 SQL 计数、毫秒级**。
- 注入点：wrapper 里 handler **之后**算 —— rename_symbols 自己会修陈旧引用，附注必须反映
  "修完之后"的现状。
- **分工不混**：staleIndexWarning 管"索引落后于磁盘"（stat 可见，全部工具）；TRUST 管
  "索引自身内部不一致"（保鲜路径看不见，唯一线索是那批行本身）。
- **刻意不设缓存**：缓存会让附注在修完后还报旧数（附注自己撒谎）。宁可每次查。

## 验收（全绿）

- 新测试 `tests/server_registry.trust_note.test.ts` 5 项：flag 结构声明 / 健康⇒无注 /
  陈旧⇒TRUST+修复指引+无缓存两次都报 / refresh:true 修完⇒注消失（闭环） / 无索引⇒不注。
- 回归 7 文件 47 项全过（含 find_references 12、first_contact 5、auto_fresh 3…）；tsc 0 错；dist 已重建并验证含新代码。
- 文档 §5-② 标已落地 + §6 验收表加 trust_note 行。

## 至此"极致"清单进度

L0 首次接触建索引 ✅ ｜ L3① 精确保鲜 ✅ ｜ ② 可信度附注 ✅ ｜ 写闸收编（edit_code/rename_file/symbol_move）✅
剩余：④ 边界扩展（设计级）｜ 能力自述 P10 ｜ 写闸收编剩余 3 工具（code_workbench/refactor_pipeline/scaffold）
｜ scopeToIndex 默认值待拍板。（harvest_from_url 前导斜杠已修，见下）

---

# 2026-09-15（05:12–05:16）追加：harvest_from_url 前导斜杠修复（最后一个非环境类失败清零）

## 根因（带 git 的环境实测复现）

`git clone "file:///C:/Users/.../brick-repo-x"` ⇒ `fatal: '/C:/Users/.../brick-repo-x' does not
appear to be a git repository`。Git for Windows 解析 `file://` URL 时把路径部分当 **POSIX 路径**
（剥 `file://` 剩 `/C:/...`），Windows 语义下必失败。

## 修复（`src/tools/harvest_from_url.ts`）

- 新增 `fileUrlToLocalPath(url)`：`file:///C:/foo` → `C:/foo`（decodeURIComponent；其余形式返回 null
  原样走 URL 分支）。
- `shallowClone`：本地路径直接 clone（**不带 `--depth`** —— 本地克隆走硬链接，`--depth` 本就被
  git 警告忽略）；远程 URL 分支保持 `--depth 1`。
- 验收：`harvest_from_url.test.ts` **11/11 全绿**；`project_root.test.ts` 带 git 环境 31/31
  （此前报错纯因测试进程 PATH 无 git，代码无错）；tsc 0 错；dist 已重建验证。

## 全量套件剩余失败口径（更新）

11 失败（5 文件）**全部为环境类**：readme_tools_gate/project_root（测试进程 PATH 无 git）、
behavior（python 夹具）、archify_demo（ARCHIFY_ROOT 未配置）。**非环境类失败 = 0**。
带 git + python + ARCHIFY_ROOT 的正常开发机上应全绿。

---

# 2026-09-15（05:20）追加：scopeToIndex 默认值拍板（问题关闭）

用户问"你是使用这个工具的人，你希望 watch 展开还是不展开"⇒ **拍板：默认展开（scope_to_index
维持 false），不改代码**。理由：① 显式起 watch = 意图"给我盯住"，收窄会留"等后台/reconcile
才追上"的时间窗；② L0 之后"保护拼图惰性"的理由消失（反正马上全被后台补齐）；③ 收窄只剩
"超大仓库只关心一块"场景，保留参数当 opt-in。已在 docs §4.1 记录。

同轮给用户的口径（未实施，等排期）：
- **边界扩展优先级下调**：L0 后覆盖缝由后台续建兜，剩余价值 = 超大仓的 noExpand 终点续长，
  建议缓一缓。
- **能力自述 P10**：按语言解析粒度自述表 + 结果标注（"解析不到 ≠ 不存在"），L0 解决不了它
  （那是解析器能力不是覆盖），仍值得做。
- **写闸收编剩余 3 工具**：code_workbench/refactor_pipeline/scaffold 的写盘点位接
  writeSourceFiles/reopenAndResolveAfterWrite + indexWriteThrough + 测试，机械活。

---

# 2026-09-15（05:25–06:05）追加：写闸收编剩余 3 工具落地 + P10 收尾

## ★★ 资产定位教训（差点改错仓库）

动手前 Glob `D:\project_develop` 找 refactor_pipeline.ts，**唯一命中是 `dsh-brain\design-canvas-dev`（副本）**——
真身 `D:\project_develop\design-canvas` 搜不到（原因未明，疑似搜索工具默认忽略规则）。dev 副本里没有
firstContactBackfill/trustNoteFor ⇒ 靠"grep 本轮特有符号"识破。**判资产必须先读 topics/asset-topology.md，
动工前必须在候选仓 grep 本轮新增符号二次验明真身。**（本次幸运：三个目标文件 dev 与真身逐字节一致，
前期通读有效；server_registry 等其余文件差异巨大，改错副本=白干。）

## 写闸收编（三工具结论各异，各按其形）

- **refactor_pipeline**（L1a）：`runStage` 每步**终态**（applied 或回滚后）跑一次 `syncSelfWrites(proj, gateFiles)`，
  gateFiles = 内容改写 ∪ 移动 from/to（from 已不在盘 → 按存在性从索引移除；to 新内容；回滚分支 hash
  未变 ⇒ skipped 零成本）。★ 只在终态跑一次——中途写穿会在回滚分支白做一轮。异常 → `recordSelfWrite`
  L1b 兜底；结果附 `StageResult.index_note`。已知边界：纯移动时引用方若不在 absToNew 里，其旧边被
  FK 级联删且无法按名重开——由管线自身 import 改写 + L4 reconcile 兜底（仍比收编前严格更好）。
- **scaffold**（L1b）：同步签名 ⇒ **写前**快照 + `snapshotAndRecordSelfWrite`（登记"计划写入集"，
  消费方按 hash 判定、被跳过的 no-op 无害；写前才能保住 overwrite 场景的可撤回）。新增 `project_dir?`
  入参（schema+handler 透传，顺带让 wrapper 的 L0/L3① 对 scaffold 生效）；根外/无索引安静跳过；
  登记结果进返回消息。
- **code_workbench 无需接闸**（结论而非代码）：approve 的真实源码写入全委托已闸的 renameFile/editCode；
  dsl_rename/dsl_intent 只改 .design-canvas store（非源码）。已写注释防未来重复排查。
- 新测试 `tests/tools/write_gate_adoption.test.ts`（5 项全绿）：applied ⇒ 索引随盘 + 引用方重开
  （旧名 failed 而非 resolved 假象）+ 再写穿全 skipped；rolled_back ⇒ 索引零扰动（引用边维持 resolved）；
  纯移动 from 移除/to 入索引；scaffold 登记可见 + 根外不登记。

## P10 收尾

tsc 重建 dist（0 错）+ dist 验证含新代码；index_integrity/parse_capability 回归全过；docs：P10 表行 ✅、
§5-④ 标 ✅（三档自述 call/symbol/none + 接线点）、§5-⑥ 全收编 ✅、§6 加 parse_capability/write_gate_adoption
两行、write_gate 行修正 9 项。

## 验收

- 定向 60 项全绿：write_gate_adoption(5)+write_gate(9)+refactor_pipeline(17)+index_integrity(7)+
  first_contact(5)+trust_note(5)+auto_fresh(3)+parse_capability(9)。
- 全量 2090 项：2051 过 / 11 失败（5 文件）**全部环境类**（本 shell 无 git/python/ARCHIFY_ROOT），
  与基线完全一致，非环境类失败 = 0。
- 提交 design-canvas `3b70664`。

## ⏭ 剩余（"极致"清单）

④ 边界扩展（设计级，已拍板缓）｜⑤ 同步工具直连 L1a（预热 parseFileFull）｜L3② 从标注升级为精确
（diff_impact 优先——它给行动建议，读旧图最危险）。

---

# 2026-09-15（06:10–06:40）追加：清理前内容级验证（用户问"dev 副本怎么还没合并进来/有没有遗漏资产"）

## 回答"为什么没合并"

因为**没有可合并的东西**——已做 blob 级铁证（详见 topics/asset-topology.md 新增节）：
47 个差异/独有文件里 43 个的 dev 版本 blob 全部在真身对象库（= 旧快照，随时可从真身历史恢复）；
仅 4 个一次性诊断小文件独有（共 ~1.6KB），其中 `_sigfix.test.ts` 守护的双冒号修复已实测在真身
dist 生效 ⇒ 4 个皆可弃。**"还没合并"的真正原因是：删除步骝始终在等用户确认，从未执行。**

## 验证方法（可复用）

- 证明"副本无独有内容"：`git hash-object <副本文件>` → `git cat-file -e <hash>` 查真身对象库。
  blob 在 ⇒ 内容可从真身任意历史恢复。别用 mtime 下结论。
- 坑：管道里裸 `sort` 在本机解析到 Windows sort.exe（`-u` 被当文件名）⇒ 管道断流、静默得到空结果；
  改用 `awk '!seen[$0]++'`。diff 对含空格路径输出带单引号（`'C:/...'`）⇒ `grep -F "Only in $P"`
  匹配不上，要按带引号形式过滤。

## 遗漏资产扫描结论（无实质遗漏）

- 归档区 `.agents/skills/archify/` = Downloads `archify/archify/` **内层目录字节级副本**
  （0 个文件内容差异）；Downloads 原件带上游 .git，是公开仓可再 clone，且**不在清理脚本目标内**。
- Downloads `weavepet/` = 空壳（package.json 0 字节，其余是 D:\project_develop\weavepet 子集）；
  `design-canvas-main.zip`(1.5MB) = 8/27 过期 zip —— 两者同样不在脚本目标内。
- dev 的 `.design-canvas` 仅 68K cache（无 features/bricks 成果数据，成果在归档区 workspace-data 25.8MB + 真身 store）。
- Downloads 里的 keyhub-local/ 等其他目录属未知项目，不在 design-canvas 资产族范围。
- ⇒ 799MB 三路径（① Downloads design-canvas-main 712MB ② dev 17MB ③ dsl-workbench 70MB）删除安全；
  默认回收站模式连那 4 个小文件都可恢复。**等用户确认后执行 `cleanup-redundant-workspaces.ps1 -Yes`。**
  脚本实际路径：`D:\project_develop\dsh-brain\scripts\cleanup-redundant-workspaces.ps1`（带 BOM ✓）。

---

# 2026-09-15（06:40–12:15）追加：清理实跑中断 → 用户拍板手动删 + 剩余清单勘误

## 实跑实录（脚本只跑到 Step 2 就死了，三目标一个都没删）

- Step 0 全过（归档 747 文件/46.9MB 完整、真身 HEAD=3b70664）｜Step 1 计量：① 713MB/17434 文件、
  ② 17MB/1099、③ **残余 63 文件/2MB**（.git 已不在，比 09-14 的 70MB 缩水；674 行未提交改动
  的 patch 兜底在归档区 `_patches/dsl-workbench-orphan.patch`）。
- **Step 2 中断（exit 1）根因**：PS 5.1 `Remove-Item -LiteralPath <junction> -Force` 对 Junction
  抛异常 + `$ErrorActionPreference='Stop'` ⇒ 脚本终止。**修脚本的话应改用
  `.NET [System.IO.Directory]::Delete(path, $false)`（只删重解析点、绝不递归目标）**。
- 工具层坑 ×2：① `cmd.exe` 被 PowerShell 工具安全策略拦截（rmdir 方案作废）；
  ② 脚本 `*>` 重定向写出 **UTF-16** ⇒ Read 判二进制 ⇒ `iconv -f UTF-16 -t UTF-8` 转码后可读。

## 用户拍板

**不跑脚本，三个目录用户自己手动删**。Junction 已由我用 .NET 方式安全解链（目标存活）⇒
①② 手动删除互不影响。清单（含清单外顺带项）已写入 topics/asset-topology.md。

## ★ 剩余清单勘误（防未来追已闭合项）

本日更早前"⏭ 剩余"里的「L3② 从标注升级为精确（diff_impact 优先）」是 **09-14 交接的陈旧条目**——
extreme 文档 §5.1 已标 ✅：L3① 结构性自动保鲜（registerAllTools 唯一入口，60 工具全覆盖）09-15 凌晨落地。
**"极致"清单真实剩余只有两项**：§5.3 边界扩展（拍板缓）＋ §5.5 同步工具直连 L1a（parseFileFull 进程启动预热）。

## 接下来菜单（已呈用户拍板）

A. **verify-boot-health 漏判启动失败修复**（保险本身失效——gen-3083 事故实证：崩了仍报 ok、
   回滚未触发、整套下线）＋插件 schema 对"无配置"健壮性（两个上游问题都"尚未动"）
B. P0-4 模糊编辑级联（先核查 edit_code 现状 + `op='replace_text'` 是否死代码）→ P1「修复→规则沉淀」
C. §5.5 parseFileFull 预热直连 L1a（小活，收掉"极致"清单）
D. P3 注册门（给存量 spawn/fork/design-canvas 补 acceptance，验收对象含 kind: mcp-server）

## 12:20–13:00 ⑤ 同步工具直连 L1a 落地（用户拍板"先做完一个方向"，design-canvas 线第 ① 项）

**提交 `0f5731c`**（design-canvas，10 文件 +438/−32）。核心发现与结构：

- **单一 await 实证**：L1a 链路（syncFile→parseFileFull→getParser）唯一 await 是动态 import
  语言包；解析与 SQLite 全同步 ⇒ 预热后同步写穿成立。
- **分层**：loader `getParserSync`（只读缓存，**绝不同步 import**）→ kernel
  `parseFileFullSync`/`parserReadyForFile`/`canParseFileSync`/`prewarmKernel`（全部已装语言×全部
  exts；缓存键=原样大小写 `'.'+末段`，与 parseFileFull 一致 ⇒ 闸的判定不会与解析行为漂移）→
  symbols 拆 `syncFilePrelude` + `applyParsedToIndex`（**落库体一份，async/sync 共用**）+
  薄封装 `syncFileSync` → write_gate `syncSelfWritesSync`（与 async 版共用 `finishWriteThrough`，
  ②③④ 口径单点）。
- **★ 预热闸（绝不半同步）**：本批任一存在文件的 Parser 未预热 ⇒ 整批落回 L1b（登记+deferred）；
  删除路径与不支持扩展名不受闸影响。
- **收编**：remove_dead_imports 两处 `idxSync ?? idxPre`（idxPre 兜底按 hash 幂等无害）；
  scaffold `inRoot` 提到写循环外、INVARIANTS 落盘后尝试同步写穿（成功 ⇒ indexNote 升级
  "同步写穿"）；server_registry `registerAllTools` 开头 fire-and-forget `prewarmKernel()`。
- **测试**：write_gate_adoption 5→8 项（⑦ resetKernel 后未预热 ⇒ deferred+登记；⑥ prewarm ⇒
  synced+引用方重开+再写穿全 skipped；组合 removeDeadImports ⇒ synced 且 files.hash 已更新）。
  目标套件 64/64；**全量 2093 项 = 2054 过 + 11 失败，与基线完全一致**（git 不在 PATH /
  npx cmd / Go 网络类，无新增失败）。docs §5.5 ✅ + §6 表同步。

**工具层新坑（连续踩 2 次）**：**同一文件的两个 Edit 并行发 ⇒ 后者基于旧快照覆盖前者**（丢
import 行 / 丢 docs 表格行），且两次都"报告成功"⇒ 同文件编辑必须串行，或发后 grep 验证。

## 下一项（design-canvas 线第 ② 项）

**P0-4 模糊编辑级联**：先核查 edit_code 现状 + `op='replace_text'` 是否死代码 → 然后
P1「修复→规则沉淀」。之后按用户拍板回 DSH 底座线（A：verify-boot-health 漏判 + 插件 schema
无配置健壮性）与 D（P3 注册门）。

## 13:15–14:20 P0-4 模糊编辑级联 ✅（commit `2db7aae`）

- **核查结论**：`replace/delete` 走 AST 符号定位、`range` 显式行号，天然不模糊 ⇒ 级联只做
  **replace_text**（其死代码疑虑 09-14 已排除，op 枚举已修）。
- **新模块 `src/tools/fuzzy_match.ts`**（~315 行）：`locateReplaceText(content, oldText)` 四级定位
  L1 逐字 → L2 空白归一（keyWhitespace：保行首缩进、去行内空白，宽容 CRLF/行尾空格）
  → L3 缩进弹性（keyIndentFree：去缩进+忽略空行，needIndent=true）→ L4 省略号占位
  （`...`/`…` 行分段、段按 L3 键顺序锚定、Set 去重窗口、MAX_COMBOS=64 防爆）；
  含省略号 ⇒ 跳过 L2/L3 直达 L4。`realignNewTextTo`：相对化最小缩进再补命中首行缩进（eol 统一）。
- **edit_code 接入**：歧义即停（candidates>1 ⇒ `不唯一（L3·缩进弹性 匹配到 2 处：行号 2, 7）`）
  + 唯一才动；未命中报错列四级尝试；**诚实回执**：`匹配 L${lv}·${label}` 进消息、L≥2 加
  `⚠ 模糊命中：old_text 非逐字一致`、L≥3 加 `new_text 已按命中缩进重排`；
  **diff "-" 侧用 `m.matchedText`（实际文件片段）而非模型 old_text**；
  L2 不改写 new_text（与 L1 同口径最小偏差），仅 L3/L4 重排。
- **细节**：局部 `eol` 删除复用外层 `detectEol`；server_registry 三处描述同步
  （1765 op 说明 / 1772 枚举 / 1784-1785 old_text·new_text）。
- **测试**：新增 `edit_code_fuzzy.test.ts` 7 项（四级各一 + L2 dry_run 真片段断言
  `-   name: 'svc',` + L3 歧义列行号 + 四级未命中报错），**一次全过**；
  既有 edit_code 27 项宽松断言不受消息变化影响；全量 **2100 = 2061 过 + 11 失败，
  与基线完全一致**（全部 git/npx/Go 环境类）。tsc 零错误。
- **docs**：`agent-code-io-adoption.md` §4 ✅（完整架构+验证记录）+ §采纳顺序第 3 条 ✅。

## 下一项

**P1-7「修复→规则沉淀」**（Grit/Codemod 路线：一次修复 → 可复跑规则 → CI 防回归；
"最差异化，最贴自进化主线"——用户拍板的 P0-4 后继）。

## 13:45–14:00 P1-7 触类旁通：实读 Grit（gritql）实现（为「修复→规则沉淀」校准设计）

- **用户先确认理解**：规则 = 每仓一套自行维护（对，随仓走 + md 自包含可复制复用）。
- **Grit 形态**（getgrit/gritql + docs.grit.io + stdlib openai.md 真身）：
  模式 = 反引号片段 + `$元变量` + `=>` 改写 + `where` 侧条件；规则文件 = **md**
  （frontmatter + ```grit 块 + `##` 测试段 = 成对 before/after 块，测试即文档）；
  `.grit/grit.yaml` 配置启用 + level ⇒ `apply`/`check` 双模式；stdlib 200+ 共享；Rust marzano（tree-sitter）。
- **★ CI 棘轮**：check 只 fail「相对上一提交**新增**的命中」⇒ 先启用不炸 CI、增量修复；
  我们用 baseline.json 复刻（不接 GitHub App）。
- **todo() 半修**：改不动 ⇒ 插 `# TODO: 原因`（三态：改了/标记了/没命中）。
- **他们的缺口 = 我们的差异化**：stdlib 测试**零反例**（手写+人审敢省）⇒ 我们的 LLM 现场长规则
  **必须加反例夹具**；且 Grit **没有自动萃取**（专家手写 pattern）⇒ 萃取是立身之本。
- **定案**：采纳 md 载体 / $hole 元变量（复用 P0-4 模糊级联做片段锚 + AST 覆盖判断）/ 棘轮 / todo 半修；
  坚持 反例夹具 + 萃取；v1 不做完整 GritQL（bubble/or/regex/join）。详见 adoption 文档 §7。

## 14:20–14:35 ★ CI 三平台全红根因定位 + 修复（commit `235bf5b`）

**用户说法需纠正**：他记的是「C/C# 环境导致 CI 过不了」——实查不成立。

- **证据链**（GitHub Actions API，仓库 xr192172/design-canvas，**122 个 run 全 failure**）：
  最近 run 34850169185 → `go-test(go-observe/go-slim)` 全 success、6 道闸门全过、
  **三个平台 node-test 全挂在第 9 步「单元测试 vitest」**。
- **★ 真凶唯一**：`readme_tools_gate --check` 报 `README=63 → 真实=64`；
  dogfood 测试 `tests/scripts/readme_tools_gate.test.ts` 断言 `r.changed===false` ⇒ **必挂**。
  **与平台无关** ⇒ 三平台同时红。**与 C/C# 毫无关系**（C/C# 只是能力矩阵声明项，从不编译）。
  修复：跑非 --check 模式自愈 README 数字（63→64）。
- **本机另有环境类假失败**（CI 上不复现，已顺修）：
  ① 宿主 `ARCHIFY_ROOT` 指向 `Downloads\...\design-canvas-main\.agents\skills`（旧副本）
  ⇒ archify「未装配」用例走 validate 分支假失败 ⇒ **测试加 beforeEach/afterEach 隔离环境变量**；
  ② CI windows runner 无 python ⇒ behavior harness `spawnSync python ENOENT`
  ⇒ **加 python 探测 + `describe.skipIf` 优雅降级**（env 不具备即 skip，不假装正确）。
- **方法学**：加 git 到 PATH 跑全量 ⇒ 失败 9→4，剩 4 个全是 behavior 的 python 依赖 ⇒
  反推 CI（ubuntu/mac 有 python3、windows 无 python）⇒ 只剩 readme 一条真失败。
- **验证**：全量 **2106 项 0 失败**；`npm run build` 0；contract_docs_gate / readme_tools_gate --check /
  capability_scan --check **三闸门均 exit 0**。
- **遗留提示**（非门禁项）：README 分组表只列 42 个（主 8 + 专项 34），22 个已注册工具未进表
  （memory_observe/memory_targets/translate_go_ts/go_originals/recommend_observe_points/
  move_symbol/list_snapshots/rollback_snapshot/index_integrity 等）。
- **教训**：`scripts/*.mjs` 里 `execSync('git rev-parse …')` 在本机需 PATH 带 git；
  **本仓测试会被宿主环境变量污染**（ARCHIFY_ROOT）⇒ 凡读 env 的功能，测试必须显式隔离。

## 14:35–14:50 R5 诊断（用户澄清：R5 = archify 及其基础上开发的组件）

**用户原话**：「R5 是其中一个开发过程的组件，开发效果不理想…我们是拿它做在 design-canvas
前端项目里的一部分的组件」⇒ 问是否屏蔽。选择**先诊断再决定**。

**诊断结论（有实测证据，非推测）**：
- **装配真相**：archify 是**外部 skill**，真身在
  `C:/Users/Admin/Downloads/Browsers/Microsoft Edge\design-canvas-main\.agents\skills\archify\`
  —— **不在 design-canvas 仓库内、不在 CI 里**，靠 `ARCHIFY_ROOT` 环境变量指向。
  （这也解释了早些时候的假失败：宿主 ARCHIFY_ROOT 指向这个旧副本）
- **archify 自身健康**：`archify doctor` **全绿**（15 项 core/template/renderer/validator 检查），
  10 个子命令（render/compare/deliver/preview/validate/migrate/inspect/check/visual-check/guide/brands）。
- **管线本身是通的**：实跑 `runArchifyPipeline`（真 ARCHIFY_ROOT）⇒ **5 类图全部 delivered:true**。
- **★ 真正的问题在产出质量**：对 5 张产物跑 `archify visual-check --json`：
  | 类型 | 结果 |
  |---|---|
  | architecture | ✅ pass |
  | workflow | ✅ pass |
  | dataflow | ✅ pass |
  | **sequence** | ❌ **fail** |
  | **lifecycle** | ❌ **fail** |
  失败诊断 = **`viewer/viewport-overflow`**（severity:error）：
  「渲染产物溢出 1440x900 视口」—— scrollHeight **1376** > innerHeight 900，overflowY=true。
  ⇒ **布局纵向尺寸失控**（不是渲染崩溃、不是 schema 不合）。

**性质判断**：不是"组件坏了"，是**两类图的布局参数没收敛到视口内**。
⇒ 处置建议：**先不屏蔽**——三类已合格；对 sequence/lifecycle 做**定向修**（控制纵向布局高度），
修不动再考虑降级/旁路。另建议把 archify 从"环境变量指向外部目录"改为**仓内 vendor 或显式版本固定**
（否则 CI 永远无法验证这条链路，且宿主环境会持续污染）。

**未决**：等用户拍板处置方向（修 / 降级 / 屏蔽）。

---

## archify 仓内 vendor 落地（09-15 下午，提交 `aa944e8` + `02a1df6`）

用户拍板：**把 ARCHIFY_ROOT 指到仓内固定位置**（= 仓内 vendor / 版本固定）。
范围选「精简 vendor」，接入方式选「仓内默认 + 环境变量可覆盖」。

### 改动

- **`third_party/archify/**`**：69 文件 / **2.18MB**（上游全量 7.5MB）。
  ★ 选 `third_party/` 而非 `vendor/` —— **`.gitignore:58` 忽略了 vendor/**，放那儿进不了库。
- **`third_party/archify/VENDORED.md`**：来源(上游 tt-a1i/archify, MIT, v2.17.0-dev.1)/升级步骤/
  自检命令，可审计。
- **`src/tools/archify_cli.ts`**：`resolveArchifyRoot` 三级优先级
  **显式参数 > ARCHIFY_ROOT 环境变量 > 仓内 third_party 默认**。
  默认根用 `fileURLToPath(import.meta.url)` 向上探 `third_party/archify/bin/archify.mjs`，
  **不依赖 cwd**（装到任意路径都能自定位）。
- **`tests/tools/archify_demo.test.ts`**：
  * `beforeEach/afterEach` 保存并删除 `ARCHIFY_ROOT` ⇒ **隔离宿主污染**；
  * "诚实降级"用例改用 `NO_ROOT='__no_such_archify_root__'`（空串语义已变=走仓内默认，
    不再能表达"找不到"）；断言放宽为 `/Archify CLI 未找到|ARCHIFY_ROOT 未配置/`；
  * **新增用例**：不传 archifyRoot 且无 env ⇒ 走仓内默认 ⇒ **5 类图全部 delivered:true**。
- **`.github/workflows/ci.yml`**：新增一步 **`archify doctor`** 自检
  （vendor 化前 CI 上这条链路必然走"未配置"分支 ⇒ **精简时删多了东西永远看不见**；
  doctor 校验"每类型 renderer+schema+example 三件套"，这次就差点漏 `examples/`）。

### ★ 精简 vendor 的坑（可复用）

`archify doctor` 会校验 **"每类型 renderer + schema + example"**。
所以 `examples/` 不是可选样例 —— 少了它 doctor 直接 `not ready`。
**必须保留的 7 个**：5 个类型示例（web-app.architecture / agent-tool-call.workflow /
cache-miss-request.sequence / product-analytics.dataflow / agent-run.lifecycle）
+ 2 个 architecture compare 夹具（checkout-platform.base/head）。
保留后 doctor 全 [ok] / "Archify is ready."

### 验证

- `env -u ARCHIFY_ROOT` ⇒ `resolveArchifyRoot()` = `D:\project_develop\design-canvas\third_party`，
  拼出 `.../third_party/archify/bin/archify.mjs` ✔
- 仓内副本 `archify doctor` 全 [ok] ✔
- 全量 **2107 项 0 失败**（32 skipped = 无 python 的脚手架类 + 环境类）✔
- 三道 `--check` 门（contract_docs / readme_tools / capability_scan）**全 exit 0** ✔
- vendor 内 69 个数据文件 **BOM 全清** ✔

### 环境约束复用（又踩一次）

- `find third_party -type f | wc -l` 在本机 shell 报「FIND: 参数格式不正确」→ **输出 0 是假的**，
  改 **node 递归统计**才拿到真值 69。⇒ 这台 shell 的 `find` 不可信，统计一律走 node。
- `node -e` 脚本里带反引号会被 bash 抢先插值 ⇒ **把脚本写成文件再跑**（本次即用此法）。

---

## ★ R5 诊断修正：是 **3 类不合格**，不是 2 类（09-15 下午，复测）

早先那份诊断（只有 sequence / lifecycle 失败、scrollH=1376）**测的是 Downloads 那份陈旧副本**。
改用**仓内 vendor** 重测（`env -u ARCHIFY_ROOT`，5 类全渲染 + `visual-check --json`）：

| 类型 | 结果 | scrollHeight vs 900 | 溢出 |
|---|---|---|---|
| architecture | ✅ pass | 900 | 无 |
| workflow | ✅ pass | 900 | 无 |
| **sequence** | ❌ fail | **1586** | **+686（76%）** |
| **dataflow** | ❌ fail | **1302** | **+402（45%）** |
| **lifecycle** | ❌ fail | **1245** | **+345（38%）** |

- 失败诊断全都是 **`viewer/viewport-overflow`**（severity:error，每张重复 6 次）。
- 性质**不变**：不是渲染崩溃、不是 schema 不合 —— 是**纵向布局尺寸没收敛到视口内**。
- ★ **教训**：用"外部副本 + 环境变量"做基线诊断 ⇒ **诊断结论本身也不可信**。
  这正是 vendor 化的附带收益 —— 以后诊断打的是仓内那一份。
- **未决**：仍等用户拍板（定向修布局 / 降级旁路 / 屏蔽）。

---

## R5 挂起（09-15 下午，提交 `dbd7e6a`）—— 用户拍板「保留在项目里、暂不管、别影响后续开发」

用户原话意图：**保留 R5 在仓内**，但要做到「它不影响后续开发、后续开发也不影响它」，
把首尾处理干净，免得日后难以继续开发。

### 处置选择：挂起（不是删、不是屏蔽）

- **为什么不是删**：管线本身是通的（5 类图都能产出、`archify doctor` 全绿），
  只是 3 类图纵向布局超视口 ⇒ 删掉等于扔掉可修的资产。
- **为什么不是屏蔽**：屏蔽（关端点/关工具）会丢掉**对外契约**，且日后恢复成本高。
- **挂起 = 保留一切代码与契约，只把「这条线自己的 28 项单测」做成可一键绕过。**

### 挂起机制（单点开关）

- 新增 `tests/helpers/r5_gate.ts`：`r5Describe()` 包装 `describe`。
  * 不设变量（**默认**）⇒ 照常跑（保留回归保护）；
  * `DC_R5_SKIP=1` ⇒ 整线 `describe.skip`，**组名带挂起标记（报告可见，非静默消失）**。
  ★ 设计理由：**默认必须是"跑"** —— 静默跳过会隐瞒问题（本项目最反对"静默给旧答案"）；
  线没坏就该继续被守着，只有真要绕过时才显式挂起。
- 4 个 R5 测试文件（`archify_{demo,mappers,project,semantics}.test.ts`，**28 项**）改用 `r5Describe`；
  顺手清掉 vitest import 里已不用的 `describe`。
- `package.json`：`test:main`（排除 R5）/ `test:r5`（只跑 R5）。
- `ci.yml`：`archify doctor` 步骤加 `if: DC_R5_SKIP != '1'`。

### ★ 边界：挂起 ≠ 免责（关键判断，别搞错）

**没有**挂起的（继续受 CI 保护，因为它们守的是主线资产/对外契约）：
- `tests/tools/view_inputs.test.ts` —— 测**中性数据层** `deriveViewInputs`，
  该层是**主线资产**（断言就是"与 Archify 无关"）；
- `tests/api/contract.test.ts` + `contract.ts` 的 `archify-demo` 定义 —— **对外契约**，
  `contract_docs_gate` 继续管。
⇒ 挂起的只是"这条线自己的单测"，不是它的契约与中性层。

### 互不干扰的核实（排查出的全部耦合面）

| 耦合面 | 处置 |
|---|---|
| `archify doctor` CI 步骤 | 加 `if` 条件，挂起时跳过 |
| 4 个 R5 单测（28 项） | `r5Describe` 可挂起 |
| `contract_docs_gate`（`archify-demo` 是对外契约） | **保留**，契约照管 |
| `/api/archify-demo` 端点 | **纯 API，无前端调用**（已核实 `fetch` 零命中）⇒ 不会造成 UI 可见故障 |
| `view_inputs.ts` 中性层 | **保留**，主线消费 |
| `capability_scan` | 零 archify 条目 ⇒ 无风险 |

### 验证

- 默认全量：2075 passed / 32 skipped / **0 failed**；
- `DC_R5_SKIP=1` 全量：2047 passed / 60 skipped / **0 failed**（差值正好 28）；
- 单跑 R5：默认 28 passed（10.9s）→ 挂起 28 skipped（0.9s）；
- 三闸门 exit 0；`tsc` 干净。

### 环境约束/坑（又踩）

- ★ `.gitignore:101` 有 `docs/*`（**发布划界**：内部过程稿不入发布仓）⇒
  新文档 `docs/r5-archify-hung.md` **必须 `git add -f`** 才进库
  （既有 4 篇 docs 也是这么进来的）。**凡新增 docs 都要 force-add**。
- Git Bash 里 `node -e` 带正则/`(` /反引号会被 bash 抢插值 ⇒ **脚本写文件再跑**
  （本次又踩两次，已固化）。

### 文档

- `docs/r5-archify-hung.md`（force-add）：R5 是什么/三层归属/为何挂起/开关用法/
  **挂起边界**/日后如何恢复/互不干扰约定。
- README「测试与验证」补 `test:main`/`test:r5`/`DC_R5_SKIP` + 「挂起的线」小节。
- `third_party/archify/VENDORED.md` 已有 vendor 溯源与升级规程。

---

## P1-7「修复 → 规则沉淀」落地（design-canvas `340e476`）

**定位**：把**一次实际修复**沉淀成可复跑规则。这是自进化落到代码改造的形态。

### 与 Grit/GritQL 的对齐与差异（设计校准的产物，别丢）

**采纳四项**（实读 docs.grit.io 校准）：
1. **md 载体**：规则 = 一个自包含 `.md`（frontmatter + 说明 + ```pattern /
   ```replace / `##` 夹具段）⇒ **测试即文档**，可整文件复制到别的仓。
2. **$hole 元变量**：占位代码片段，改写按名回填同一实参。
3. **CI 棘轮**：`baseline.json` 存存量命中 ⇒ **只在"新增命中"上 fail**。
   这样"先启用一条当前就失败的规则"不会炸 CI，而新引入的问题立刻被拦。
4. **todo() 半修**：三态 applied / todo（歧义即插 TODO 注释，不失败）/ clean。

**坚持两项差异（我们的独有）**：
- ★ **反例夹具**：Grit stdlib **零反例**；缺阴性对照就无法证明"只在该改的地方改"。
- ★ **萃取动作**：Grit 的 pattern **全靠专家手写**（无自动萃取）⇒ 我们让 agent
  现场从改动本身长出规则，这是"自进化"的关键一环。

**v1 有意不做**：完整 GritQL（bubble / or / 正则绑定 / join）—— 匹配层复用 P0-4 模糊级联。

### 文件拓扑（design-canvas）

| 文件 | 职责 |
|---|---|
| `src/tools/rule_tokens.ts` | 行/token 归一化小工具（**口径单源**，与 P0-4 L2/L3 同源） |
| `src/tools/rule_library.ts` | md 载体：parse / serialize / load / write + 合法性判据 |
| `src/tools/rule_match.ts` | $hole 匹配核（空白归一 / 同一 hole 同一实参 / `...` 跨行 / 唯一才动） |
| `src/tools/rule_extract.ts` | 萃取：diff 变化窗口 → 标识符抽象 → 自动夹具 → 三关校验 → **逐级放宽梯子** |
| `src/tools/rule_apply.ts` | 三态应用 + 棘轮 baseline + 夹具自检 + 目录遍历（排除派生物） |

**工具（3 个，refactor 线）**：`export_rule` / `apply_rules` / `check_rules`
（走 `wrapData`，不是 `wrap` —— 后者会 **丢掉 data**，结构化结论必须能返回）。
规则库位置：`<project>/.design-canvas/rules/`。

### ★ 核心机制：逐级放宽梯子（"泛化的度不靠猜"）

泛化不是越多越好（抽象多了规则过宽，会把不该改的也改了）。
**从"最多抽象"开始试**，每级用**验收三关**裁决；通过就采纳该级；全败则退化为
纯字面量规则，并如实标 `degraded` + `no_hole` warn。`attempts` 记录每级裁决。

**验收三关**（落盘前的机器判据）：
1. **出生回归**：pattern 必须命中 fixture.before，且改写结果 == after（复现不了这次修复 = 没萃取到）。
2. **反例不命中**：每条 negative 夹具都不得被命中。
3. **幂等**：把规则应用到 after 上不得再命中。

⇒ **三关不过时，即使 `dry_run=false` 也绝不写盘**（"绝不先写了再说"，已单测）。

### ★ 本轮修掉的 4 个真 bug（**都由新写的测试/验证器抓出来** —— 验证器的价值证明）

1. **夹具 scope 错位**：pattern 从"**变化行窗口**"泛化，夹具却用**整份输入**
   ⇒ 1 行 pattern 去改 3 行文本 ⇒ 必然误报 `birth_regression_diff`。
   **修**：pattern / replace / 夹具三者**同 scope**（走 delLines/insLines）。
2. **同行只报最左一处命中**：`f(); f();` 只报 1 处 ⇒ 就会去改第一个
   ⇒ **直接违反"唯一才动、歧义即停"**（会静默改错）。
   **修**：起点从"仅 0"改为"逐字符试"，同行两处都报 ⇒ 上层判歧义 ⇒ 转 todo。
   ★ 教训：**"唯一才动"必须建立在"能看见全部命中"之上**，否则纪律是假的。
3. **缩进补两次 / 漏补**：命中区间是否含行首缩进，必须与实际补缩进
   **恰好互补一次**（补重 ⇒ 2 空格变 4 空格；补漏 ⇒ 整块左移）。
   **修**：新增 `RuleMatch.absorbsIndent` 作为**唯一判据**；并把"命中是首行首个
   非空白内容 ⇒ 行首缩进并入区间"做成归一化（否则同一处命中会以两种 span 报两次 ⇒ 假歧义）。
4. **`diffLines` 的 `kind:'change'` 是死分支**：LCS 把"改一行"拆成 del + ins，
   原 `push` 只合并同类 ⇒ change 永不出现，`diffBlocks` 计数虚高。
   **修**：相邻 del+ins **合并为 change**。

### 验证

- `tsc` 0 错；**全量 2137 passed / 0 failed**（32 skipped = R5 挂起，符合设计）；
- 三条门禁全 rc=0：`readme_tools_gate`（自愈 64→**67**，并把我补的 3 个工具从
  "README 零提及"名单里去掉）/ `capability_scan` / `contract_docs_gate`；
- 新增测试 62 项（`rule_library.test.ts` 50 + `rule_tools.test.ts` 12）。
  **工具层单测的必要性**：回执是人/LLM 唯一能看到的诚实出口（是否落盘、
  泛化有没有降级、棘轮为什么 fail）—— 库函数对但回执撒谎 = 这套机制白做。

### 环境坑（本次新增）

★ **`export PATH=...` 会把 `git` 从子进程 PATH 里挤掉** ⇒ 全量测试 3 个文件
（`project_root` / `harvest_from_url`，用 `execSync('git init')`）报
`'git' 不是内部或外部命令`。**这是"环境类失败"，不是代码问题**。
⇒ 正确 PATH 要带上 `:/c/Program Files/Git/cmd`：
`export PATH="/c/Windows/System32:/c/Windows:/usr/bin:/bin:/c/Program Files/nodejs:/c/Program Files/Git/cmd"`
★ 另外：`vitest` 的 `include` 是 `tests/**/*.test.ts` ⇒ **临时冒烟脚本必须放
`tests/` 下**（放仓库根不会被收集，报 "No test files found"）。
`mv`/`sed` 在这个 shell 不可用 ⇒ 直接用 Write 写目标路径。

### 下一步（P1-7 之后）

按用户拍板的顺序：P1-7 ✅ → 之后是
**verify-boot-health 漏判修复 + 插件 schema 无配置健壮性 + P3 注册门**。
（design-canvas 的"极致"清单只剩 ④ 边界扩展，拍板缓。）

---

# 2026-09-15（下午·续）verify-boot-health 漏判：根因定位（**已坐实，非猜测**）

## 事故重放（全部来自磁盘证据，无推测）

| 时刻 (UTC) | 事件 | 证据来源 |
|---|---|---|
| `08:25:31.328` | `===== BOOT gen=gen-3083 =====` 写入 | `gen-3083/boot.log:599` |
| `08:25:34.072` | switchboard 记 **`result:success`** · 已切换 → gen-3083 | `handover-status.jsonl` |
| `08:25:34.709` | **boot.log 最后写入（mtime）** ← 崩溃文本落盘 | `statSync` |
| `08:25:34.737` | **`gen EXIT code=1`** | `gen-3083/lifecycle.log` 末行 |

⇒ **崩溃文本比 `success` 晚 637ms 落盘**，进程比 `success` 晚 665ms 退出。

## 根因（两个，叠加；**不是**"正则漏了"）

**★ 先排除的假设**：`plugin tree failed to load` / `failed to apply loader entry include`
**本来就在** `FATAL_BOOT_PATTERNS` 里。所以不是模式缺失。跑 `findFatalBootErrors(整段)`
今天读那个文件**会命中**。⇒ 盲区在**时机**，不在**判据**。

1. **★ 时序竞态（主因）**：`verify` 阶段读 boot.log 时，崩溃文本**还没写到磁盘**
   （写发生在 637ms 之后）。⇒ 读到了一个"看起来干净"的段 ⇒ 判 ok。
   这与 2026-09-14 修过的 `verifyStableMs` 是**同一类病**，但那次只修了"进程存活"，
   **没修"文本可见性"** —— 稳定窗再探一次 `probe`，只证明进程还活着（此刻确实还活着！），
   不证明"日志已 flush"。**而且 `fast` 模式把稳定窗整个跳过了**，
   这次事故恰好是 `已快速切换` ⇒ **fast 路径 = 零观察窗**。
2. **★ 缺"正面证据"要求（结构性）**：检查是**纯否定式**——"没命中 6 个致命模式 ⇒ 健康"。
   但"还没写出来"与"写了但没命中的干净启动"**在观测上完全同形**。
   ⇒ 必须要求一个**只有在真正成功启动后才会出现**的正向标记（"引导完成"信号），
   观测不到就**不允许判健康**（宁可回滚，不可放行）。

## 结论（写进后续实现的判据）

> **否定式判据无法区分「干净」与「还没写完」。** 必须叠加正向完成信号；
> 且健康检查必须在**有界等待**内**重读**日志，而不是读一次就定论。


## 修复落地（2026-09-15 下午，任务 A 完成）

**新增** `packages/switchboard/src/boot-health.ts`（判据抽成独立模块，理由：**它是事故的保险本身，
必须可单测**）。三条改动：

1. **有界等待 + 重读**（`verifyBootHealth`，窗口默认 **6s** = 实测 3.41s 的 1.75×）；
   三态 `healthy`/`fatal`/**`unknown`**，**`unknown` 与 `fatal` 同等回滚**。
2. **★ 要求正向完成信号** `dsh web: http://127.0.0.1:<port>`（`BOOT_READY_PATTERNS`）——
   把否定式判据补成"需要证据"。**经验验证**：扫 10 个真实启动段，9 个有该信号，
   **唯一没有的正是崩溃段（零误报）** ⇒ 精确判别器。
3. 新增 3 条**裸根因**模式 + 确定性死亡信号 `spawned.proc.exitCode !== null`（内核回填，无竞态）
   优先于 `kill(pid,0)` 轮询。

**核心澄清**：`plugin tree failed to load` / `failed to apply loader entry include`
**本来就在** `FATAL_BOOT_PATTERNS` 里 —— 同一文件整段读**会命中**。⇒ **盲区在时机，不在判据。**
另：`verifyStableMs` 稳定窗只重探 `probe`（"进程还活吗"，崩溃前确实还活着），
**不覆盖"日志落盘了吗"**；且 **`fast` 把稳定窗整个跳过**，事故恰在 fast 路径。
⇒ 有界等待与 `verifyStableMs` **正交**，fast 不得跳过（`SWITCH_BOOT_HEALTH_TIMEOUT_MS` 可调）。

**验证**：
- `scripts/test-boot-health.mjs` **32 项全通**（含"旧实现会判 ok 的输入 ⇒ 新实现判 unknown"）；
- `scripts/verify-boot-health-on-real-3083.mjs` 在**真实磁盘日志**上跑真实现：
  落盘前 → `unknown`（拦截）／完整文件 → `fatal`（0ms 早退）；
- ★ **对照实验（同一份真实文件、同一时刻状态）**：旧判据 = **健康 → 放行崩溃代**；
  新判据 = unknown → 回滚。**这就是漏判与修复的分界。**
- `tsc --noEmit` 0 错；`check-bom` 209 文件 0 违规；`check:profile` 587 行 /
  `pet` 0 / **`duplicate loader entry id` 0** / `received undefined` 0。

**文档诚实化**：`docs/gen-plugin-tree-partial-failure.md` 原表把措施 ③ 标成"已修"，
**这是假修复** ⇒ 新增 §0.1 记「假修复 vs 真修复」全过程 + 时间轴证据；
`docs/handover-vs-restart.md` 同步加警示 + fast/健康检查正交说明。

**★ 通用教训（可复用）**：
> **对"异步落盘的外部证据"做一次性读取 = 必然有竞态。**
> **纯否定式判据（"没有错 ⇒ 对"）无法区分「干净」与「事实还没产生」。**
> 两者叠加 = 静默放行（最危险的一类失败：无人察觉）。修法是「有界等待 + 要求正向证据」。


---

# 2026-09-15（下）② 插件 schema 无配置健壮性 —— 拆掉 gen-3083 那颗雷

## 做了什么

把 7 个 `@dsh-brain/*` 插件里 6 个 zod 包的 `Config` 套上 `tolerantConfig`
（= `z.preprocess(v => v ?? {}, schema)`），使**缺 `config:` 块不再炸掉整棵插件树**。
`subagent-council` 用 schemastery，**原生就容忍** undefined/null，实测确认，未改。

## 关键实测（三个反直觉点）

1. **裸 `z.object({...})` + undefined ⇒ 抛 ValidationError ⇒ gen 启动即 EXIT code=1**
   （gen-3083 原事故）—— 用**真实的 cordis `resolveConfig`** 复现：2 个 REJECT。
2. **`.default({})` 是更坏的假修复**：不抛错，但返回**字面量 `{}`**、短路内层解析，
   让所有字段变 undefined（静默降级，比崩更隐蔽）。字段级 `.default()` 也救不了 ——
   它只在"对象在、键缺"时生效，对"对象本身是 undefined"根本不进入。
3. ✅ **`z.preprocess(v => v ?? {}, schema)`** 是正解：归一化后默认值照常生效，
   **且类型错仍拒绝**（不是无脑吞配置）。

## 一个既有认知修正（重要）

> **`npm run check:profile`（`--dump-config`）不校验插件 config。**
> 实测：把 capability-bridge 的 `config:` 整块删掉（正是事故形态），
> `--dump-config` 仍 **EXIT=0 / 579 行** —— 它只组装并打印配置文本，
> 不实例化插件、不调 `resolveConfig`。
> ⇒ "check:profile 绿 ⇒ 插件配置没问题"是**错误推论**。

## 产物

- `scripts/check-config-tolerance.mjs`（新，**56 项**）：取真实构建产物的 Config，
  经 **cordis 真实 `resolveConfig`**（同一份生产实现，非仿写）喂 5 类输入。
- `scripts/probe-config-resolveconfig.mjs`（新，真机探针，7 包 × 3 输入）。
- 门禁**回归自证**：换回裸 z.object ⇒ EXIT=1（2 项 FAIL 指名 `缺 config（undefined）`）；
  换回 preprocess ⇒ 56/56 绿。**非空过。**
- 文档新增 §0.2「治本：插件 Config 容忍缺 config 块」+ 修正过时的"config 必须显式写"。

## 两个"检查退化成空过"的坑（写门禁时踩到，可复用）

1. **`.default({})` 返回的 `{}` 没有"值为 undefined 的字段"** ⇒ "扫 undefined" 会**空过**；
   必须做**键集合比对**（`expected.filter(k => !(k in value))`）。
2. **`z.preprocess` 编成 pipe，内层 schema 在 `def.out`（不是 `def.shape`）** ⇒
   内省要下钻；否则探不到字段名，只能发"未知键"，而 **zod 忽略未知键** ⇒ 假通过。

## 验证（全绿）

`check:profile` 582 行 / err 0 / pet 0 / dup 0 ｜ `check:bom` EXIT 0 ｜
`check-config-tolerance` 56/56 ｜ `test-boot-health` 32/32 ｜ 真机探针 0 拒绝。


---

# 2026-09-15（下·续）上游缺陷归属盘点 —— 「要不要提 PR」的答案

## 用户问：这些其实是在修 DSH 官方的 bug 吧？要不要整理出来提 PR？

**答案：提 PR 这条路被官方政策直接封了。** `CONTRIBUTING.md` 原文：

> **We are sorry that we cannot accept external pull requests at the moment.**
> … Identify and report issues or bugs in **GitHub Discussions**.

仓库 `github.com/deepseek-ai/deepseek-harness`（MIT，公开，当日仍有提交）。
⇒ 唯一官方通道是 Discussions 提 bug。另注他们内部用
`.agents/notes/archived/bug-fix/YYYY-MM-DD-<slug>.md` 记缺陷（配 `.zh.md` + `.i18n.yaml`）。

## 更重要的是：先把归属分清（否则报出去是噪音）

产物 `docs/upstream-defects.md`。归类：

| 项 | 归属 | 上游核验 |
|---|---|---|
| U1 `goal-round-driver` `event.data.reason.kind` 缺 `?.` | **上游 bug** | ✅ **确认 master 仍在**（2 处、无 try/catch） |
| U2 `app-boot` 裸 `JSON.parse` 无 BOM 防护 | ⚠️ **待复核** | 上游已重构 + 搜不到防护代码 |
| D1 `web-app` 把**实例端口**写进 system prompt → cache 必 miss | 设计缺口（非 bug） | 单实例下完全正确 |
| D2 `--dump-config` 不校验 config（假绿） | UX 缺口 | 本地实测 |
| O1 switchboard 启动健康漏判 / O2 6 包 Config / O3 压缩兜底 / O4 profile·桌宠 | **我们自己的** | 不可报 |

## ★ 两个诚实性发现（比结论更重要）

1. **U2 的「不报」理由**：上游 2026-09-14 把 boot 重构成
   `packages/boot/app-boot/src/profile-resolution/{service,resolver}.ts`，
   而我们的 patch 打的是 **rc.2 的编译产物 `lib/index.js`**（行号 `:412/:430/:551` 已失效）。
   且在 `packages/boot` 内搜 `FEFF`/`charCodeAt`/`stripBom`/`65279` **全部 0 命中**
   ⇒ 上游无字面量级 BOM 处理；但多处 `JSON.parse` 已被包进 `try/catch` ⇒
   症状很可能从「崩」降级为「静默取不到 manifest」。
   ⇒ **拿 rc.2 的观测去描述 master 的代码 = 「判据与信号不可混」的反面**，故标待复核。
2. **cordis × zod 那条不该报**：cordis 自带的 schemastery **原生容忍** undefined/null
   （本地实测 `subagent-council` 全通过）；是**换用 zod 会静默失去这层宽容**。
   ⇒ 这是「插件作者选型的隐藏契约」= **文档缺口**，不是代码缺陷。

## 上报排序建议

只报 U1（确认+干净+我们手上有触发现场）。U2 复核后再定；D1/D2 走讨论帖而非 bug 帖。
★ 我们的护城河是**真实运行日志与复现前提**（U1 的「交接/中断边界」、U2 的真实 gen 日志）——
那正是上游小团队做不到的一半。

---

# 2026-09-15（晚）P3 注册门落地

## 判据阶梯的能力级实施

设计在 `docs/capability-registry-evolution.md` §5.4 / §9，本次把 L0/L1 接成可执行门禁：

| 级 | 判什么 | 状态 |
|---|---|---|
| L0 机械门 | provider **真跑** `apply(mockCtx, cfg)` 捕获 → 5 成员齐 + `capabilities` 键集与声明一致；mcp-server 走工具面/线索/无漂移 | ✅ |
| L1 不变量门 | `role`/`writeScope`/`credentials`/`budget` 必须显式声明（缺 ⇒ fail-closed）；设计类角色禁 `production` 写权 | ✅ |
| L2/L3/L4 | 基线不退化 / 隐藏 holdout / 反事实对照 | ✗ 未实施 |

★ 不撒谎：回执写 `proofLevel:'L1'` + `unenforced:['L2','L3','L4']`，并把"这些没有被验证"打印出来。

## 三条纪律

1. **注册 ≠ 采纳**：`register`/`init` 只建 `pending`，只有门能转 `active`；
   `acceptance.kind` 只认 `'gate'`。此前 `council-architect` 有一条
   `{kind:'ref', status:'passed'}` 而 ref 只是段文档 = **假回执**，已移除。
2. **未实施的级不得计作通过**（否则就是本项目花两天修的那类"保险自己失效"）。
3. **假红与假绿同等有害**（见下）。

## 本轮两个真问题（都已修）

1. **扫描器过期 ⇒ 假漂移**：`scanMcpSource` 用 design-canvas 改造**前**的正则，
   报「67 工具全部未归线」；权威值 64 且全部归线。
   ⇒ 抽 `scripts/capability-sources.mjs` 单一实现，与 `diff-dc-capability-map.mjs` 同路径。
2. **我自己的判据错了 ⇒ 假红**：「`inheritsParentContext=false` + `credentials=inherit` ⇒ 违反凭据边界」
   是错的（不同维度；in-process 同进程共用凭据是架构事实）。过门只能写假声明 `own`。
   ⇒ **判据做错方向有两面：假绿与假红同等有害；会误报的门终会被绕过。**

## 验证

`check:bom` 0 违规｜`check:profile` 582 行/err 0｜`config-tolerance` 56/56｜
`test-boot-health` 32/32｜**`test-capability-gate` 29/29（两方向）**｜
`registry check` ✅ 无问题｜`gate run --all` **4 admitted / 0 blocked**。

## 记忆维护

MEMORY.md 由 **19,416 字符瘦身**至限额内（原已超限、注入时被截断）：
细节按主题迁入本目录，索引只留「环境约束 + 铁律 + topics 表 + 短交接」。

---

## 2026-09-15 傍晚：P4 设计前讨论 —— 能力清单「追加 + 压缩时物化」

用户提案：工具注入改为动态 —— 初始化注入当前清单；之后卸载/加载各**追加一条指令**；
压缩时对加载/卸载做**零和运算**（抵消），只保留当前状态 = 相当于刷新一遍。问对缓存是否更友好。

**结论：方向对，且比原方案 C 更强；但有一条硬约束决定它能否成立。** 已写入
`docs/capability-registry-evolution.md` **§6.1**（+ §9 的 P4 行）。

### 为什么方向对（是本项目已验原则的外推，不是新赌注）

`prompt-cache.md` 已固化：**外置化/缩减必须在「写入时」append-only，不能事后 replace**；
推论「动态的东西不要放前缀，放尾部或按需」。用户方案逐条对上：
init 清单 = 写入时基线｜变更 = 追加尾部｜压缩时物化 = **借 ①「压缩」那次必要重写**（边际≈0）。

### ★ 硬约束：`tools` 是结构化 header，不是文本

`request/header` 就是发给 LLM 的真实 header（system + tools）；形态指纹 `(system 字符数 | tools 数)`，
Native/Dual 实测 `1789|69~71`。
⇒ **模型能调用什么由 header 的 tools 清单决定；追加一行文字不能让未声明的工具变得可调用。**
（强行调用会被 API 拒 → 模型白烧轮次重试 → **比不提供更坏**。）

⇒ 「追加」只对**通过稳定通用工具可达**的东西成立 ⇒ 仍然需要 `delegate_capability(name, task)`：
- **调用入口**（schema）在 `tools` 段里，**恒定**
- **地址集合**（有哪些名字）走尾部追加，随时变

⇒ **追加方案不取代方案 C，而是补全它**，且强两点：
① 原 C 只管"长尾不进前缀"，追加法连**核心集合变动**也便宜（加通知而非重写 header）；
② 它给了一条**「推」的通道** —— 模型不必先问就知道有什么。
这与刚定下的教训直接相关：**"不知道去哪发现"是最坏的一类失败**（连坐标都没有）。

### ★ 「零和运算」这个提法必须改写：加减**不可交换**

```
+X, -X  ⇒ 最终无 X   ← 净零，可丢
-X, +X  ⇒ 最终有 X   ← 净"非零"；成对相消的手法会把它**错判成零**
```
所以正确形态不是"抵消成对事件"，而是**「上次物化状态 M + append-only diff」**：
判据 = **当前集合 == M?** 是 ⇒ 整段 delta 可丢（更强也更简单）；否 ⇒ 物化一次。
物化时机：① 压缩时（已在付重写代价）② delta 超阈值。
= **op-log + snapshot / 定期物化**（LSM compaction、CRDT op-log 压缩同源），成熟做法。

### 三条正确性不变量

1. **文本承诺的集合 ⊇ 实际可调用的集合**（反之绝不允许）—— 只能宣传真能调通的能力；
   经 `delegate_capability` 中转 ⇒ 名字非法可报错 ⇒ **路由判据仍可执行**（C 相对 B 的核心优势必须保住）。
2. **物化必须与压缩摘要「同一次写入」**。分两步做 ⇒ 摘要可能丢"加载"留"卸载" ⇒ **静默状态丢失**。
3. **物化后回到"干净基线"**（形态 == init 的形态），否则每次压缩换一种写法 ⇒ 又成一个改写源。

### 验收口径（可测）

用 `dump-request-tools.mjs` 扫多轮会话，断言 **`tools` 数在无压缩轮里恒定**
（指纹 `1789|69~71` → `1789|N`，N 不跳），`(system 字符数 | tools 数)` 只在压缩处变化。
⚠️ 诚实边界：本方案**只**消除"能力增删"这一改写源；④ 冷启动/空闲过期是结构性的，不受影响。

### 待定（P4 开工前必须先答）

**「核心 vs 长尾」的切分判据**：按**变化频率**还是按**调用频率**？两者会给不同答案。
用户已同意先定这个判据再写代码（判据定错则后面白做）。

---

## 2026-09-15 傍晚（续）：§6.2 —— 「不进 surface」优于「进去再剔除」

用户追问：*给工具的指令带一条**不显示在上下文中的标记**，由工具去手动合并和剔除，不行吗？*

**行，且 DSH 原生有全套机制 —— 但要把"藏起来的标记"与"根本不写进去"分开。**
已写入 `docs/capability-registry-evolution.md` **§6.2**。

### 关键事实（核到类型定义）

```ts
export type SurfaceEventType = 'user/message' | 'assistant/message' | 'tool/result'
```

⇒ **`surface` = 「产生模型消息」的那三类事件**（不是 UI 层面）。
`dsh-session/lib/types/surface.js` 有 `isSurfaceEvent` / `isAppendSurfaceEvent` /
`isReplacementSurfaceEvent` / `foldSurface` / `deriveEventMessage`。
**不属于这个三元联合的事件类型，按构造就不可能成为模型消息** —— 只在日志里。

⇒ 所以能力增删可以写成一个**自定义非 surface 事件** → **0 token、0 前缀影响**。
（自家先例：`packages/tool-evolution` 已注册自定义会话事件 `tool/review`。）

### 「手动合并和剔除」的现成机制：`surfaceOp` + `sourceEventSeqs`

```ts
SessionEvent = { type, seq, time, data, ignorable?: true }
             & (K extends SurfaceEventType
                  ? { sourceEventSeqs?: number[]; surfaceOp?: SurfaceOp } : object)
```
`surfaceOp` = `'append'` | replace 变体；`sourceEventSeqs` = 本事件取代/派生自哪些更早事件
（= 精确剔除的载体）。非 surface-eligible 事件携带 `surfaceOp` 会**直接抛错**。

### ★ 但别用中途 replace —— 我们踩过

`replace` = 事后改写。实测：`dsh-compaction-tool-result-pruner` 用 `surfaceOp: replace`，
**161 次改写里占 72 次** ⇒ 已 `disabled`（`prompt-cache.md` ② "已消除"）。

### ★★ 最优形态：不靠 replace，靠「搭已有 append 的车」

把"剔除"升级为"**从未进入**"，代价从"一次重写"降到 **0**：
① 能力增删 → 非 surface 事件（0 成本）；
② 折叠**搭在本来就要发生的 append 上**（下次 user/message 或 tool/result），
   作为**该次 append 的内容**写出 —— append 在尾部 ⇒ **前缀一字未动** ⇒ 只付新增那几行；
③ 模型提前需要知道 → `list_capabilities()` 按需（pull 兜底）；
④ 幂等记账 → 记「已折叠到哪个 seq」。

**"从未进入"严格优于"进入后剔除"** —— 标记的真正作用**不是"藏起来"，而是"幂等记账"**。

### 一处纠正

`SessionEvent.ignorable?: true` 易被误读成"不显示在上下文"。实际是**日志前向兼容**：
读日志遇到本 build 不认识的类型，未标 `ignorable` 就**拒绝解释整份日志**
（"likely written by a newer harness"），标了则可跳过。**与模型上下文无关。**

### 合并后的完整方案（§6.2.6）

| 层 | 承载 | 变化 |
|---|---|---|
| `tools` 段 | `delegate_capability` + 核心角色工具 | **恒定**（指纹 `(system\|tools)` 不跳） |
| 能力增删 | **非 surface** 自定义事件 | 随时，**0 成本** |
| 模型可见的当前集合 | 折叠行（搭 append 的车）/ `list_capabilities` | 低频，仅新增行未命中 |

⇒ 上一轮"先定核心 vs 长尾切分判据"的建议可**再放宽**：
**默认全部走 dispatcher**，只有当"经中转真不够用"时才升级成专属工具槽
（判据 = 是否需要 schema 级确定性调用；一旦占槽，改它就要重写 header）。

---

## 2026-09-15 傍晚（再续）：§6.3 架构决策 —— 自建插件，不加深上游补丁

用户问两件事：① 我们依赖这个不稳定版本的内部实现挺多，要不要**自建插件**或**改它**？
② 它现在的上下文管理是 append-only（只追加），**能做到我们说的那个吗？**

已写入 `docs/capability-registry-evolution.md` **§6.3**。

### 答②：能 —— 而且 append-only **是我们的朋友**，不是障碍

关键是把两层分开：

| 层 | 是否只追加 |
|---|---|
| **会话日志**（`session.jsonl`） | **严格只追加**，从不改写 |
| **surface 投影**（`foldSurface` → 模型可见消息） | 可收缩/可替换 |

`surfaceOp: replace` **不是"改日志"**，而是**追加一条「替换意图」**；`foldSurface` 在**投影时**折叠掉。
⇒ 日志仍只追加。而 §6.2 的方案（非 surface 事件 + 搭车折叠）**全是纯追加** ✓。

**唯一与 append-only 语义相冲的是 `replace` —— 而那恰恰是我们不能用的那条**
（事后改写 ⇒ 击穿前缀，§6.2.3）。⇒ **用户的担心正好指向我们要走的反方向。**

### 答①：自建插件，且**不需要改它**

已核实的**扩展点**（全是公开面，不是内部实现）：

| 需要的能力 | 机制 | 性质 |
|---|---|---|
| 观察每次追加 | `ctx.on('session/event', (id, event))` + `session/flush` | 公开 firehose |
| 写自己的能力变更 | `ctx.session.append(type, data, ...opts)` | 公开方法 |
| 自定义事件类型 | 支持（**自家先例**：`tool-evolution` 已注册 `tool/review`） | 既有扩展点 |
| 非 surface ⇒ 不进上下文 | `SurfaceEventType` 是固定三元联合，其它类型**按构造**不进 | 类型系统保证 |

⇒ 这正是"写插件"的用法，不是"改内核"的用法。P4 这部分应做成**我们自己的插件**
（或并入 `capability-bridge`），而不是继续给 `dsh-compaction-basic` 加深补丁。

**理由是可维护性，不是洁癖**：上游是移动靶，且已证实 —— 2026-09-14 上游把 boot 重构成
`profile-resolution/*`，`patch-app-boot-bom.mjs` 引用的行号**当场失效**（见 `upstream-defects.md` §U2）。
我们已有 5 个补丁脚本 + 1 个 500 行 patch-package 补丁，每加一处都是"上游一改就坏且不告诉你"。
且补丁打的是**编译产物**，上游重构后行号/函数名全失效。

### ★ 顺带松开了不变量 2：折叠写「快照」不写「diff」

**内部记账**用 diff（哪些变更还没进模型）；**写出去的是完整当前集合快照**。带来：

1. 幂等且可丢（可重复写、可丢）；
2. ★ **丢掉的后果是「不知道」，不是「以为错」** —— §6.1.4 不变量 2 担心的
   "摘要丢加载留卸载 ⇒ 静默状态丢失"，**在快照形态下不会发生**（快照里没有"卸载"这种相对量）；
   丢了最多是模型不掌握当前清单，由 `list_capabilities` 兜底。
   ⇒ **不需要 patch 压缩插件保证"同一次写入"，这条约束自然消失。**

### ★ 查出一个同类"假绿"（待拍板）

`scripts/patch-*.mjs` **没有任何 `process.exit`** —— 锚点找不到时只打印
`FAIL` / `⚠️ anchor not found` 然后**继续**。而它们挂在 `postinstall`
⇒ **上游一变，补丁静默失效，无人被告知**。与我们反复修的"假绿"同类。
建议：锚点未命中 ⇒ 非 0 退出（`DSH_PATCH_STRICT=0` 可显式降级）。
**待用户拍板**（影响 `npm install` 行为）。

---

# 2026-09-15（夜）P4 开工：补丁严格化 ✅ + 折叠内核 ✅

用户「可以」= 批准两件：① 补丁脚本严格化 ② P4 开工。

## ① 补丁脚本严格化（提交 `4d9feb6`）

问题：`scripts/patch-*.mjs` **没有任何 `process.exit`**，锚点找不到只打印后继续，
却挂在 `postinstall` ⇒ 上游一变补丁静默失效、无人被告知。

修法：`scripts/patch-anchors.mjs` **三态判定**（already / pending / missing ⇒ 非 0 退出，
`DSH_PATCH_STRICT=0` 降级）。
★ 单列 missing 抓出一个**隐蔽假绿**：helper 插了但调用没改 ⇒ 之后每次因 MARK 命中整文件 skip
⇒「有 helper 没人调用」= 毫无防护却永远显示"已打补丁"。现 app-boot 计 5 处，必然暴露。
`all:true` 的顺序要紧：锚点还在就必须继续替换，否则"改了一半"被误判成 already。
顺带：`patch-goal-round-driver` 此前不在 postinstall（补丁存在但装完不打）→ 已挂；
`patch-profile-deps` 加后置校验（原把 `BOM: PRESENT(bad)` 打印出来却不当回事）。

自证 `test-patch-anchors.mjs` **17 项两方向**。★ 测试自身也踩过假绿：
`execFileSync` 成功退出时拿不到 stderr ⇒ "降级告警"恒假通过 ⇒ 改 `spawnSync`。

## ② P4 折叠内核（离线可验）

`packages/capability-bridge/src/notice.ts`（纯函数）+ `scripts/test-capability-notice.mjs`（**27 项**）。

证三件事：
1. **幂等** —— 同状态重复折叠，第二次起 `no-pending`/`dropped`，绝不重复写；
2. **快照非增量** —— 断言文本含全部当前项**且不含"新增/已卸载"措辞**。
   写增量就引入相对量，丢了会让模型**以为错**（而非"不知道"）；
3. **顺序无关** —— 判据是「当前集合 == 上次折叠时的集合？」，不是成对相消
   ⇒ `+X,-X` 与 `-X,+X` 同结果，§6.1.3 的顺序坑消失。
   集合未变 ⇒ `dropped`（不写）**但推进水位**，否则 delta 永远留在 pending 白做功。

## ⏳ 下一步：接线（刻意未做）

接错会把坏插件塞进 boot 路径，而装配链今天刚出过事。待定：
- 观察口 `ctx.on('session/event')`（`SessionStore` 注释明说这是插件观察会话的官方姿势）
- 写入口 `session.append(type, data, ...opts)`；但事件只给 session **id**，
  取回句柄经哪个服务待核（`tool-evolution` 走 `exec.agent.session`，是工具级路径）
- 模块级注册自定义事件类型（照抄 tool-evolution 的 `KNOWN_SESSION_EVENT_TYPES.add`）
- 验收：`dump-request-tools.mjs` 断言 tools 数在无压缩轮恒定。**换代由用户发。**

## 验证（无回归）

check:bom 0 违规｜check:profile 582 行/err 0｜config-tolerance 56/56｜
boot-health 32/32｜capability-gate 29/29｜patch-anchors 17/17｜capability-notice 27/27。

---

# 2026-09-15（深夜）接线路径核实 + 「拆装不确定」根因定案

## ① P4 接线路径核实完毕（阻塞解除）

权威先例 = `@deepseek-ai/dsh-session-persistence`：

```js
ctx.on("session/event",   (session, event) => { ... })   // 回调直接给活的 session 对象
ctx.on("session/flush"|"session/created"|"session/disposed", (session) => ...)
for (const session of ctx.sessions.list()) this.initFor(session)   // 回填既存会话
```

- 服务名 **`sessions`**（`SessionStore extends Service` + `super(ctx, "sessions")`）
  ⇒ `ctx.inject(['sessions'], sctx => …)`
- ★ 我原先担心"事件只给 session id，要自己 get"是**多余的** —— 回调签名就是 `(session, event)`
- ★ **必须回填既存会话**，否则插件加载前就存在的会话永远收不到通知（persistence 正是这么做的）

## ② 「拆装不确定」根因定案 → §6.4

用户问：DSH 宣传的连续性（可热重载 / Agent 自己执行 / 能拆就能装）为什么我们没达到？

**体感对，但它描述「插件层」；连续性在「能力层」是真的。**

- **能力层可逆实证**：`dsh-tool-subagent` 的 `provider-added ⇒ mount()` /
  `provider-removed ⇒ disposeTool()` ⇒ 注销 provider 即自动摘工具 = temporal composability 真的在工作。
- **插件层没有**：① 上游把 `cordis-plugin-hmr` 在 `dsh-web-app/cordis.patch.yml:21-23`
  `disabled: true`（注释自陈 reload lifecycle 还没测通），常驻 client-hmr 只管浏览器侧；
  ② 我们的改动大量落在 cordis 之外（node_modules 编译产物补丁 / bundles / patch.yml /
  profile package.json + pnpm）⇒ 不在 effect 体系，改了不撤销。
- ⇒ 失效模式全是配置层的：duplicate id / no dsh.bundle / BOM / 缺 config / **disabled ≠ 移除**。

**结论**：不是做错了，是**层选错了**。P3/P4 的方向正是把工作搬到"成立的那一层"：
P3 让能力采纳有可真跑的判据；P4 让能力的增删不改写前缀、Agent 自己装卸而不重启。

## ③ 工具层又踩到一坑

**用标题行当 Edit 锚点插入内容时，没把标题写回 ⇒ 标题被吃掉**
（本轮误删 `## 7. 外部 Agent 接入`，其正文被并进 §6；已修复并逐标题核对）。
⇒ 以标题为锚插入时 newString **必须**以该标题结尾；
修文档结构前先 `grep -nE "^#{1,2} "` 列全部标题 —— 只看正文 grep 发现不了标题丢失。

## 2026-09-15 深夜（续）：P4 **接线完成** + §6.5「干净」的两种

### ✅ P4 接线已完成，并被离线自证（16 项）

**关键突破：真正的"搭车位"是 `tools/post-execute` waterfall，不是自己 append 一条 surface 事件。**

```
'tools/post-execute'(exec, result, next) => Promise<PostToolDecision>
```
- 它是**写入前**决定写什么 ⇒ **前缀不动**（与 `dsh-spill-policy` 同一机制，文档明确标它"前缀不动 ✅"）
- 对比 `surfaceOp: replace`（事后改写）⇒ 必击穿 —— 我们正是因此关了 tool-result-pruner
- ★ **surface 只有三类**（`user/message|assistant/message|tool/result`）⇒ 自己造一条会假装成
  "别人说的话"；而 **`additionalContexts` 才是上游设计给插件注入上下文的通道**
  （`dsh-agent-loop` 会 `acceptContext`；先例 `dsh-repeat-tool-reminder`）

**接线的五个确定件**（全部有权威先例）：
1. **非 surface 事件类型**：`KNOWN_SESSION_EVENT_TYPES.add('capability/change')`（照抄 tool-evolution）
2. **服务名 `sessions`**（`SessionStore extends Service` + `super(ctx,"sessions")`）⇒ `ctx.inject(['sessions'], …)`
3. **观察**：`sctx.on('session/event', (session, event) => …)` —— 回调直接给活的 session
4. **写日志**：`session.append(CAP_CHANGE_EVENT, {...})`
5. **取 session id**：`exec.agent?.session.header.id`（spill-policy 的 `ownerSessionId`）
   —— 无 agent 时是 `undefined`（直接/测试调用），要容忍

**通知条目形状**（照抄 repeat-tool-reminder）：
```js
{ content: [{type:'text', text}],
  source: { kind:'plugin', plugin:'capability-bridge', form:'notice', summary } }
```
★ **`source` 标注是必需的**，上游注释明说：漏了它，未标注的 context 会在**派生历史里被渲染成用户提示**（假消息）。

**自证** `scripts/test-capability-notice-wiring.mjs`（**16 项**，mock ctx 真调 `apply()`）：
处理器装上、首次见到会话只记基线不记变更、能力库变了才挂车、幂等收敛、
集合没变就不写（dropped）、`source` 齐全、能力库读不到 ⇒ 原样返回（**失败隔离**）、无 agent 调用不炸。

**设计要点**：全部包 try/catch —— **通知失败绝不影响工具结果，更不能影响 boot**。
收益是"省缓存"，boot 是命脉，代价不对等 ⇒ 一律降级为 no-op。

### ★ §6.5 回答用户：「装得干净、卸得干净」的两种，以及前提的一处修正

用户：*上游关了 HMR 只是不能重载，但装卸干净这个特点我们也要维护好。*

**结论同意，但前提要修**：HMR 关闭**不是**与"能否卸干净"无关的事 ——
上游注释说的是「after its **reload lifecycle** is tested」，而 reload lifecycle
**正是拆卸与重装的顺序问题**。⇒ 它是"拆卸没把握"的**症状**。
**不能因为 HMR 关了，就假定拆卸是干净可靠的；没被测的正是拆卸那一半。**

**"干净"分两种，我们今天主要靠前者**：

| 种 | 怎么来 | 可组合？ | 在哪 |
|---|---|---|---|
| **进程级干净** | 改配置 → **重启** = 全新进程 | ❌ | 插件层（我们的主路径） |
| **效应级干净** | `ctx.effect` 回滚 / 依赖驱动卸载 | ✅ | 能力层 |

★ 改 `bundles` 后重启**根本没有"卸载"发生** —— 是个全新进程
⇒ 那是"简单粗暴地干净"，不是"可组合地干净"。

**我们正在破坏这个性质的 5 处（可核查）**：① 手工改 node_modules 编译产物（体系外，卸不掉）
② `disabled` 当卸载用 ③ 包目录残留（`dsh.client` 可能被前端加载）④ 状态文件残留
⑤ **`KNOWN_SESSION_EVENT_TYPES` 的进程级 mutation**（卸载不撤销 —— **P4 自己引入的有意例外**，因为它是读日志的兼容性要求）。

**建议**（未实施）：`scripts/check-plugin-hygiene.mjs` —— 正查 bundles 完整性、
反查残留包、deps 与 bundles 一致性、遗留物、源码比产物新（提醒没重建）。
口径：每条能指名"哪个包、哪一步没卸干净"，不出总分。

## 验证（无回归）

check:bom 0 违规｜check:profile 582 行/err 0｜config-tolerance 56/56｜
boot-health 32/32｜capability-gate 29/29｜notice 内核 27/27｜**notice 接线 16/16**｜patch-anchors 17/17。

## 2026-09-15 深夜：插件卫生门 ✅（装得干净/卸得干净可核查）

用户选 ① —— 把 §6.5 的性质做成可跑的门。产物 `scripts/check-plugin-hygiene.mjs`
+ 自证 `scripts/test-plugin-hygiene.mjs`（**26 项，两方向**）。

### ★ 它一上来就报出两处**真问题**（不是空门）

1. **4 个 bundle 未在 profile 的 `dependencies` 里声明**（tool-evolution / subagent-council /
   capability-bridge / switchboard）—— 它们靠 `profiles/web/node_modules/@dsh-brain/*` 的
   **符号链接**解析。⇒ **跑一次 `pnpm install` 可能把它们剪掉，那几个 bundle 会静默失效。**
   ⚠️ 这与我们这两天修的"静默失效"是**同一类失败**，只是换了层。
   **未擅自修**：改 profile + pnpm install 是今天出过事的操作，blast radius 大，待用户拍板。
2. **`~/.dsh/pet.json` 残留**（桌宠插件已移除，状态文件还在）+ `~/.dsh/.backup/` 一个备份目录
   —— 正是 §6.5.3 表里的第 4 类"卸不干净"。留着无害，但如实报出。

### 门的设计口径（三条，都写进脚本头注释）

- **每条指名「哪个包 / 哪一步」，不出总分**（沿用 §5 fail-closed 逐项纪律）。
- **ERROR ⇒ 非 0 退出；WARN ⇒ 退出 0 但大声打印**。
  ★ 为什么 WARN 不失败：有些项**可能是有意为之**（如 `packages/` 里留一个开发中的新包）。
  **一律判红 ⇒ 门会被绕过 ⇒ 比不设门更糟**（与"假红"的教训一致）。
  想要更严用 `--strict`。
- `--json` 可机读；`--profile-dir / --packages-dir` 供自证夹具注入（默认走真实路径）。

### 检查项

正查（ERROR）：bundle 的包目录存在 / `lib/index.js` 存在 / `cordis.patch.yml` 存在 /
`package.json` 有 `dsh.bundle` / profile 侧链接存在且不悬空 / manifest 无 BOM。
反查（WARN）：`packages/` 里有但未被任何 bundle 启用（残留 or WIP，需人甄别）/
bundle 未声明进 deps / 已移除插件的遗留状态文件 / `.backup` / **源码比编译产物新**（改了没重建）。

### 自证的诚实边界

悬空链接那条本来"跳过"（本机建不了符号链接）⇒ 我没就这么算它验过：
改用**空目录当链接**命中同一条判据（「link 路径存在 且 link/package.json 不存在」），
现在该分支有覆盖，且额外断言"不能误报成缺链接"。

### ⚠️ 一个待处理的组织问题

现在**门已经有 9 个**（check:bom / check:profile / config-tolerance / boot-health /
capability-gate / notice 内核 / notice 接线 / patch-anchors / plugin-hygiene 自证），
**缺一个统一入口** —— 这本身就会变成"开发起来混乱"的新来源。
建议加 `npm run check:all`（复用 `check-plugin-hygiene.mjs` 的"逐项 + 汇总"口径）。
**待用户拍板**（要不要做、失败语义怎么定）。

## 验证

九门全绿：check:bom 0 违规｜check:profile 582 行/err 0｜config-tolerance 56/56｜
boot-health 32/32｜capability-gate 29/29｜notice 内核 27/27｜notice 接线 16/16｜
patch-anchors 17/17｜**plugin-hygiene 自证 26/26**；门本体：ERROR 0 / WARN 6。

## 2026-09-15 深夜：修掉 4 个未声明依赖 + 定下「不追上游」策略

### 用户定策（长期规则，已进 MEMORY.md 铁律 #10）

> **不追上游版本**。上游随时会改，我们**只按需合并对我们有利的改动**，不为追新而升级。
> **上游自身的问题暂不处理**；**管好我们自己部分的开发即可**。

**边界判据**（本轮核实，用于回答"这是上游的还是我们的"）：
- `@deepseek-ai/*` 从 `profiles/node_modules/` 那条**上游共享树**解析 ⇒ 上游的，不管
  （例：`@deepseek-ai/dsh-web-app` 是 bundle 但不在 deps，**不动**）
- `@dsh-brain/*` 从 `profiles/web/node_modules/` 每-profile 目录解析 ⇒ **我们的，要管**

### ✅ 修掉 4 个未声明依赖（卫生门抓出的那处）

**问题**：`tool-evolution` / `subagent-council` / `capability-bridge` / `switchboard`
只在 `dsh.profile.bundles` 里，**没进 `dependencies`** ⇒ 只靠
`profiles/web/node_modules/@dsh-brain/*` 符号链接解析；而 profile 的 `pnpm-lock.yaml`
只认已声明的包 ⇒ **一次 `pnpm install` 就会剪掉它们，那几个 bundle 静默失效**
（与我们在 boot/配置层修过的"静默失效"同类，只是换了层）。

**修法（脚本化，不手工改）**：扩展 `scripts/patch-profile-deps.mjs` ——
遍历 `dsh.profile.bundles`，为每个 `@dsh-brain/*` 且包目录存在的项补
`link:D:/project_develop/dsh-brain/packages/<name>`。三处硬化：
- **只在内容真变化时才写**（旧版无条件重写）
- **★ 写之前先备份**（该文件在 `~/.dsh/` 下、**不在 git 里**，改坏了没历史可回滚）
- **后置校验**：任何人方 bundle 未声明 ⇒ 非 0 退出（`DSH_PATCH_STRICT=0` 降级）

**结果**：`我方 bundle 声明 7/7`；幂等（再跑"无变化"）；**`check:profile` 仍 582 行 / err 0 /
dup 0 / pet 0**（装配没坏）；`check:bom` 0 违规；**卫生门 WARN 6 → 2**（剩 `pet.json` + `.backup`）。

### ⚠️ 未做（待用户定）：同步 `pnpm-lock.yaml`

新增声明后它与 `package.json` 不再一致：
- 普通 `pnpm install` 会同步它（= 本次修复的收尾动作）
- `--frozen-lockfile` 会**明确报错** —— **响亮 >> 静默**，方向是安全的
- **没擅自跑**：profile 装配链今天出过事，blast radius 大；建议用户在能盯着的时候跑

### 交付物

`scripts/patch-profile-deps.mjs`（扩展）+ `~/.dsh/.backup/profile-web-package.json.*.bak`（改前备份）。

### ✅ pnpm-lock 已同步（2026-09-15 深夜，用户选 ①）

**关键发现：`pnpm` 其实是有的** —— 在 `C:/Users/Admin/AppData/Roaming/npm/pnpm.cmd`
（v10.26.0），只是**不在 PATH 上**；`npx pnpm` 不行，但全路径可直接调。

**用最小改动面做**：`pnpm install --lockfile-only` —— **只改 lock，不碰 node_modules**
（对比 `pnpm install` 会顺带 reconcile node_modules）。先备份 lock 到 `.backup/`。

#### ★ 顺带发现：lock 本来就脏，而且脏在**两个方向**

| 方向 | 内容 |
|---|---|
| **漏** | 4 个我们自己的包（tool-evolution / subagent-council / capability-bridge / switchboard） |
| **多** | `@dsh-brain/handover-agent`（09-14 已从 deps 删的悬空项）+ **`@linxin666/dsh-pet`**（已移除的桌宠，**连它的 integrity 哈希和依赖树 clsx/schemastery/cosmokit 都还在**） |

⇒ 又一次"卸不干净"的残留；而且**没人发现**，因为 `check:profile` 只看装配**结果**、不看 lock。

#### 结果（全部实测）

- lock importers deps **9 条 == manifest dependencies 9 条**（双向零差集）
- 两个陈旧条目归零（`grep -c` 均为 0）
- **`check:profile` 仍 582 行 / err 0 / dup 0**（装配没坏）
- **node_modules 未被动**：`@dsh-brain` 链接仍 7 个

#### ★ 补掉卫生门的盲区（这是本轮更值钱的部分）

给 `check-plugin-hygiene.mjs` 加了 **lock ↔ manifest 一致性检查**：
- manifest 有、lock 无 ⇒ **ERROR**（`--frozen-lockfile` 会失败；正是我们刚修的那种不一致）
- lock 有、manifest 无 ⇒ **WARN**（陈旧条目，会被 prune；属"卸不干净"残留）
- 没有 lock / 解析不出 ⇒ **WARN 且明说"无法核对"**（**不假装通过**）

自证从 26 → **34 项**（含 lock 一致 / 漏依赖 / 陈旧条目 / 无 lock 四种）。
★ 又踩一次自证的坑：断言只查了 `what`，而"无法核对"在 `detail` 里 ⇒ 假失败；已改成
`what + detail` 一起查。

#### 剩余 2 个 WARN（未处理）

`~/.dsh/pet.json`（桌宠残留状态）+ `.dsh/.backup/`（3 项备份，含我这轮新建的 2 个）。
留着无害；要不要清由用户定 —— 注意 `.backup` 里那两项**是有用的回滚点**。

#### 工具层教训（累计第 5 次）

`node -e` 里带正则/引号会被 bash 抢插值 ⇒ **一律写 `.mjs` 文件再跑**。
本轮又中一次（写 lock 解析正则时）。这条已在 MEMORY 环境约束里，但我仍会顺手用 `node -e`
—— 需要更强的自律。

## 2026-09-15 深夜：check:all ✅ + P4 换代验收就绪 ✅ + 「外置端口」的答案

### ✅ ① `npm run check:all`（用户选 1）

`scripts/check-all.mjs`：**12 道门 / ~10.7s / 全过**，逐项 + 末尾汇总，任一失败非 0 退出。
- **有副作用的门如实标注**（`capability-gate run --all` 会写 registry.json 回执）——
  把"检查"和"会改东西的检查"混为一谈，是让人不敢跑总检查的常见原因。
- `--only <substr>` / `--list` 便于调试。
- ★ **刻意不收 `verify-p4-after-swap`**：它换代前**本来就该红**，收进来会让 check:all 恒红
  ⇒ 门被当噪音 ⇒ 被绕过。由 `npm run verify:p4` 单独跑（脚本里写了原因，防后人误加）。

### ✅ ② P4 换代验收就绪：`npm run verify:p4`

`scripts/verify-p4-after-swap.mjs` 三段验收：
- **A 出生证明**：活跃代（控制面 `?cmd=status` 权威）的 boot.log 里有没有
  `registered custom session event type: capability/change`。
  ★ 这条是**预置好的判别器**：gen-3086（换代前）实测**没有** ⇒ 它出现 = 新代码在跑。
- **B 指纹恒定**：`(system 长度 | tools 数)`；变化只发生在压缩附近 ⇒ 可接受；
  **无压缩伴随的变化 ⇒ 异常**（P4 未生效或另有改写源）。
- **C 折叠痕迹**：`capability/change` 事件数 / `能力集合已更新` 文本。
  都为 0 是**正常的**（能力库没变过 ⇒ 链路应当静默）—— 脚本明确这么说，**不谎报失败**。

**换代前基线已固化**（跑过一次，输出见日更）：A 段红（旧代码）、B 段绿（tools 恒定）、
A 另有"启停干净"绿。

#### ★★ 我自己的验收脚本出了一次假红（值得记）

第一版对 boot.log **全文** grep `plugin tree failed to load` ⇒ 在 gen-3086 上报了 2 条错误。
**是假红**：boot.log **跨多次启动追加**，错误行在第 12/63 行，而本次的 `===== BOOT` 标记在
**第 186 行**，尾部还有正向信号 `dsh web: http://127.0.0.1:3080` ⇒ 那一代起得很干净。
`docs/gen-plugin-tree-partial-failure.md` **早就警告过这个坑**（"直接全文 grep 会读到旧代残留"）。

**修法**：不写第三份解析 —— 直接 import 生产保险
`packages/switchboard/lib/boot-health.js` 的 `lastBootSegment` / `findFatalBootErrors` /
`hasReadySignal`，判据与 `verifyBootHealth` **完全同源**。实测：段 21 行 / 致命 0 / ready true。

### ★ ③ 用户问「要不要留一个外置端口」——**端口早就有了**

**核实结果**（实测）：

| 项 | 事实 |
|---|---|
| 控制面 | **`http://127.0.0.1:31800`**（`SWITCH_ADMIN_PORT` 默认 31800；**3080 是 GUI 前门，不是控制面**） |
| 状态 | **正在监听**（pid 29892，与 3080 同进程）⇒ **不需要新端口** |
| 只读指令 | `?cmd=status` / `result` / `flow` / `panel` |
| 有副作用 | `?cmd=handover` / `apply` / `restart`（+ `&fast=1` `&profile=` `&kernel=` `&verify=`） |
| 注入口 | **`&fail=spawn\|catchup\|freeze`**（`?cmd=fail` 只是个 ack） |

我实测调通了只读指令：`?cmd=status` → `stage:idle, result:success(已快速切换 → gen-3086),
activeGen:{gen-3086,port:3086,pid:29396}, locked:false`。

⇒ **所以问题不是"要不要留端口"，而是"要不要让 Agent 用它"。** 建议三档权限：
1. **只读**（status/result/flow/panel）→ Agent **自由调用**（无副作用；正好是核实换代结果的工具）
2. **换代**（restart/handover）→ Agent **可以发**，但**先说明理由与影响**（会**接手用户会话**：
   实测注入 2 条提示并真的驱动一轮 LLM，见 `generation-swap.md`）
3. **注入口**（`&fail=`）→ **Agent 永不调用**（只属人工测试）

这条正好是《总纲》§4「**无环原则**」的实例：被改的系统不能自动批准自己的部署。

#### 一个很实际的技术约束（新发现）

**换代会在回答中途打断我自己** —— 我这一轮的回复还没发完，会话就被接手到新代了。
⇒ 所以"Agent 可以直接发"在**时序上**做不到"发完再答"；**必须由用户在"我答完之后"发**，
或者我发起的那个动作要放在**这一轮之外**。
这不是治理洁癖，是**结构性**的：**回答者不能在自己的回答里重启自己**。

**给用户的命令**（任选）：
- 你自己发：浏览器打开 `http://127.0.0.1:31800/?cmd=restart`（`restart` 等价 `fast=1`，秒级）
- 或说一声我来发（下一轮开始时发，然后立刻跑 `npm run verify:p4`）

## 2026-09-15 深夜：P4 **首次真机换代验证**（我发的换代）

### 换代执行（用户授权「你来发」）

```
curl "http://127.0.0.1:31800/?cmd=restart"
→ {"ok":true,"cmd":"restart","stage":"started","fast":true}
→ result: {"result":"success","note":"已快速切换 → gen-3087","resumeSession":""}
```

**gen-3086 → gen-3087**，约 5s，`resumeSession: ""`（**没接手任何会话**）。

### ★ 纠正上一轮我自己的一个错误理由

我上一轮说「换代会在回答中途打断我自己 ⇒ 回答者不能在自己的回答里重启自己」——
**错了两处**：
1. `handover-status.jsonl` 最近 4 次换代的 `resumeSession` **都是空串** ⇒ **根本没接手会话**；
   历史上被接手的都是 **DSH 的 session**（`session-28f50f57…`），与我的对话无关；
2. 按已定案的 **我的宿主是 WorkBuddy、被换代的是 DSH —— 两者是不同系统，我天然是外部观察者**。
   ⇒ 换代**不影响我**；实测我发完换代后这一轮照常跑完。

**教训**：我又一次把两个系统混为一谈（记忆里"我已经不会再吐槽自己不能被重启"正是为此写的）。

### ✅ A 段绿：P4 第一个真机证据

`npm run verify:p4`（gen-3087）：
- ✓ 启停干净（有正向完成信号 `dsh web: …`、无致命模式）—— 判据复用生产保险 `boot-health.js`
- ✓ **boot.log 出现 `capability/change` 注册行 ⇒ 新代码已生效**
  （对照：换代前 gen-3086 **没有**这一行 ⇒ 该判别器有效）
- ✓ tools 数恒定（**但见下面的诚实边界**）

### ⚠️ B 段是「平凡绿」—— 不能当证据

当前会话是 **Code Mode**，指纹 `79032 | 1` ⇒ **tools 数只有 1 个**（Code Mode 把工具教学放进
system、只留 `run_code`）。⇒ **P4 的收益在 Code Mode 下结构性不可见**：
能力清单本来就没进 tools 段，恒定是必然的，**不是 P4 的功劳**。
要真正验 B，需要 Native/Dual 模式（tools 69~71）。

**⇒ 记下：报告 B 时必须说明会话模式，否则就是拿平凡绿冒充证据。**

### ★ C 段的边界：**我触发不了**

C 段（端到端折叠）需要 **DSH 侧的 agent 调一次工具**，因为折叠挂在 DSH 的
`tools/post-execute` waterfall 上。而**我的工具调用走的是 WorkBuddy 的工具运行时**
⇒ 不经过 DSH 的 `tools/post-execute` ⇒ **永远不会触发 DSH 那个插件的折叠**。

**这同时回答了用户的「外置端口」问题**，而且更精确了：

| 面 | 位置 | 我能做什么 |
|---|---|---|
| **换代面**（控制面） | `127.0.0.1:31800` | ✅ 能读状态、能**发**换代（已实测） |
| **会话面**（驱动一轮对话） | **没有暴露**（`/api` → 404；dsh-web-app 里没找到路由） | ❌ **驱动不了** |

⇒ **"让 Agent 自己处理外部"缺的不是控制端口，是会话端口。** 现有 31800 是**换代面**。

### 触发条件已置备（等 DSH 侧动一下）

`capability-registry.mjs signal spawn --kind invoke` ⇒ `updatedAt`
`2026-09-15T12:03:31Z` → **`12:24:11Z`**（**在 gen-3087 启动 12:23:27 之后**，顺序正确）。
⇒ 只要**在 DSH 侧（3080）让 agent 调一次工具**，链路应当：
`session/event` 记一次变更 → 下一条 tool result 挂上 `additionalContexts` 通知。
之后重跑 `npm run verify:p4`，C 段应出现 `capability/change ≥ 1` 与「能力集合已更新」。

## 2026-09-15 深夜：诊断「elv 窗口上下文崩坏」

用户报：除当前项目外最新的那个窗口**上下文又崩坏了**。

### 两条**排除**（都有数据）

1. **不是日志损坏**：扫全部 29 个会话，**29/29 都能正常解压解析，0 失败**。
2. **不是我刚加的东西**：**没有任何会话含 `capability/change`**（新事件类型还没往日志写过）。

### ★ 我自己的又一次假红（记下来）

我一度把「`seq` 范围 0..1803 但只有 333 个事件（33 处缺口）」读成"丢了事件 ⇒ 上下文崩坏"。
**是误读**：扫全部 29 个会话，**26 个都是稀疏的**（密度 10~36%），且各会话 max seq 互不衔接
⇒ **`seq` 是全局/进程级计数器，不是每会话单调** ⇒ **缺口是正常的**。
⇒ 教训：**判"异常"之前先看它在同类样本里是不是常态**（基线意识），否则又是假红。

### ★★ 根因：**是我们自己**在 09-14 把默认 preset 换成了 Code Mode 版

| # | 事实 |
|---|---|
| ① | `~/.dsh/.agent-presets/code-council/` **09-14 13:40 创建**（`scripts/add-preset-council.mjs`） |
| ② | 其 `agent.cordis.yml` = 上游 **`code` preset 的副本**，文件头自述「presented as **Code Mode**」 |
| ③ | `~/.dsh/settings.yaml`：`agent-presets.default: **code-council**`（同刻） |
| ④ | ⇒ **09-14 起所有新窗口都是 Code Mode**（实测：tools=1、system 76632~81793 字符） |
| ⑤ | 而 **09-03~09-13 的窗口都是 Native/Dual**（tools 27~102、system 6445~18020） |

tools 数时间线（29 个会话实测）：09-13 14:33 = 94 / 18:05 = 95（Native）；
**09-14 04:44 起全部 = 1（Code Mode）**；09-15 12:34（elv）= 1，system **81793**（历史最大）。

**症状原文**（elv 会话 `session-faeac7ca`，错误码 `UNKNOWN_TOOL:4 / CODE_RUN_FAILED:4 / INVALID_ARGS:1`）：
```
Error: unknown tool "pwsh": only `run_code` is callable directly — call `pwsh` from inside a `run_code` program instead
Error: unknown tool "read": only `run_code` is callable directly …
Error: code run failed (exception): 'import', and 'export' cannot be used outside of module code
Error: code run failed (exception): Expected ';', '}' or <eof>
```
⇒ **模型（agnes-2.5-flash）反复直接调 `pwsh`/`read`（Code Mode 下不可直调），又写不出合法 JS。**
⇒ 那个窗口"看起来崩坏"= **Code Mode 与模型能力不匹配**。
（对照：dsh-brain 的大会话也有 `UNKNOWN_TOOL:12 / CODE_RUN_FAILED:3` ⇒ 不是 elv 独有。）

### 修法选项（**未擅自改** —— 这是面向用户的设置）

- **(a) 把 `agent-presets.default` 改回非 Code 的**（如上游 `standard`）⇒ 新窗口回到 Native
- **(b) 另造一个基于 `standard` 的 `council` preset**，保留议事厅但不用 Code Mode
- **(c) 不动默认**，只是开窗口时手选别的 preset

**待用户拍板。** 注意 09-14 那次是有意为之（为了用 council-architect），
所以这不是"回退错误"，而是"要不要让**默认**也变成 Code Mode"。

### 待确认

用户说"上下文崩坏"的**界面具体表现**是什么？（一直报错 / 历史消息乱掉 / 空白 / 答非所问）
—— 我的诊断指向"一直报错做不成事"，但要与用户实际所见对齐才算闭环。

## 2026-09-15 深夜：诊断「历史加载失败」与「交接后 reading 'kind' flake」

用户给出**真实报错**（比我的推测有价值得多）：
- `session-b79a6e91`: `SessionPersistenceCorruptionError: session event at seq 5342 lacks an identified message`
- `session-432f6207`: `corrupt session log: seq gap in committed region at line 2167 (expected 9435, got 9428)`
- `session-ce5fa937`: 同上，`line 3031 (expected 16099, got 16096)`
- 另有若干会话「本轮运行失败 `Cannot read properties of undefined (reading 'kind')`」+ `UNKNOWN`
- 用户补充：**新会话没问题**；那个是 PTC/Code Mode

### ✅ 已闭环：seq 5342 —— **全库只有 1 条真正坏掉的事件**

**方法**（这次终于做对）：**先从全体样本统计每种类型的正常形状，再找离群**。

```
user/message（469 条）
   468  (99.8%)  {content,id,role,source}    ← 正常（平铺）
     1  (0.2%)   {content,role}              ← ★ 唯一离群 = seq 5342
assistant/message（3535）  99.9% {message,step,turn,usage}（2 条带 interrupted，合理）
tool/result（9374）        71.1% {message,step,turn} + 24.7% +meta + 4.1% +error（都是正常变体）
```

⇒ **`session-b79a6e91` 的 `seq=5342`**：`user/message` 只有 `{role, content}`，**缺 `source` 与 `id`**
⇒ 正是报错说的「lacks an identified message」。**逐字对上。**
⇒ 旁边紧邻 `seq=5343 agent/inbox/spliced` ⇒ **嫌疑生产者 = inbox splice 路径写出了"匿名" user/message**。

### ⚠️ 未闭环（诚实标注）

- **seq gap（432f6207 / ce5fa937）**：日志在某处 seq 不连续（9430~9433 缺失等），
  validator 判为 `corrupt session log`。**已刻画、未定根因**。
- **`reading 'kind'` flake**：
  - **已排除 `turn/end.reason`**：`dsh-agent-loop:592-597` 上游自己就有兜底
    `reason: turnEnds ?? { kind: "completed" }`，且全库实测 **缺 reason 的 turn/end = 0**。
  - 已定位**呈现路径**：`本轮运行失败` 是 `dsh-client-ui-conversation` 对
    `turn/end`(reason.kind==='error') 的 UI 文案；`UNKNOWN` = agent-loop 给非 LLM 错误的兜底 code。
    ⇒ 所以那个 `.kind` 崩溃发生在**服务端跑一轮的过程中**，被捕获后变成 turn 错误。
  - **未定根因** —— 不假装。候选站点还有：`dsh-agent:197`、`dsh-session-query:308/312`、
    `dsh-session-telemetry:242`（都是无保护的 `switch (reason.kind)` 类）。

### ★★ 我在本轮**连续三次写错判据**（重要教训）

为了查"坏事件"，我先后用了三个**凭印象**的判据，全部假红：
1. `seq 缺口` ⇒ 误判为"丢事件"（真因：`seq` 是**全局计数器**，26/29 会话都稀疏）
2. `surface 事件一律要有 data.message` ⇒ 误判 469 条（真相：`user/message` 是**平铺**的，
   只有 `assistant/message`/`tool/result` 才包 `message`）
3. `user/assistant/message 一律要有 id+source` ⇒ 误判 3536 条（真相：`assistant/message` 用 `{turn,step,message,usage}`）

**正确方法（应一开始就用）**：
> **先把该类型的「正常形状」从全体样本里统计出来（键集合频次分布），再找离群。**
> 即：**判据来自数据，不来自我的印象。**

这与 `gate-authoring` 里那条"依赖脆弱假设 ⇒ 假红"同源；已在技能里，但我仍未内化。
