# 2026-09-20

## 11:50 补上"真机验收"缺的那一环：`scripts/verify-drain-after-swap.mjs`

背景：09-16 修完换代写入竞态后，验收只剩"在真机上跑一次换代并读数"这一步，
但那一步当时只有一份**手抄清单**（人看 `state.jsonl` 容易看漏）。今天把它做成了一条命令。

- 新增 `scripts/verify-drain-after-swap.mjs`（`npm run verify:drain`）：判据表 R1–R9b，
  全部读真机落盘物（`state.jsonl` / `<oldGen>/resume.jsonl` / `boot.log` / 会话体健）。
  - **R9b 是新增的**：会话检查器只把 seq gap 记成 WARN（不阻塞启动），所以"**新增** gap"必须
    靠基线对比才看得见 —— 而这正是本 bug 类别的**直接**指纹。基线落 `out/drain-acceptance-baseline.json`。
  - `--selftest` 两方向自证：真·修复前日志 ⇒ 7 FAIL（红）；合成修复后 ⇒ 9 ok / 0 FAIL（绿）。
  - **刻意不进 `check:all`**（换代前本来就该红，与 `verify:p4` 同理）；已在 `check-all.mjs` 里写明理由。
- 真机现状（本次实测）：**整套 DSH 栈没在跑**（3000–3300 无监听；控制面 31800 拒绝；
  `lease.json` 最后写入 09-15 23:59），`state.jsonl` 最近一次 freeze 仍是 09-15 22:52（`lastSeq=0`）
  ⇒ 验收脚本如实报 **7 FAIL / 未生效**（还没重启、还没换代，不是回归）。
- 顺带复验：`node scripts/check-all.mjs` = 15/15 全绿（12.1s），仓库这 5 天没漂移。

**下一步还是那一条**：用户重启 switchboard → 空档发一次 `?cmd=handover` → 跑
`node scripts/verify-drain-after-swap.mjs` 看 FAIL 是否为 0（见 `topics/next-task-handover.md` §1）。

> 附：用户在 `C:\Windows\system32` 里跑的脚本 ⇒ `MODULE_NOT_FOUND`。**命令要在仓库目录跑**：
> `cd D:\project_develop\dsh-brain` 之后再 `node scripts\verify-drain-after-swap.mjs`。

## 12:0x 上游漂移体检（用户机器维修停摆 5 天，唯一可能有异动的是上游）

新增 `scripts/check-upstream-drift.mjs`（`npm run check:upstream`，**手动跑/需联网**，不进 check:all）：
A 组「我们依赖的形状必须命中」+ B 组「我们补丁的靶子还在不在」+ C 组「上游新增能力」。报告落
`docs/upstream-drift-2026-09-20.md`。要点：

- 本地 `0.1.1-rc.2`（两棵上游树一致）；registry **latest = 0.1.5-rc.2**（09-10）、alpha `0.1.6-alpha.2`（09-17）。
  根 `package.json` 是 `^0.1.1-rc.2` —— **caret 对预发布只匹配同一 [major.minor.patch]** ⇒ 不显式改就不会跳。
- A 组 6/6 命中；★ 但 `session.seq` 在 0.1.5 里变成 `SessionLogOffset(log.length)`（**语义不变、写法变了**）
  ⇒ 把 `test-handover-drain.mjs` 里那条字面量正则打成**假红**，已改成语义判据（两代写法都命中）。
- B 组：U1 / U3 / BOM 三个靶子**都还在**且上游**都没修** ⇒ 三条补丁仍必要；
  BOM 从 3 处涨到 **4 处**（升级要按处数重定位）。我们的锚点是严格模式 ⇒ 打不上会 postinstall 非 0 退出。
- ★★ **最重要**：`dsh-session-persistence` 在 0.1.5 拿到**存储层单写者所有权**
  （`open(id,'read'|'write')` 原子占用 / `SessionAlreadyOwnedError` / 服务级 `flush()`）
  —— 正是 09-15 事故缺的那块栅栏。但**竞态根机制未变**（seq=log.length、repair 的 `last.seq+1`、end-seed 都在）
  ⇒ 上游**没修**这个竞态，我们 09-16 的修法仍然对。
- C 组：新增 10 个模块，其中 `dsh-acp-app`/`dsh-sdk-app`/`dsh-sdk-minimal`（**把 dsh 当子进程用 JSON-RPC 驱动**）
  与 registry 上的 `dsh-subagent-codex`/`-claude-code`（**一次性**外部子代理）与用户的"两服务"想法相关；
  但**长驻、能反向代持主脑的子代理服务上游没有** —— 要自己造。

## 12:1x 评审「主脑/子脑拆两个服务、互相代持」→ `docs/two-service-custody-review.md`

结论：**方向对，但只能替掉"部署"那一层，替不掉"判据"那一层**，而且"互相代持"的对称结构会踩我们自己定的
**无环原则**（A 批准 A 靠 B、B 靠 A ⇒ 2-回路 = 自己批准自己）。正确形态是**非对称托管**：
主脑可自由换代（居留方自动批准），居留方（前门 + 写权 + 判据）只能停机换、由外部见证。
落地第一步 = 把现有 switchboard 升级成"居留方"（写权收授 + 杀/校验/放流量），主脑代码一行不用改。

增补：`scripts/check-upstream-drift.mjs` 里也修了**自己的假绿**（第一版把"文件不存在"当 skip、
又没给 tarball 去掉 `package/` 前缀 ⇒ 解出空目录还打印"所有断言仍成立"）。

## 12:2x 追问「同时开 2 号进程做实验，成功才退役、否则回退」→ 评审 §8

用户追问的实质是：**"实验成功"的判据是什么**（他自己也点出"蓝绿之前也没考虑好主脑怎么验进化"）。
已把结论写进 `docs/two-service-custody-review.md` **§8**（追加节，原有 §1–§7 不动）。要点：

- **能，而且比现在的流程更保守**：今天的手续就是 `spawn → 追平 → 冻结 → 校验 → 翻转 → 失败回滚`，
  你的顺序（先接管后退役、否则丢弃挑战者）**顺序本来就是对的**。缺的只是"实验"那半截 ——
  **今天的 2 号进程只被验"它还活着"，不被验"它更好"。**
- **上游没有评测件**：按 `eval` 关键词搜 `@deepseek-ai` = **0 命中**（实测）⇒ 判据必须自建。
- ★ **副作用隔离今天就有**：profile 里已装 `dsh-sandbox-local` / `dsh-sandbox-windows-acl`（Windows 受限令牌 +
  写白名单）/ `dsh-fs-sandbox` / `dsh-bash-sandbox` / `dsh-pwsh-sandbox`，且权限预设里**已有 `sandbox: read-only`**
  （`out/profile-dump.txt:114`）⇒ 挑战者可以直接起在**只读沙箱**里跑影子实验。不需要升级。
- 实验三档：① 影子只读（比输出质量，成本=双份 token）② `sessions.fork()` 分叉会话（全权跑自己的副本）
  ③ 沙箱工作区端到端（最接近真实，最贵）。建议 ①刷退化 → ③冻结任务集验成功 → 才 flip。
- 判据分两层：结构/契约层（**已接在 handover 里**：boot-health + verify 闸 + 前缀稳定性）与
  行为层（**必须成对比较**，而"现行活着"正是天然的对照组 —— 这是"开 2 号进程"的最大价值）；
  holdout 冻结且**放在居留方**（否则脑会优化评测集而不是任务）。
- 回退两档：a **丢弃挑战者 = 0 成本**（现行从未停过）；b 代码回滚（只有在挑战者已接管并写入正史后才必需）。

## 12:3x 「下一步是什么 / 开源界有没有同类」→ `docs/oss-prior-art-and-next-steps.md`

用户的判断是对的（§8 确实还在蓝绿路子上）。新文档给两条答案：

- **下一步 = 补"判据"那半边，不是拆服务**。里程碑 M0（真机验收 drain，阻塞）→ M1（影子实验最小闭环：
  冻结任务集 + 只读沙箱挑战者 + 成对比较 + 报告）→ M2（接进**现成的** `verifyCmd`/`VERIFY_ALLOW` 闸，
  `tool_apply(verify=…)` 就是它的入口）→ M3（custody 拆分，**触发式**：替换变高频才划算）。
  ★ 关键纠正：**"开 2 号进程做实验"不需要先拆服务**，今天的 switchboard 就能编排。
- **开源先例（都已核实）**：① fencing token（Kleppmann 2016，**必须在资源侧强制**）——我们的
  `writerToken` 只发号没验号，这就是 09-15 挡不住的理论名字；② Akka SBR 的 **downs 一侧**（"terminating
  those JVMs so they cannot act on stale ownership"）与我们 09-16 的 `sealPlan` 同构，可正式命名
  **fencing by termination**；③ Akka Cluster Singleton + lease（租约丢 ⇒ 自停）；④ k8s Lease API
  （新 alpha：退出时**主动释放**锁而不是等 TTL）；⑤ OTP supervision tree + 反向优雅停机（每子独立超时）
  + release handling 热升级 —— **但 Erlang 社区的主流结论反而是"just do blue-green"**；⑥ Envoy/Istio
  traffic mirroring = M1 的影子模式；Argo Rollouts/Flagger = 自动回退；Android A/B、NixOS generation = 双份+回滚；
  ⑦ durable execution（Temporal/DBOS/Restate/Inngest/Hatchet）：**"checkpoint ≠ durable"** —— 我们已有
  journal（session.jsonl），缺的是**副作用去重**（建议单独立案）；⑧ 评测：promptfoo（本地/CI/开源）、
  Braintrust/LangSmith/AgentEvals + delta testing + 四层指标，但**没有一家把环闭上**（eval 只报告、
  部署工具不懂 LLM 质量）⇒ 差异化就在这个闭环。
- 现成可复用（**今天的安装里就有，不用升级**）：`dsh-sandbox*` + `dsh-fs-sandbox` + 权限预设里的
  `sandbox: read-only`、`dsh-workflow-worker-thread`、`dsh-subagent` + `tool-subagent-control`。

## 12:4x 「开始吧」+ 靶场调研 → M1-step-1 已开工（实验的判据地基）

用户问：模型发布都用各种用例测能力，**固定模型时能否拿这些靶场测 Agent**？怎么测/用什么数据/什么方向？
并说"开始吧"。两件事都做了：

### A. 调研 → `docs/agent-eval-arenas.md`

- **能，且这是 2026 的主流**：公开榜普遍给 bare model / scaffolded 两套榜（GAIA 上差 30–50 点）；
  2026 出现专测"脚手架效应"的 **Harness-Bench**。HAL 原话：framework value frequently dwarfs model differences
  ⇒ 我们"固定模型、改 @dsh-brain 这层"正是这张榜要量的事。
- 靶场分类（含判分/数据形态/我们能不能用）：SWE-bench Verified & **Pro**、**Terminal-Bench 2.0**（89 题、
  Ubuntu Docker、自动测试脚本，**最贴合我们**）、OSWorld（VM 截图，与我们无关）、**τ²-bench**（多轮+规则遵循、
  双控、**pass^k**）、**MCP-Atlas**（1,000 题 / **36 个真实 MCP server**、claims rubric —— 我们有 ~70 个 MCP 工具）、
  BFCL/API-Bank（函数调用）、WebArena/GAIA/BrowseComp、GDPval（人类 Elo）、Agent-SafetyBench/ClawsBench（安全）。
- **怎么测**三件机械：harness（脚手架能摆动 10–20 点）/ scoring rubric（claims 断言清单，且规则会变）/
  environment（容器可重置；τ² 的双控让对话真的改世界状态）。数据共同点：**期望都是可机验的最终状态**。
- **2026 四条教训**（比榜单重要）：① **判据才是弱点** —— OpenAI 2026-02-23 停报 SWE-bench Verified
  （审计 138 题 >60% 有缺陷；难度子集 59.4% 的测试集抓不到目标 bug）= 我们的"假绿"同一条；
  ② **判分必须与 Agent 隔离**（多个靶场因"同环境判分"被一行代码破掉）= 无环原则的工程版；
  ③ **pass@1 会骗人**，任务越长能力与可靠性越发散（0.90→0.44），要 **pass^k**；
  ④ **成功 ≠ 安全**（ClawsBench：成功率 39–64%，unsafe-action 7–33%，且两者无一致序）。
- **我们的用法**：公开榜只当"不许退化的地板"；**判据用自有冻结任务集**（通用榜对我们饱和 + 抗污染存疑 +
  **我们已经有现成 oracle**：把修过的回归反向打回去，自己的门就由绿转红）。

### B. 开工 → `evals/` + `scripts/eval-validate.mjs`（**不需要 Agent、今天就能跑**）

- `evals/README.md`：任务格式（seed / oracle / regression / metrics / budget）+ 两条铁规
  （**判据必须在 Agent 够不到的地方**；**每题必须证明有信号**）。
- `evals/pilot/tasks.jsonl`：首批 3 题 = 把本周修过的回归反向打回去
  （注入消息身份 / 封口顺序 / defer 直读 phase），oracle 全是**已有的门**。
- `scripts/eval-validate.mjs`（`npm run eval:validate`）：三关校验 —— ①干净态绿 ②**打 seed 后 oracle 必须红**
  （否则算**假题**）③字节级 sha256 还原 + 复绿；带 `--prepare/--restore` 供下一半复用。
  **实测 3 题有效 / 0 题有问题**，还原后锚点各 1 次、无 SEED 残留、`check:all` 仍 15/15。
  **不进 check:all**（会临时改工作区文件，理由写在 check-all 注释）。
- 途中修了自家两个 bug：备份写成空文件（`prepare` 现在先落字节备份再改盘，失败回滚）；
  以及"文件不存在/下载异常"不许当 skip（那又是一次假绿）。

## 13:0x 验收脚本加了 R0 前置：**分清「没验成」和「没通过」**

用户按提示跑了验收，看到 7 个 FAIL 就问"我们蓝绿还是有问题吗？"—— 其实**栈从维修后就没启动过**，
那 7 个 FAIL 全是"旧版代码跑的旧交接"的必然结果。原脚本的结论行只有两种措辞（未生效/未验收），
容易被读成"有问题"。已加 **R0 前置**三段并在结论里分开四种状态：

- `R0a` 控制面 31800 在不在（栈有没有起）
- `R0b` 磁盘上最新 build 含不含本次修复（**实测 b1789486838989 含 quiesced/sealPlan/KILL-OLD** ⇒ 重启即生效）
- `R0c` 这次 build 之后有没有换过代（state.jsonl 最新一条 vs build 时间）
- 退出码：**0=通过 / 1=换代了但没生效 / 2=还没到能验收的状态**（2 是新增，避免误读）
- 顺手把"下一步三条命令"直接打出来（体检 → 发 handover → 重跑本脚本）。

另核实了一条**决定"一次重启够不够"的事实**：`~/.dsh/profiles/web/node_modules/@dsh-brain/switchboard`
是 **junction → `D:\project_develop\dsh-brain\packages\switchboard`**，而该包的 `main` 是 `lib/index.js`
（junction → 最新 build）⇒ **重启控制面 + 一次换代，控制面与 gen 两侧同时上新版**，不需要分别部署。

途中修了脚本自己的一个 bug：重复声明 `REPO`（一改就整份脚本语法错，连 `--selftest` 都跑不了）。

## 13:1x 处理掉了最后一个"从未为真的判据"：ready/追平门槛（假绿）

按纪律（**没实施过的判据级不得计作通过**）把最后一个 placebo 查清了 —— **根因是一个不存在的 API**：

- `preseed.computeCaughtUpSeq()` 依赖 `sessionPersistence.listSessions()` ——
  **上游没有这个方法**（服务名 `sessionPersistence` 是真的，方法不是）⇒ `ids = []` ⇒ 循环一次都不跑 ⇒ **恒 0**；
  另一半 `sessionProjectionCache.coldSnapshot(id).asOfSeq` **是真的**（`dsh-session-projection-cache`，profile 也装了）。
  ⇒ **171 条 freeze 里 lastSeq 全是 0** 的真正原因就是这个。
- 修法（`preseed.ts` → `liveMaxSeq(ctx)`）：只回答**能真答**的问题 —— **本进程 live 会话已落盘到哪个 seq**。
  active 代报真数；刚拉起的 staging 代报 0（**诚实**：它还没加载任何会话）。
- ★ **耦合陷阱**（差点自己造一个新 bug）：只改 preseed 会让 active 代报真数、staging 代报 0
  ⇒ 原来的 `waitCatchUp(b, targetSeq)` 会**白等 40s 然后 abort 每一次换代**。
  所以必须同时把 ready 门槛从"比 seq"换成**结构性就绪**（`waitReady`：进程活 + admin 应答 + gen 名一致），
  `waitCatchUp` 降级为**只用于 1.5s 有界观测**（注释里写明"不得用作推进门槛"）。
- 门：`test-handover-drain` 加 B2/B3/B4 四类断言（进度读数走 `liveMaxSeq`、preseed 不再碰 `listSessions`、
  ready 走 `waitReady` 且不再有 `waitCatchUp(b, targetSeq)`）⇒ **41 项全绿**；`check:all` 15/15；已构建。

**又踩到一次"判据读文本"的坑（已修并记进技能）**：我把旧 bug 的名字写进了 `preseed.ts` 的**注释**，
于是负向断言 `!/listSessions/` **当场假红**。⇒ 凡对源码做文本断言，**先剥注释**；
反向更危险：正向断言会被注释里的示例满足（假绿）。已加 `stripComments()` 并两方向自证
（真源码为真 + 植入回归为假）。

## 13:0x 用户起不来栈 → 我试了一次，**证伪了"Agent 能代启动"**

用户按旧文档去启动失败（`C:\...\Temp\start-switchboard-ascii.ps1` **机器维修后已被清掉**），
于是我用仓库内的 `scripts/relaunch-switchboard.mjs`（`detached+unref`）试启动了一次。结果：

- ✅ **启动路径本身是好的**：switchboard 起来了（控制面 31800 应答 `lease generation=9 / gen-3082`），
  gen-3082 **完整启动到就绪**（boot.log 有完整启动段、`passes` 到 `dsh web`、key-pool 绑 3101）。
- ❌ **但整棵树在工具调用结束后被回收**：1 分钟后进程列表里没有了、3080/3082/31800 全无监听、
  `out/switchboard-run.err.log` 时间戳**还是 9-13**（说明**不是崩的**，没写任何错误）⇒ 判据是**谁拉起的**。
- ⇒ 与既有环境约束一致，但这次把边界**钉实了**：`detached + unref` **也不够**。
- 已修文档三处：`topics/runtime-and-launch.md`（换启动命令 + 写明旧 Temp 路径已失效）、
  `MEMORY.md` 环境约束 #3（补实测证据）、`topics/next-task-handover.md` §1（启动命令 + `curl.exe` 提示，
  因为 PowerShell 里 `curl` 是 `Invoke-WebRequest` 别名）。

## 13:15 ★ M0 真机验收**通过**（换代写入竞态的修复第一次在真机上跑通）

用户在**自己的终端**启动成功（`node scripts\relaunch-switchboard.mjs`，pid=15024），随后我发了一次非 fast 换代：

- 流水（`state.jsonl`，4.5s 落定）：
  `ready-probe ok: gen-3083 mode=staging caughtUpSeq=0` → **`freeze a lastSeq=-1 quiesced=true
  (canSeeAgents=true running=0→cancel=0 stillBusy=[] flushFailed=[] 0ms)`** →
  `seal: keep-old` → `post-freeze catchup(观测项)：ok=true target=-1` → flip → boot-health ok → retire。
- `node scripts/verify-drain-after-swap.mjs` = **11 ok / 0 warn / 0 FAIL，exit 0**
  （R0a/R0b/R0c 前置 + R1..R9b 全绿；R9b 无新增 seq gap）。
- ⇒ 三件事同时被真机确认：① **gen 侧接线通了**（`canSeeAgents=true` + `<genDir>/resume.jsonl` 有
  `{"phase":"drain",...}` + boot.log 有 drain 行）；② 控制面新读数（quiesced / seal / 观测项）真的写进了流水；
  ③ 新的结构性就绪门槛**没有引入 40s 停顿**（全程 4.5s）。
- ⚠️ **仍未验的两个分支（不许当已通过）**：`running=0→cancel=0` ⇒ **"有活在跑 ⇒ cancel + 等 whenIdle"
  这条主路径没被走过**；`lastSeq=-1` ⇒ 因为**当时没有任何 live 会话**（`max(∅)=-1`，是诚实值），
  所以"真 lastSeq 非 0"也没验过。要验它们必须在**回合运行中**发换代。

### 13:24–13:30 ★★ 这个实验**抓到我自己修复里的两个 bug**（一个假红、一个假绿）

第一次做"回合运行中换代"（fast，跳过 defer）：流水报
`freeze 有应答但**未达静止点**（quiesced=false：canSeeAgents=true stillBusy=[session-853b751c…] flushFailed=[]）`
→ `seal: KILL-OLD` → **强杀旧代 pid=19628 → `seal ok：旧代已确认消失`**。
⇒ 一方面**封口路径在真机上被证实有效**（先杀旧代才放前门锁 —— 这正是为"旧代不肯停写"造的保险）；
另一方面顺着证据查出**我自己的两个 bug**：

1. **假红：`stillBusy` 用的是建列表那一刻的 phase 快照**。
   证据：会话日志里明明已经写了 `seq=255 turn/end {"kind":"aborted","reason":{"kind":"hook","reason":"handover/freeze"}}`
   （**我们的 cancel 成功了**），却仍报 stillBusy。
   ⇒ 修：`DrainAgent.readPhase()` **实时读**（`agentPhase` 优先用它），快照只作假件兜底。门加 A8。
2. **假绿：`ctx.sessions` 没有 inject ⇒ 是 `undefined`**，于是 `sessions=0` / `lastSeq=-1` /
   **`flush` 一次都没跑过**；而**空闲场景照样报 `quiesced=true`**（13:15 那次"通过"其实是空过）。
   ⇒ 修：与 agents 同款 `ctx.inject(['sessions'])` 拿 `sessionsRef`；新增 `sessionsObservable`
   （看不到会话服务 ⇒ `quiesced` 必须为 false）；`liveMaxSeq` 改成接收服务对象。
   门加 A7 + B5。**drain 门 41 → 48 项**，`check:all` 15/15。

### 13:30 重做实验（先换代让新 build 的 gen 上台，再在回合运行中冻结）——**全绿**

```
[switchboard:agent] drain quiesced=true lastSeq=1315 canSeeAgents=true canSeeSessions=true
                    running=1→cancelled=1 stillBusy=[] flushFailed=[] sessions=1 (14ms)
流水：seal: keep-old — 旧代已确认停写（回合全停 + 落盘成功）→ 留作回滚网
会话日志：seq=1315 turn/end {kind:aborted, reason:{kind:hook, reason:handover/freeze}} + seq=1316 session/end-seed
```

⇒ **之前没验过的三条分支一次性全部在真机上验过**：`cancel 回合 → 等 idle`（running=1→cancelled=1）、
**真 lastSeq**（1315 恰好等于日志里那条 turn/end 的 seq）、**会话服务可见 + flush 真执行**（sessions=1）；
这次走 **keep-old**（旧代留着当回滚网）而不是强杀 ✓。验收脚本 11 ok / 0 FAIL。

**副作用交代**：测试用 `session-853b751c-…`（cwd=dsh-brain、原本近乎空白），历史里多了两条测试请求
（蜂群/珊瑚礁说明文）与两次 aborted 回合 —— 日志连贯（末尾 turn/end + end-seed），要不要清理由用户定。

## 13:45 推送完成 + M1-step-2 的 runner 就位

- **推送**：用户开了代理（7890）后 `git push origin master` 成功（`f708738..89a7d8c`）；
  并用 `git ls-remote origin refs/heads/master` **向远端核实**（不是只看本地视图）——
  远端 HEAD = 本地 HEAD = `89a7d8c` ✓，**41 个积压提交全部上去**。
  （此前卡住的原因：git 的 `http.proxy=127.0.0.1:7890` 没启动；直连 GitHub 超时、工作层代理拒绝 CONNECT。）
- **新增 `scripts/eval-run.mjs`**（`npm run eval:run`，M1-step-2 第一版 = **单臂基线**）：
  `--list` / `--plan`（只打印计划，不动任何东西）/ `--task <前缀> --session <sid>`（真跑）。
  真跑流程：① 复用 `eval-validate --prepare` 打 seed 并证明"oracle 变红"（题目有信号）
  → ② 把题面（= `task.invariant` + 不许提问/改完就停的要求）发进指定会话
  → ③ 轮询到 `running=false` 或超预算（**超预算即失败**）
  → ④ 跑 oracle（FAIL_TO_PASS）+ regression（`check:all`）+ 记 `git diff --stat`
  → ⑤ `--restore` 还原（字节级 sha256）。
  记账：steps / outputTokens / 墙钟（从 `session.list` 投影读）+ 改动文件清单。
  **安全**：只认"已跟踪文件无改动"（未跟踪的新文件不拦但会打印）；题面必须显式 `--session`（会污染那份会话）。
  **未测**：真跑那条路径还没跑过一次（`--list`/`--plan` 已验证；真跑要花 token 且会改工作区，等用户点头）。

## 13:42 回执复核（本轮回执按约定写进 topics/next-task-handover.md 顶部）

约定确认：**回执写在 `topics/next-task-handover.md` 顶部**（那份文档顶部就是回执结构），**不另开文件**。

### ✅ 解掉原文档标记的"推送阻塞"

原 §1.5 记「40 提交待推送，卡在代理 7890 未启动」。13:42 复核：
- 代理 **7890 已监听**（PID 8832）
- **远端 `origin/master` = 本地 HEAD**（`git ls-remote` 直问服务器确认）⇒ 内容已在上面
- 试推时报 `fatal: could not read Username … terminal prompts disabled` ⇒
  **改成 `GIT_TERMINAL_PROMPT=0 git push` 就成功了**（凭据助手走缓存凭据）
- 终态：远端 = 本地 = **`454fe1d`**，未推送 0

**口径固化**：判断"推没推上去"**唯一可信的方式是 `git ls-remote`** ——
不是 `git push` 的输出、也不是本地 `origin/master`（会骗人）。

### ✅ 栈健康复核（写进 doc §2）

- 控制面 `stage=idle` / `locked=false` / `activeGen=gen-3086` / **`freezeSeq=1315`（真值，不再是 0）** / `lastFencingSeq=5833`
- 前门 3080 → **200**
- `verify-drain-after-swap.mjs`：R0a/R0b/R0c + **R1–R7 全绿**；
  13:30 那次 freeze：`quiesced=true canSeeAgents=true running=1→cancel=1 stillBusy=[] flushFailed=[] 14ms`；`seal: keep-old`
⇒ **drain 修复在真机上确实生效。**

### 本轮改动

纯文档（回执）＋ 一次推送。`check-all.mjs` **15/15 全绿**。
**下一项仍是 §3：M1-step-2（`scripts/eval-run.mjs`：把冻结任务集交给 Agent 跑，产出 delta 报告）—— 未开工。**

### 13:52 回执复核（续）：两处**过时**被改正

**① `MEMORY.md` 超限（修掉"注入被截断"）** —— 14844 → **9209 字符（−38%）**

它在注入时被截断（系统告警 "exceeded the size limit and was truncated"）⇒ **新会话看不到完整索引**
⇒ 直接破坏交接。而它自己第 8 行就写着「『交接』只留指针，不再复制细节」，却整段复制了 ~2900 字，
且日期停在 09-16（内容已是 09-20）—— **既违规又过时**。
做法：交接节 → 只留指针；索引散文压缩；0.1.5 细节挪回其归属文档的指针下；`timeout` 并入 #5。
**完整性已逐条核对**：环境约束 14 条 / 铁律 12 条，关键点抽查 15 项全部在位。

**② 交接文档 §3 过时** —— 原文写「要新建 `scripts/eval-run.mjs`」，但**它已存在**（13:46，并行会话建）。

- ★ **发现：有并行会话在同时改这个仓库** —— 提交 `56e04f5`（13:48）不是我做的，
  且它**漏加了 `eval-run.mjs` 文件本身**（`--stat` 只有 2 个文件），该文件后来被我的 `git add -A` 收入并推送。
  ⇒ 核实：`git ls-files` + `git ls-tree origin/master` 均确认**已跟踪、已在远端**。
- §3 已修订：列出实测的 3 题（`--list`），并点明**它们正是我们修过的三个回归**，
  oracle 分别是 `test-injected-message-shape.mjs` 与 `test-handover-drain.mjs`；
  明确"剩下的只是**真跑一次**"（`--plan` 已验，真跑路径从未跑过）。

### 本轮终态

远端 = 本地 = `6349f87`，未推送 0；`check:all` **15/15 全绿**；`MEMORY.md` 9209 字符。
