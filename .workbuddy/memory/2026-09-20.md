# 2026-09-20

## 11:50 补上"真机验收"缺的那一环：`scripts/verify-drain-after-swap.mjs`

背景：09-16 修完换代写入竞态后，验收只剩"在真机上跑一次换代并读数"这一步，
但那一步当时只有一份**手抄清单**（人看 `state.jsonl` 容易看漏）。今天把它做成了一条命令。

- 新增 `scripts/verify-drain-after-swap.mjs`（`npm run verify:drain`）：判据表 R1–R9b，
  全部读真机落盘物（`state.jsonl` / `<oldGen>/resume.jsonl` / `boot.log` / 会话体健）。
  - **R9b 是新增的**：会话检查器只把 seq gap 记成 WARN（不阻塞启动），所以"**新增** gap"必须
    靠基线对比才看得见 —— 而这正是本 bug 类别的**直接**指纹。基线落 `out/drain-acceptance-baseline.json`。
  - `--selftest` 两方向自证：真·修复前日志 ⇒ 7 FAIL（红）；合成修复后 ⇒ 9 ok / 0 FAIL（绿）。
  - **刻意不进 `check:all`**（换代前本来就该红，与 `verify:p4` 同理）；已在 `check-all.mjs` 里写明理由。
- 真机现状（本次实测）：**整套 DSH 栈没在跑**（3000–3300 无监听；控制面 31800 拒绝；
  `lease.json` 最后写入 09-15 23:59），`state.jsonl` 最近一次 freeze 仍是 09-15 22:52（`lastSeq=0`）
  ⇒ 验收脚本如实报 **7 FAIL / 未生效**（还没重启、还没换代，不是回归）。
- 顺带复验：`node scripts/check-all.mjs` = 15/15 全绿（12.1s），仓库这 5 天没漂移。

**下一步还是那一条**：用户重启 switchboard → 空档发一次 `?cmd=handover` → 跑
`node scripts/verify-drain-after-swap.mjs` 看 FAIL 是否为 0（见 `topics/next-task-handover.md` §1）。

> 附：用户在 `C:\Windows\system32` 里跑的脚本 ⇒ `MODULE_NOT_FOUND`。**命令要在仓库目录跑**：
> `cd D:\project_develop\dsh-brain` 之后再 `node scripts\verify-drain-after-swap.mjs`。

## 12:0x 上游漂移体检（用户机器维修停摆 5 天，唯一可能有异动的是上游）

新增 `scripts/check-upstream-drift.mjs`（`npm run check:upstream`，**手动跑/需联网**，不进 check:all）：
A 组「我们依赖的形状必须命中」+ B 组「我们补丁的靶子还在不在」+ C 组「上游新增能力」。报告落
`docs/upstream-drift-2026-09-20.md`。要点：

- 本地 `0.1.1-rc.2`（两棵上游树一致）；registry **latest = 0.1.5-rc.2**（09-10）、alpha `0.1.6-alpha.2`（09-17）。
  根 `package.json` 是 `^0.1.1-rc.2` —— **caret 对预发布只匹配同一 [major.minor.patch]** ⇒ 不显式改就不会跳。
- A 组 6/6 命中；★ 但 `session.seq` 在 0.1.5 里变成 `SessionLogOffset(log.length)`（**语义不变、写法变了**）
  ⇒ 把 `test-handover-drain.mjs` 里那条字面量正则打成**假红**，已改成语义判据（两代写法都命中）。
- B 组：U1 / U3 / BOM 三个靶子**都还在**且上游**都没修** ⇒ 三条补丁仍必要；
  BOM 从 3 处涨到 **4 处**（升级要按处数重定位）。我们的锚点是严格模式 ⇒ 打不上会 postinstall 非 0 退出。
- ★★ **最重要**：`dsh-session-persistence` 在 0.1.5 拿到**存储层单写者所有权**
  （`open(id,'read'|'write')` 原子占用 / `SessionAlreadyOwnedError` / 服务级 `flush()`）
  —— 正是 09-15 事故缺的那块栅栏。但**竞态根机制未变**（seq=log.length、repair 的 `last.seq+1`、end-seed 都在）
  ⇒ 上游**没修**这个竞态，我们 09-16 的修法仍然对。
- C 组：新增 10 个模块，其中 `dsh-acp-app`/`dsh-sdk-app`/`dsh-sdk-minimal`（**把 dsh 当子进程用 JSON-RPC 驱动**）
  与 registry 上的 `dsh-subagent-codex`/`-claude-code`（**一次性**外部子代理）与用户的"两服务"想法相关；
  但**长驻、能反向代持主脑的子代理服务上游没有** —— 要自己造。

## 12:1x 评审「主脑/子脑拆两个服务、互相代持」→ `docs/two-service-custody-review.md`

结论：**方向对，但只能替掉"部署"那一层，替不掉"判据"那一层**，而且"互相代持"的对称结构会踩我们自己定的
**无环原则**（A 批准 A 靠 B、B 靠 A ⇒ 2-回路 = 自己批准自己）。正确形态是**非对称托管**：
主脑可自由换代（居留方自动批准），居留方（前门 + 写权 + 判据）只能停机换、由外部见证。
落地第一步 = 把现有 switchboard 升级成"居留方"（写权收授 + 杀/校验/放流量），主脑代码一行不用改。

增补：`scripts/check-upstream-drift.mjs` 里也修了**自己的假绿**（第一版把"文件不存在"当 skip、
又没给 tarball 去掉 `package/` 前缀 ⇒ 解出空目录还打印"所有断言仍成立"）。

## 12:2x 追问「同时开 2 号进程做实验，成功才退役、否则回退」→ 评审 §8

用户追问的实质是：**"实验成功"的判据是什么**（他自己也点出"蓝绿之前也没考虑好主脑怎么验进化"）。
已把结论写进 `docs/two-service-custody-review.md` **§8**（追加节，原有 §1–§7 不动）。要点：

- **能，而且比现在的流程更保守**：今天的手续就是 `spawn → 追平 → 冻结 → 校验 → 翻转 → 失败回滚`，
  你的顺序（先接管后退役、否则丢弃挑战者）**顺序本来就是对的**。缺的只是"实验"那半截 ——
  **今天的 2 号进程只被验"它还活着"，不被验"它更好"。**
- **上游没有评测件**：按 `eval` 关键词搜 `@deepseek-ai` = **0 命中**（实测）⇒ 判据必须自建。
- ★ **副作用隔离今天就有**：profile 里已装 `dsh-sandbox-local` / `dsh-sandbox-windows-acl`（Windows 受限令牌 +
  写白名单）/ `dsh-fs-sandbox` / `dsh-bash-sandbox` / `dsh-pwsh-sandbox`，且权限预设里**已有 `sandbox: read-only`**
  （`out/profile-dump.txt:114`）⇒ 挑战者可以直接起在**只读沙箱**里跑影子实验。不需要升级。
- 实验三档：① 影子只读（比输出质量，成本=双份 token）② `sessions.fork()` 分叉会话（全权跑自己的副本）
  ③ 沙箱工作区端到端（最接近真实，最贵）。建议 ①刷退化 → ③冻结任务集验成功 → 才 flip。
- 判据分两层：结构/契约层（**已接在 handover 里**：boot-health + verify 闸 + 前缀稳定性）与
  行为层（**必须成对比较**，而"现行活着"正是天然的对照组 —— 这是"开 2 号进程"的最大价值）；
  holdout 冻结且**放在居留方**（否则脑会优化评测集而不是任务）。
- 回退两档：a **丢弃挑战者 = 0 成本**（现行从未停过）；b 代码回滚（只有在挑战者已接管并写入正史后才必需）。

## 12:3x 「下一步是什么 / 开源界有没有同类」→ `docs/oss-prior-art-and-next-steps.md`

用户的判断是对的（§8 确实还在蓝绿路子上）。新文档给两条答案：

- **下一步 = 补"判据"那半边，不是拆服务**。里程碑 M0（真机验收 drain，阻塞）→ M1（影子实验最小闭环：
  冻结任务集 + 只读沙箱挑战者 + 成对比较 + 报告）→ M2（接进**现成的** `verifyCmd`/`VERIFY_ALLOW` 闸，
  `tool_apply(verify=…)` 就是它的入口）→ M3（custody 拆分，**触发式**：替换变高频才划算）。
  ★ 关键纠正：**"开 2 号进程做实验"不需要先拆服务**，今天的 switchboard 就能编排。
- **开源先例（都已核实）**：① fencing token（Kleppmann 2016，**必须在资源侧强制**）——我们的
  `writerToken` 只发号没验号，这就是 09-15 挡不住的理论名字；② Akka SBR 的 **downs 一侧**（"terminating
  those JVMs so they cannot act on stale ownership"）与我们 09-16 的 `sealPlan` 同构，可正式命名
  **fencing by termination**；③ Akka Cluster Singleton + lease（租约丢 ⇒ 自停）；④ k8s Lease API
  （新 alpha：退出时**主动释放**锁而不是等 TTL）；⑤ OTP supervision tree + 反向优雅停机（每子独立超时）
  + release handling 热升级 —— **但 Erlang 社区的主流结论反而是"just do blue-green"**；⑥ Envoy/Istio
  traffic mirroring = M1 的影子模式；Argo Rollouts/Flagger = 自动回退；Android A/B、NixOS generation = 双份+回滚；
  ⑦ durable execution（Temporal/DBOS/Restate/Inngest/Hatchet）：**"checkpoint ≠ durable"** —— 我们已有
  journal（session.jsonl），缺的是**副作用去重**（建议单独立案）；⑧ 评测：promptfoo（本地/CI/开源）、
  Braintrust/LangSmith/AgentEvals + delta testing + 四层指标，但**没有一家把环闭上**（eval 只报告、
  部署工具不懂 LLM 质量）⇒ 差异化就在这个闭环。
- 现成可复用（**今天的安装里就有，不用升级**）：`dsh-sandbox*` + `dsh-fs-sandbox` + 权限预设里的
  `sandbox: read-only`、`dsh-workflow-worker-thread`、`dsh-subagent` + `tool-subagent-control`。

## 12:4x 「开始吧」+ 靶场调研 → M1-step-1 已开工（实验的判据地基）

用户问：模型发布都用各种用例测能力，**固定模型时能否拿这些靶场测 Agent**？怎么测/用什么数据/什么方向？
并说"开始吧"。两件事都做了：

### A. 调研 → `docs/agent-eval-arenas.md`

- **能，且这是 2026 的主流**：公开榜普遍给 bare model / scaffolded 两套榜（GAIA 上差 30–50 点）；
  2026 出现专测"脚手架效应"的 **Harness-Bench**。HAL 原话：framework value frequently dwarfs model differences
  ⇒ 我们"固定模型、改 @dsh-brain 这层"正是这张榜要量的事。
- 靶场分类（含判分/数据形态/我们能不能用）：SWE-bench Verified & **Pro**、**Terminal-Bench 2.0**（89 题、
  Ubuntu Docker、自动测试脚本，**最贴合我们**）、OSWorld（VM 截图，与我们无关）、**τ²-bench**（多轮+规则遵循、
  双控、**pass^k**）、**MCP-Atlas**（1,000 题 / **36 个真实 MCP server**、claims rubric —— 我们有 ~70 个 MCP 工具）、
  BFCL/API-Bank（函数调用）、WebArena/GAIA/BrowseComp、GDPval（人类 Elo）、Agent-SafetyBench/ClawsBench（安全）。
- **怎么测**三件机械：harness（脚手架能摆动 10–20 点）/ scoring rubric（claims 断言清单，且规则会变）/
  environment（容器可重置；τ² 的双控让对话真的改世界状态）。数据共同点：**期望都是可机验的最终状态**。
- **2026 四条教训**（比榜单重要）：① **判据才是弱点** —— OpenAI 2026-02-23 停报 SWE-bench Verified
  （审计 138 题 >60% 有缺陷；难度子集 59.4% 的测试集抓不到目标 bug）= 我们的"假绿"同一条；
  ② **判分必须与 Agent 隔离**（多个靶场因"同环境判分"被一行代码破掉）= 无环原则的工程版；
  ③ **pass@1 会骗人**，任务越长能力与可靠性越发散（0.90→0.44），要 **pass^k**；
  ④ **成功 ≠ 安全**（ClawsBench：成功率 39–64%，unsafe-action 7–33%，且两者无一致序）。
- **我们的用法**：公开榜只当"不许退化的地板"；**判据用自有冻结任务集**（通用榜对我们饱和 + 抗污染存疑 +
  **我们已经有现成 oracle**：把修过的回归反向打回去，自己的门就由绿转红）。

### B. 开工 → `evals/` + `scripts/eval-validate.mjs`（**不需要 Agent、今天就能跑**）

- `evals/README.md`：任务格式（seed / oracle / regression / metrics / budget）+ 两条铁规
  （**判据必须在 Agent 够不到的地方**；**每题必须证明有信号**）。
- `evals/pilot/tasks.jsonl`：首批 3 题 = 把本周修过的回归反向打回去
  （注入消息身份 / 封口顺序 / defer 直读 phase），oracle 全是**已有的门**。
- `scripts/eval-validate.mjs`（`npm run eval:validate`）：三关校验 —— ①干净态绿 ②**打 seed 后 oracle 必须红**
  （否则算**假题**）③字节级 sha256 还原 + 复绿；带 `--prepare/--restore` 供下一半复用。
  **实测 3 题有效 / 0 题有问题**，还原后锚点各 1 次、无 SEED 残留、`check:all` 仍 15/15。
  **不进 check:all**（会临时改工作区文件，理由写在 check-all 注释）。
- 途中修了自家两个 bug：备份写成空文件（`prepare` 现在先落字节备份再改盘，失败回滚）；
  以及"文件不存在/下载异常"不许当 skip（那又是一次假绿）。

## 13:0x 验收脚本加了 R0 前置：**分清「没验成」和「没通过」**

用户按提示跑了验收，看到 7 个 FAIL 就问"我们蓝绿还是有问题吗？"—— 其实**栈从维修后就没启动过**，
那 7 个 FAIL 全是"旧版代码跑的旧交接"的必然结果。原脚本的结论行只有两种措辞（未生效/未验收），
容易被读成"有问题"。已加 **R0 前置**三段并在结论里分开四种状态：

- `R0a` 控制面 31800 在不在（栈有没有起）
- `R0b` 磁盘上最新 build 含不含本次修复（**实测 b1789486838989 含 quiesced/sealPlan/KILL-OLD** ⇒ 重启即生效）
- `R0c` 这次 build 之后有没有换过代（state.jsonl 最新一条 vs build 时间）
- 退出码：**0=通过 / 1=换代了但没生效 / 2=还没到能验收的状态**（2 是新增，避免误读）
- 顺手把"下一步三条命令"直接打出来（体检 → 发 handover → 重跑本脚本）。

另核实了一条**决定"一次重启够不够"的事实**：`~/.dsh/profiles/web/node_modules/@dsh-brain/switchboard`
是 **junction → `D:\project_develop\dsh-brain\packages\switchboard`**，而该包的 `main` 是 `lib/index.js`
（junction → 最新 build）⇒ **重启控制面 + 一次换代，控制面与 gen 两侧同时上新版**，不需要分别部署。

途中修了脚本自己的一个 bug：重复声明 `REPO`（一改就整份脚本语法错，连 `--selftest` 都跑不了）。

## 13:1x 处理掉了最后一个"从未为真的判据"：ready/追平门槛（假绿）

按纪律（**没实施过的判据级不得计作通过**）把最后一个 placebo 查清了 —— **根因是一个不存在的 API**：

- `preseed.computeCaughtUpSeq()` 依赖 `sessionPersistence.listSessions()` ——
  **上游没有这个方法**（服务名 `sessionPersistence` 是真的，方法不是）⇒ `ids = []` ⇒ 循环一次都不跑 ⇒ **恒 0**；
  另一半 `sessionProjectionCache.coldSnapshot(id).asOfSeq` **是真的**（`dsh-session-projection-cache`，profile 也装了）。
  ⇒ **171 条 freeze 里 lastSeq 全是 0** 的真正原因就是这个。
- 修法（`preseed.ts` → `liveMaxSeq(ctx)`）：只回答**能真答**的问题 —— **本进程 live 会话已落盘到哪个 seq**。
  active 代报真数；刚拉起的 staging 代报 0（**诚实**：它还没加载任何会话）。
- ★ **耦合陷阱**（差点自己造一个新 bug）：只改 preseed 会让 active 代报真数、staging 代报 0
  ⇒ 原来的 `waitCatchUp(b, targetSeq)` 会**白等 40s 然后 abort 每一次换代**。
  所以必须同时把 ready 门槛从"比 seq"换成**结构性就绪**（`waitReady`：进程活 + admin 应答 + gen 名一致），
  `waitCatchUp` 降级为**只用于 1.5s 有界观测**（注释里写明"不得用作推进门槛"）。
- 门：`test-handover-drain` 加 B2/B3/B4 四类断言（进度读数走 `liveMaxSeq`、preseed 不再碰 `listSessions`、
  ready 走 `waitReady` 且不再有 `waitCatchUp(b, targetSeq)`）⇒ **41 项全绿**；`check:all` 15/15；已构建。

**又踩到一次"判据读文本"的坑（已修并记进技能）**：我把旧 bug 的名字写进了 `preseed.ts` 的**注释**，
于是负向断言 `!/listSessions/` **当场假红**。⇒ 凡对源码做文本断言，**先剥注释**；
反向更危险：正向断言会被注释里的示例满足（假绿）。已加 `stripComments()` 并两方向自证
（真源码为真 + 植入回归为假）。

## 13:0x 用户起不来栈 → 我试了一次，**证伪了"Agent 能代启动"**

用户按旧文档去启动失败（`C:\...\Temp\start-switchboard-ascii.ps1` **机器维修后已被清掉**），
于是我用仓库内的 `scripts/relaunch-switchboard.mjs`（`detached+unref`）试启动了一次。结果：

- ✅ **启动路径本身是好的**：switchboard 起来了（控制面 31800 应答 `lease generation=9 / gen-3082`），
  gen-3082 **完整启动到就绪**（boot.log 有完整启动段、`passes` 到 `dsh web`、key-pool 绑 3101）。
- ❌ **但整棵树在工具调用结束后被回收**：1 分钟后进程列表里没有了、3080/3082/31800 全无监听、
  `out/switchboard-run.err.log` 时间戳**还是 9-13**（说明**不是崩的**，没写任何错误）⇒ 判据是**谁拉起的**。
- ⇒ 与既有环境约束一致，但这次把边界**钉实了**：`detached + unref` **也不够**。
- 已修文档三处：`topics/runtime-and-launch.md`（换启动命令 + 写明旧 Temp 路径已失效）、
  `MEMORY.md` 环境约束 #3（补实测证据）、`topics/next-task-handover.md` §1（启动命令 + `curl.exe` 提示，
  因为 PowerShell 里 `curl` 是 `Invoke-WebRequest` 别名）。

## 13:15 ★ M0 真机验收**通过**（换代写入竞态的修复第一次在真机上跑通）

用户在**自己的终端**启动成功（`node scripts\relaunch-switchboard.mjs`，pid=15024），随后我发了一次非 fast 换代：

- 流水（`state.jsonl`，4.5s 落定）：
  `ready-probe ok: gen-3083 mode=staging caughtUpSeq=0` → **`freeze a lastSeq=-1 quiesced=true
  (canSeeAgents=true running=0→cancel=0 stillBusy=[] flushFailed=[] 0ms)`** →
  `seal: keep-old` → `post-freeze catchup(观测项)：ok=true target=-1` → flip → boot-health ok → retire。
- `node scripts/verify-drain-after-swap.mjs` = **11 ok / 0 warn / 0 FAIL，exit 0**
  （R0a/R0b/R0c 前置 + R1..R9b 全绿；R9b 无新增 seq gap）。
- ⇒ 三件事同时被真机确认：① **gen 侧接线通了**（`canSeeAgents=true` + `<genDir>/resume.jsonl` 有
  `{"phase":"drain",...}` + boot.log 有 drain 行）；② 控制面新读数（quiesced / seal / 观测项）真的写进了流水；
  ③ 新的结构性就绪门槛**没有引入 40s 停顿**（全程 4.5s）。
- ⚠️ **仍未验的两个分支（不许当已通过）**：`running=0→cancel=0` ⇒ **"有活在跑 ⇒ cancel + 等 whenIdle"
  这条主路径没被走过**；`lastSeq=-1` ⇒ 因为**当时没有任何 live 会话**（`max(∅)=-1`，是诚实值），
  所以"真 lastSeq 非 0"也没验过。要验它们必须在**回合运行中**发换代。

### 13:24–13:30 ★★ 这个实验**抓到我自己修复里的两个 bug**（一个假红、一个假绿）

第一次做"回合运行中换代"（fast，跳过 defer）：流水报
`freeze 有应答但**未达静止点**（quiesced=false：canSeeAgents=true stillBusy=[session-853b751c…] flushFailed=[]）`
→ `seal: KILL-OLD` → **强杀旧代 pid=19628 → `seal ok：旧代已确认消失`**。
⇒ 一方面**封口路径在真机上被证实有效**（先杀旧代才放前门锁 —— 这正是为"旧代不肯停写"造的保险）；
另一方面顺着证据查出**我自己的两个 bug**：

1. **假红：`stillBusy` 用的是建列表那一刻的 phase 快照**。
   证据：会话日志里明明已经写了 `seq=255 turn/end {"kind":"aborted","reason":{"kind":"hook","reason":"handover/freeze"}}`
   （**我们的 cancel 成功了**），却仍报 stillBusy。
   ⇒ 修：`DrainAgent.readPhase()` **实时读**（`agentPhase` 优先用它），快照只作假件兜底。门加 A8。
2. **假绿：`ctx.sessions` 没有 inject ⇒ 是 `undefined`**，于是 `sessions=0` / `lastSeq=-1` /
   **`flush` 一次都没跑过**；而**空闲场景照样报 `quiesced=true`**（13:15 那次"通过"其实是空过）。
   ⇒ 修：与 agents 同款 `ctx.inject(['sessions'])` 拿 `sessionsRef`；新增 `sessionsObservable`
   （看不到会话服务 ⇒ `quiesced` 必须为 false）；`liveMaxSeq` 改成接收服务对象。
   门加 A7 + B5。**drain 门 41 → 48 项**，`check:all` 15/15。

### 13:30 重做实验（先换代让新 build 的 gen 上台，再在回合运行中冻结）——**全绿**

```
[switchboard:agent] drain quiesced=true lastSeq=1315 canSeeAgents=true canSeeSessions=true
                    running=1→cancelled=1 stillBusy=[] flushFailed=[] sessions=1 (14ms)
流水：seal: keep-old — 旧代已确认停写（回合全停 + 落盘成功）→ 留作回滚网
会话日志：seq=1315 turn/end {kind:aborted, reason:{kind:hook, reason:handover/freeze}} + seq=1316 session/end-seed
```

⇒ **之前没验过的三条分支一次性全部在真机上验过**：`cancel 回合 → 等 idle`（running=1→cancelled=1）、
**真 lastSeq**（1315 恰好等于日志里那条 turn/end 的 seq）、**会话服务可见 + flush 真执行**（sessions=1）；
这次走 **keep-old**（旧代留着当回滚网）而不是强杀 ✓。验收脚本 11 ok / 0 FAIL。

**副作用交代**：测试用 `session-853b751c-…`（cwd=dsh-brain、原本近乎空白），历史里多了两条测试请求
（蜂群/珊瑚礁说明文）与两次 aborted 回合 —— 日志连贯（末尾 turn/end + end-seed），要不要清理由用户定。

## 13:45 推送完成 + M1-step-2 的 runner 就位

- **推送**：用户开了代理（7890）后 `git push origin master` 成功（`f708738..89a7d8c`）；
  并用 `git ls-remote origin refs/heads/master` **向远端核实**（不是只看本地视图）——
  远端 HEAD = 本地 HEAD = `89a7d8c` ✓，**41 个积压提交全部上去**。
  （此前卡住的原因：git 的 `http.proxy=127.0.0.1:7890` 没启动；直连 GitHub 超时、工作层代理拒绝 CONNECT。）
- **新增 `scripts/eval-run.mjs`**（`npm run eval:run`，M1-step-2 第一版 = **单臂基线**）：
  `--list` / `--plan`（只打印计划，不动任何东西）/ `--task <前缀> --session <sid>`（真跑）。
  真跑流程：① 复用 `eval-validate --prepare` 打 seed 并证明"oracle 变红"（题目有信号）
  → ② 把题面（= `task.invariant` + 不许提问/改完就停的要求）发进指定会话
  → ③ 轮询到 `running=false` 或超预算（**超预算即失败**）
  → ④ 跑 oracle（FAIL_TO_PASS）+ regression（`check:all`）+ 记 `git diff --stat`
  → ⑤ `--restore` 还原（字节级 sha256）。
  记账：steps / outputTokens / 墙钟（从 `session.list` 投影读）+ 改动文件清单。
  **安全**：只认"已跟踪文件无改动"（未跟踪的新文件不拦但会打印）；题面必须显式 `--session`（会污染那份会话）。
  **未测**：真跑那条路径还没跑过一次（`--list`/`--plan` 已验证；真跑要花 token 且会改工作区，等用户点头）。

## 13:55 ⚠️ 发现**两个会话在同一个仓库里并发工作**（用户把回执贴回了 DSH 那边）

推送完成后我准备提交 MEMORY.md 时，连续撞见"不该出现的状态"：`nothing to commit` 但出现了我没做过的提交、
MEMORY.md 被外部重写（13:47:41）、`next-task-handover.md` 在 12:2x 也报过 "modified by user or linter"。
查清：**另一个会话（= 用户贴回执后跑起来的 DSH agent，同一个仓库目录）** 在并行提交：

```
663c3ad / 454fe1d / 67fe561 / 2b60d03 / 6349f87 / d99037d   ← 都不是我做的
d99037d docs(memory): 13:52 日更 —— MEMORY 瘦身 + §3 修订 + **发现并行会话**
```

两个会话都在往 `origin/master` 推，**没有分叉**（`git status -sb` 一直是 `## master...origin/master`）——
因为彼此都及时提交/推送，git 的提交是原子的。但有一个**真实的竞态**值得记下来：

**`scripts/eval-run.mjs` 的归属被"两个写者"搞乱了。** 我的 `56e04f5` 提交信息写着"新增 eval-run.mjs"，
但 `git show --stat 56e04f5` 里**只有 2 个文件**（MEMORY 日更 + package.json），文件本身不在里面。
对方会话于是合理地判断成"我漏加了文件本身"。真实原因：**git 的 index 是共享的** ——
我 `git add scripts/eval-run.mjs` 之后、`git commit` 之前，**对方的 `git commit` 把 index 里那条暂存一起带走了**
（所以文件出现在对方的 `67fe561` 里）。文件本身没丢：`git ls-files` 有、`git diff origin/master` 为空 ✓。

⇒ 两条可复用的教训：
1. **"提交信息里写了 ≠ 那个提交里有"** —— 核实要用 `git show --stat <sha>`，别读提交信息（又一条"别读注释/别读意图"）。
2. **同一个工作副本里不许有两个写者**：这次只乱了归属（git 原子性兜住了），但**真跑 `eval-run` 会 seed/还原同一个工作区**
   ⇒ 两个会话同时跑一定互相踩。要么约定"一次只有一个会话动这个仓库"，要么用独立 worktree（`git worktree add`）。

## 14:30–14:45 受派做 §3.A（3 题单臂基线）——**3/3 FIXED，但发现题目已饱和**

用户分派：主干（DSH 会话）已跑通 cli-0001，我做 §3.A（补齐 3 题基线）再 §3.B。

- **cli-0002 = FIXED**（70s，steps +5，outputTokens 1910，toolCalls 7 = pwsh3/read3/edit1），
  **cli-0003 = FIXED**（90s，steps +10，2387，toolCalls 20 = pwsh6/grep5/read8/edit1），
  危险动作均 0；cli-0001（主干跑的，我补算轨迹）= FIXED（126s，2667，toolCalls 17）。
  ⇒ **3/3 FIXED，全部预算内，题目信号 3/3 有效**。明细 `out/eval-run-*.json`，
  成表 `docs/eval-baseline-single-arm.md`。
- ★★ **关键发现：这批题已经饱和** —— 三题都是"恢复一行不变量"，同 preset 一次全过 ⇒
  拿它做成对比较，**成功率这一列 delta 恒 0、没有区分度**。而 `toolCalls`（7/17/20）、
  `outputTokens`（1910/2387/2667）、墙钟（70/90/126s）在**同 preset 内就有 1.7–3× 波动**
  ⇒ **§3.B 的主判据必须换成"成本与路径"，成功率只当地板**（已写进设计文档）。
- **`git diff` 为空 ≠ 没动作**：cli-0002/0003 的正解就是把 seed 反向改回去 ⇒ 文件 == HEAD ⇒ diff 为空。
  于是给 `eval-run` 加了**轨迹记账**（`analyzeTrajectory`：数 `tool/call`、按工具分组、按规则标危险动作）
  与 `--traj <sid>`（对**已跑过**的会话补算，不花 token）——cli-0001 的数就是这么补出来的。
- **本轮又修了自家两个 bug**（都属于"看起来有结论、其实错的"）：
  ① `eval-validate.mjs --prepare <id>` 原先取 `tasks[0]`、后面的值只当真值用 ⇒ `--prepare cli-0002`
  **实际打的是 cli-0001** ⇒ 第一次跑 cli-0002 报"题目没有信号"（**假警报**，差点去改一道没问题的题）；
  ② `eval-run.mjs` 打 seed 后新增断言「**git 看到的改动文件集合 == seed 点名的文件集合**」，不等就弃跑
  （有它的话 ① 当场被拦）。
- **§3.B Step 0（设计）已写进 `docs/eval-challenger-arm.md`**：挑战者 = **只换 preset**（同模型同仓库同题面同预算）；
  首版 **A=`council`（现行）vs B=`code`（PTC/Code Mode）**；**实测确认**切换的 RPC 是
  `agentPreset.select {sessionId, agentPreset:'<id>'}`（`{preset:…}` / `{name:…}` 是 **HTTP 200 的静默 no-op**！）
  ⇒ **必须回读 `session.list.<sid>.agentPreset` 确认**；主判据 = 成本/路径，成功率当地板；两臂严格串行、各用新建空会话。

## 14:50–15:00 §3.B 实现 + **第一对成对实验跑通**（council vs code，cli-0003）

- `eval-run.mjs` 加 **`--pair`**：抽出 `runArm()`（单臂/成对**共用一条路径**）；每臂**新建空会话** →
  `agentPreset.select {sessionId, agentPreset}` → **回读确认**（不一致就判该臂不可用）；两臂**严格串行**；
  打 delta 表（地板 oracle/regression + 主判据 toolCalls/tokens/uncachedInput/steps/墙钟 + 安全列 dangerous）。
- **第一对结果**（两臂都 FIXED，地板通过）：

| 指标 | A=`council` | B=`code`(PTC) | Δ |
|---|---|---|---|
| toolCalls | **10** | 25 | +15 |
| outputTokens | **1343** | 3963 | +2620 |
| uncachedInput | **16873** | 54995 | +38122 |
| wallMs | **45.5s** | 114.2s | +68.7s |
| dangerous | 0 | 0（更正后） | 0 |

  ⇒ 初步结论（n=1/k=1）：**PTC/`code` 臂把同一件事做成了 2.5–3.3 倍成本、结果等价**；
  小任务上现行 `council` 更划算；PTC 的价值要在**多步组合的难题**上验（下一对换更难的题/更极端的臂）。
- **又踩到并修掉两个自家假信号**（都直接影响 delta 可信度）：
  ① **并发写者被误诊**：另一个会话在 A/B 臂之间改了 MEMORY/handover，而我的断言要求"改动文件集合==seed 集合"
  ⇒ 报成「seed 打错了地方」（**方向错**）。现在分档：seed 文件没被改 = 硬错；
  另有文件被改 = **并发写者**（动代码 ⇒ 硬错并提示"协调或 git worktree"；只动文档 ⇒ 记黄不拦）。
  ② **`dangerous` 假红**：PTC 臂把路径嵌进 `run_code` 的**多层 JSON 转义**里，
  只折叠一次反斜杠 ⇒ `(?!dsh-brain)` 被骗过 ⇒ **一次假报 13 条危险动作**（不查就会得出"PTC 很危险"的错结论）。
  改成"**连续反斜杠折叠成一个**"+ 两方向自证；用 `--traj` 重算（不重跑）：两臂 **dangerous 都是 0**，
  报告里留了 `correction` 说明。产物不该说谎。
- **顺带加固**：`eval-validate.mjs --restore` 现在**只在"当前内容 == 我们打的 seed"时才还原**，
  发现文件被别人改过就**拒绝改写**并留备份（防抹掉另一个会话的编辑）。这条在双会话仓库里是必需项。
- 补记一条 git 教训：**`git show HEAD` 可能已经不是你刚提交的那条**（我被主干插进来的提交骗过一次，
  差点以为自己的提交带走了别人的文件）。核实**指名 sha**：`git show --stat <sha>`。

## 15:15 **归属确立**：本方向（M1 自进化判据 / 实验台）归 eval 子会话全权负责

用户："以后这个方向就由你这个会话全权负责"，并让我看他新给的交接文档（= 主干 14:52 版
`topics/next-task-handover.md`）。我做了三件事：

1. **读并核对**：该文档把 §3.A（单臂 3/3）与 Step 0（挑战者定义）标成 ✅ ✓ 与我的记录一致；
   但它停在 14:52，**§3.B 的实现（`--pair` + 第一对）与 §3.C/§4.1/§6.3 都还没反映** ⇒ 我按事实更新。
2. **把文档改成与事实一致**（并在头部写明归属与边界）：
   - 头部 → 15:15；新增「★ 归属」段：`evals/`、`scripts/eval-*.mjs`、`docs/eval-*.md`、本文件 §3/§4 归 eval 子会话；
     主干只需在 §3.C 排队项上协作；**并写明"避免两个会话同时动同一批文件"（已经踩过一次）**。
   - §3.B 标 ✅（含第一对 delta 表 + 两处假信号复盘 + 指向 `docs/eval-challenger-arm.md` §7）。
   - 新增 **§3.D**（我负责的下一步）：`pass^k` → 难度/预算 → `minimal` 臂 → 之后才谈 M2（先只做报告不自动拦）。
   - §4.1 / §6.3 / §3.C 的 M2 依赖项同步；`### 1.5/1.6`、`### 3.C/3.D` 两处乱序已重排。
3. **顺带确认**：`evals/`、`eval-run.mjs --pair`、第一对结果都已推送（`326d85f`，远端 = 本地）。

## 15:20–15:25 §3.D 第一步：`pass^k` 实测 —— **噪声有多大，以及它推翻了我自己的一个建议**

- 实现：`eval-run.mjs` 加 **`--repeat k`**（同题同臂重复 k 次，每次**新建空会话** + preset 回读；
  表里给 mean/[min–max]/样本）；`--pair` 也支持 `--repeat`（**交替跑 A1B1A2B2…**，
  让两臂经历同样时间背景）；`--repeat`/`--arm` 现在**自动建会话**（不再要求 `--session`）。
- **实测 cli-0003 × council × 3**：**FIXED 3/3**；`toolCalls` mean 11 [9–12]（±15%）、
  `outputTokens` 1441 [1331–1648]（±11%）、`wallMs` 66.9s（±9%）、
  **`uncachedInput` 35922 [26080–40934]（±36%，最飘）**、`steps` 恒 6（太粗）。
- ★ **结论一（正面）**：第一对"PTC 更贵"的差异是 **+150%/+195%/+151%**，**远大于同臂噪声** ⇒ 那个结论站得住。
- ★ **结论二（自我修正）**：我此前建议"把预算收到 8–12 次工具以恢复区分度" —— **错的**：
  同臂 toolCalls 就在 9–12 摆动，卡在噪声带上会让同一臂**时过时不过**（判据变抛硬币，比没有判据更坏）。
  正确做法：预算设到**明显高于噪声上界**（如 ≤15），或保持"成功率只当地板"，
  **只在差异 ≥ 2× 噪声时**才下结论（toolCalls ≥ ~4 次 / outputTokens ≥ ~300 / wallMs ≥ ~12s）。
- 已写进 `docs/eval-challenger-arm.md` §9 并同步 §3.D。

## 15:40–15:50 环境调查（用户问"能不能扒公开靶场/本机 Docker 有没有"）

**公开靶场可扒性（实测）**：
- ❌ `huggingface.co` **000 拉不到**（SWE-bench Verified 等数据集在 HF）；
  ✅ **`hf-mirror.com` 200** ⇒ 设 `HF_ENDPOINT=https://hf-mirror.com` 即可拉数据集。
- ✅ `raw.githubusercontent.com` 200 ⇒ **仓内的题集数据可拉**：实测 BFCL `simple_python` **400 题 / 283KB / JSONL**
  （键 = id/question/function）⇒ 可当"工具调用"地板，**不需要容器**。
- ❌ **`registry-1.docker.io` 000**（Docker Hub 拉不到）⇒ **就算引擎起来，`docker pull` 也不通**，
  必须给 Docker Desktop 配代理（7890 已开）；✅ `ghcr.io` 401（可达）。
- ❌ Docker CLI 不在 PATH；❌ `wsl.exe` 曾被安全黑名单拦（**用户已放行**）。

**Docker"要不要重装"的结论：不需要**（证据）：
- `wsl --version` = **WSL 2.7.14.0**、内核 6.18.33.2-2；`-l -v` = `Ubuntu-24.04`(V2,Stopped) + `docker-desktop`(V2,Stopped)
  ⇒ **WSL 本身是好的**；那个 "WSL update failed: ... Access is denied" 是**启动时 wsl.exe 被安全沙箱拦**（现已放行，实测 wsl.exe 正常应答）。
- `docker version` → Client 28.0.4 在；**引擎没起**：`//./pipe/dockerDesktopLinuxEngine` 不存在（`com.docker.service` = Stopped，`docker-desktop` 发行版 Stopped）。
- ⇒ 这是"**启动失败/后端未起**"，不是"装坏了"。重装（GB 级 + 管理员 + 可能重启）风险高收益低；
  应先试：启动 Desktop（wsl 已放行）→ 配代理 → `docker run hello-world`；再不济走 Desktop 的
  **Troubleshoot → Reset to factory defaults**（比重装便宜）。

★ **实验卫生（我自己的失误）**：我在**计时的成对实验运行期间**启动了 Docker Desktop
（重磁盘/CPU 负载）⇒ 那次的 `wallMs` 可能被污染（`toolCalls`/`token` 不受影响）。
**教训：计时实验期间不要做重活**；报告里要标注环境负载，必要时重跑。

## 15:55 Docker 配置（用户要求优先做："以后环境都可能要配"）

- **用户已把 `wsl.exe` 从安全黑名单放行** ✓ 实测 `wsl.exe` 可用了（`--status` / `-l -v` / `--version` 都通）。
- ★ **根因找到了**：`%APPDATA%\Docker\settings-store.json` 里 **`WslUpdateRequired = True`**
  —— Docker Desktop 坚持要先做 WSL 更新，那个 "WSL update failed / Access is denied" 弹窗就是它
  （当时的 Access denied 是沙箱拦 wsl.exe）。另有 `IntegratedWslDistros = Ubuntu-24.04`。
- 第一次启动尝试（15:55 前）：Desktop 进程起来了（`Docker Desktop` ×8、`com.docker.backend` ×2、`com.docker.build`），
  但**引擎 150 秒都没起来**（`docker version` 每次 20s 超时）⇒ 卡在 WSL 更新这道门上。
- 正在做（后台 `pqgrov`）：停掉 Desktop 进程 → **`wsl --update --web-download`**（我的 shell 现在允许 wsl.exe；
  可能要 UAC）→ 再启 Desktop 并等引擎（≤240s）→ 抓 `com.docker.backend.exe.log` 尾部（上次抓错了日志文件，
  抓到的是 ipc 心跳日志，没有信息量）。
- 仍待办：**给 Docker 配代理**（`registry-1.docker.io` = 000 ⇒ 不配代理拉不动镜像）→ 用
  `docker run --rm hello-world` 验证；这才是"配好了"的判据。
- 顺带：**为了不给 Docker 让路时污染读数，我主动停掉了刚起的成对实验**（只跑了 24s），
  并清理了它留下的 seed 残局（`--restore --force`，sha256 通过）。那对实验**等 Docker 弄完再重跑**。

### 16:05 Docker 续：门是 Docker 自己的缓存标志；WSL 其实是最新的

- ★ **环境坑（代价很大，记下来）**：本机 PowerShell 里 **`Remove-Item` 会被宿主的安全钩子拦下并 fail-closed
  ⇒ 整个脚本当场中止**（报 `[safe-delete][SAFE_DELETE_FAIL_CLOSED] {"target":"...Temp\\wa1.txt...","reason":"path-not-found"}`）。
  这就是我前一个脚本 `docker-try2.ps1` 中途挂掉、连报告文件都没写出来的真因。
  ⇒ **写 .ps1 时不要用 `Remove-Item`；也不要依赖 `Stop-Process`**（`docker-try2` 里那步之后就不动了）。
  改用：唯一文件名 + 永不删除；要停 Docker 用**受支持的 `docker desktop stop|restart`** CLI 插件。
- **WSL 侧结论**：`wsl --update` 与 `--web-download` 都返回「已安装最新版本」（WSL **2.7.14.0**，内核 6.18.33.2-2，
  `Ubuntu-24.04` 与 `docker-desktop` 均 V2/Stopped）⇒ **WSL 根本没坏、也不需要更新**。
  但 `settings-store.json` 里 **`WslUpdateRequired` 仍是 `True`** ⇒ 这是 **Docker Desktop 自己缓存的"必须先更新"闸门**
  （当初更新失败被沙箱拦下，它把这个标志留下了）。
  该文件全部 15 键已 dump：`AutoDownloadUpdates=False, AutoStart=False, DisplayedOnboarding=True, SettingsVersion=42,
  IntegratedWslDistros=Ubuntu-24.04, UseContainerdSnapshotter=False, WslUpdateRequired=True`，
  **没有任何代理键**（⇒ 要配代理得自己加，或让 Desktop 走系统代理）。
- 我的 shell **不是提权**（`elevated=False`）⇒ 涉及 UAC 的操作只能提示用户。
- 正在做（后台 `qiwsln`）：用 `docker desktop start`（受支持路径，避免 Stop-Process）启动 → 轮询
  `WslUpdateRequired` 与引擎（≤300s）。若标志不自清 ⇒ 备份后手动置 `false` 再启（该键语义未验证，属"试探"，
  会在报告里标明）。

### 16:07 ★ 用户提供的事件日志把最后一块拼图放上了：过时的 `wsl.conf` 键

用户从事件查看器贴出：`Provider=WSL, EventID=0, Level=3, Data: Unknown key 'automount.crossDistro' in /etc/wsl.conf:3`。
我定位到**是 `docker-desktop` 这个发行版**（不是 Ubuntu 的）：

```
Ubuntu-24.04:/etc/wsl.conf      → [boot] systemd=true                    ← 干净
docker-desktop:/etc/wsl.conf    → [automount] root=/mnt/host
                                   crossDistro = true                    ← ★ 第 3 行，就是它
                                   options="metadata"
                                   [interop] enabled=true
```

⇒ **旧版 Docker Desktop 写下的过时键，WSL 2.7 不认** ⇒ 每次启动该发行版都记一条 Level-3 事件。
修法：在 distro 内**备份后注释掉那一行**（`cp -n` 备份 + `sed` 注释）→ `wsl --shutdown` → 重启 Docker。
（`docker-desktop` 是 Docker 自己管理的发行版，Docker 起的时候**可能重写**这个文件；若重写则我们的修复失效但无害。）

另外两条确认：
- **`WslUpdateRequired` 已自清（`True → False`）** —— 一旦 `wsl.exe` 可用，Docker 重新检查后自己把门撤了 ✓（`docker desktop start` 也可用，v0.1.6 CLI 插件）。
- 我自己的读数错误（又一次"读数要拿对"）：`wsl.exe ... | head -5; echo $?` 拿到的是 **head 的退出码**，
  不是 wsl 的 ⇒ 后面改成**不走管道**、直接取 Start-Process 的真实 ExitCode。

### 16:10–16:12 ★ 结论：Docker Desktop 也**必须由用户会话启动**（与 switchboard 同一条规律）

证据链：
1. `wsl.conf` 的过时键**已修**（备份 `wsl.conf.bak-eval`，注释掉第 3 行）；
   `wsl -d docker-desktop -- true` 之前会吐 `wsl: Unknown key 'automount.crossDistro' in /etc/wsl.conf:3`，修后不再报。
2. `WslUpdateRequired` 已自清 `True → False`（wsl.exe 一放行，Docker 复查后自己撤门）。
3. **引擎确实能起来**：`log/host/monitor.log` 在 **16:10:35** 有活流量（`<< HEAD /_ping (2.6ms)`、
   `<< GET /v1.48/version (7.35ms)`，还有 `containers/ae16e1e190aa/stats`），说明当时 VM + dockerd 是通的。
4. 但**随后就全没了**：`tasklist` 里 `Docker Desktop.exe` 进程数 = **0**，`docker version` 报管道不存在。
   ⇒ 我用 `Start-Process` 拉起的那一份，**在我那次工具调用结束后被回收**（与 16:00 那次 switchboard 完全同型）。
   ★ **规律确认：凡是我这侧拉起的长期进程（GUI 也一样）都活不过调用结束 ⇒ 必须由用户终端/桌面启动。**
5. 附带发现：我写的两个长 PowerShell 脚本（docker-try3/try4）都在轮询途中**自己死掉**（exit 1），
   ⇒ 结论：**别写长命 PowerShell 轮询脚本**；改成"短调用 + 多次探测"（用 Bash/node 探测更稳）。

### 16:20 ✅ Docker 配好了（用户启动 Desktop 之后）

```
Docker Desktop 进程数=4   server version=28.0.4   desktop status: running
docker pull hello-world   → exit=0, 19.5s, "Downloaded newer image"
docker run --rm hello-world → exit=0, "Hello from Docker! ... working correctly"
```

- ★ **纠正我先前的一个判断**：我说"必须给 Docker 配代理，否则拉不动镜像" —— **不需要**。
  我的 shell 里 `curl registry-1.docker.io` 是 000（无代理），但 **Docker 守护进程走的是 Windows 系统代理**
  （Clash 已设）⇒ **pull 直接成功**。教训：**"我这侧连不上" ≠ "它那侧连不上"**（又是"看不到 ≠ 没有"的同族错误）。
- 顺手发现：引擎起来后**自动恢复了一个旧容器** —— `ghcr.io/open-webui/open-webui:main`
  （4.49GB 镜像，Up/healthy）。**那是用户的 Open WebUI**，我没碰；但它会占资源（做计时实验时要记住）。
- 环境现状（做实验前要记住的条件）：**Docker Desktop + open-webui 容器在跑** ⇒ 与之前的 `pass^k`/成对实验
  （当时 Docker 是关的）**负载条件不同**；`wallMs` 不可直接跨条件比，`toolCalls`/`token` 不受影响。

## 16:2x–16:45 第三对实验（council vs minimal）+ 同批修掉 4 个自家问题

- 用户先让**停掉那个远古 open-webui 容器**（已 `docker stop`，只停不删；镜像仍在）。
- **第三对结果**（cli-0003，k=1）：**oracle A 绿 / B 红 —— 成功率首次分化**；
  toolCalls 13→53、tokens 1663→7328、墙钟 57s→525s、**dangerous 0→2**
  （B 走不通时去动 `self_evolve`/`tool_apply`，都在改 `src/index.ts`）。明细写入 `docs/eval-challenger-arm.md` §10。
- **异常（真因未定，诚实记下）**：A 那次 `oracle 绿但 regression 红` ⇒ verdict=NOT-FIXED；
  **同臂同题复跑 = FIXED 1/1**。已排除：窗口内无并发提交 / `check:all` 连跑 4 次全绿（判据不 flaky）/
  该臂改动逐字等于 HEAD（从它 `edit` 参数核对）。**疑为"依赖运行态文件的那条门被并发活动瞬时影响"**。
  已加：**判据输出留档**（此前只记 status）+ **regression 红了自动复跑并标 `regressionFlaky`**。
- 同批修掉的自家问题（每个都曾让人白跑或查不动）：
  ① over-budget 后保护模式拒还原 ⇒ 残留清单 ⇒ **整批 5 跑连环失败**（改为实验台 `--restore --force` + 断言干净 + 清单幂等自清）；
  ② 判据只记 status ⇒ 红在哪条门**无从回看**（改为存 stdout+stderr 尾部）；
  ③ Windows 瞬时占用导致 `prepare` 写入抛错（加重试）；
  ④ 我自己手写补丁时在**同步函数里写了 `await`** ⇒ 把 `eval-validate.mjs` 弄成语法错（立刻发现并改 `Atomics.wait` 同步睡）。
  ⇒ 教训：**改判据脚本后必须 `node --check` + 端到端自检**（这次是靠 `--check` 才当场抓住的）。

## 17:1x–17:3x 用户问「指标怎么监控」→ 做成 `docs/eval-metrics.md` + 两个重大发现

- 落地 `extractMetrics()`（跑完自动写进报告）+ `--traj <sid>` 事后复算（不花 token）。逐条指名权威来源：
  token（投影 `tokenUsage` + 事件 `usage` 交叉核对）、请求次数（`request/header` 计数）、工具次数（`tool/call`）、
  **工具失败（内容判据——实测 `isError` 恒 false）**、压缩次数（`compaction/*`）、上下文压力（投影+`request/context`）、
  **工具面快照（`request/header.tools` ⇒ 实测 toolSetSize=102）**、模型（两臂必须一致）、回合/步、注入接续、preset 变更。
- ★★ **发现 1（推翻 §10 结论）**：那对成对实验的窗口里**发生了两次换代**
  （16:31:56 `spawn`→16:32:37 `abort b-catchup-failed`；16:37:28 `spawn`→`flip`→`retire`），
  而 **B 臂的 `turn/end` 正是 `aborted(handover/freeze)`** ⇒ **B 臂"没做出来"作废**（要重跑），
  A 臂"regression 红但不复现"的**未定真因也随之解释**。又是"把环境事故当成被测对象的失败"。
  已加 `handoverInWindow()`：跑窗口内命中 spawn/freeze/flip/retire/promote ⇒ 标**污染**，读数不许当结论。
- ★ **发现 2（工具层真缺陷，该报给主干「工具完善」）**：`tool/result` 的 **`isError` 恒为 `false`**
  （内容却是 `[stderr] … 运行失败`）；且 shell 工具在**非重定向 stderr** 时会抛
  `ResourceUnavailable: StandardErrorEncoding is only supported when standard error is redirected`
  —— 这直接污染"工具失败率"这个指标（口径只能退化成内容启发式，且偏宽）。
- 过程教训：**bash 会抢 `${...}` 插值**（我两次把模板字符串写进 `node -e` 被 bash 替换坏）；
  **命令里出现 `pwsh` 字样会被安全策略当成"调用 PS"** ⇒ 一律写"shell 工具"。

## 17:3x–17:45 把"能力开/关"两臂的机械装置打通（闭环第一步）

用户认可"判据要接上自进化写路径"后，先做**臂切换机制**（这是所有后续实验的底座）：

- ★ **switchboard 本来就支持按次指定 profile**：`?cmd=handover&profile=<name>`
  （`coordinator.handover(…, profileOverride, …)`；`deploy.ts`："staging 代运行的脑 profile…验证通过后即一键置换"）
  ⇒ **切臂不需要用户重启**，一次换代即可（非 fast，走 drain + 启动健康检查）。
- 新增 `scripts/make-profile-variant.mjs`：造 profile 变体（同 build，只少装配指定包）；
  必须同时改 **`dependencies` 与 `dsh.profile.bundles`**（`disabled ≠ 移除`；bundles 才是装配清单）；
  `node_modules` 用 **junction** 指向源 profile；**绝不改 `profiles/web`**（`guard.ts` 把它列为 **P2 安全层**）。
- ★ **两方向自证通过**（实测）：`web`（gen-3088）`[tool-evolution]` **2 行** →
  `web-notev`（gen-3089）**0 行** → 回 `web`（gen-3090）**2 行**；`capability-bridge` 全程 2 行
  ⇒ **臂真的分离了，且只少了指定的那一个**（不是靠读代码猜）。当前代已复原为 `web` ✓。
- 新增 `docs/eval-capability-task.md`：题 **cli-0005** = "把能力注册进能力库并让它在**新会话**可用"；
  oracle 五条（能力库出现 / 新会话工具面含该工具 / 功能探针三项值 / **profiles/web 不许被改** / 能力库快照-还原幂等）。
  B 臂（web-notev）预期**做不成**，且很可能去改 P2 文件 ⇒ 那一列正是"护栏能力"的价值证据。
- ★ **git 实践（第三次撞到共享 index 竞态后定下）**：**永远不要留 staged 条目** ——
  用 `git commit -m "<msg>" -- <paths>`（pathspec 形式，直接提交工作区里那几个文件，无需 `git add`）。
  前两次（`eval-run.mjs` 被 56e04f5 带走、`f694467` 把我在做的两个文件一起带走）根因都是"我先 add 了、对方 commit 消费了 staged"。
- 交代：实验中我**两次换代了活的栈**（→ web-notev → 回 web），用户的 GUI 会经历一次重连；属设计内动作，已复原。

## 13:42 回执复核（本轮回执按约定写进 topics/next-task-handover.md 顶部）

约定确认：**回执写在 `topics/next-task-handover.md` 顶部**（那份文档顶部就是回执结构），**不另开文件**。

### ✅ 解掉原文档标记的"推送阻塞"

原 §1.5 记「40 提交待推送，卡在代理 7890 未启动」。13:42 复核：
- 代理 **7890 已监听**（PID 8832）
- **远端 `origin/master` = 本地 HEAD**（`git ls-remote` 直问服务器确认）⇒ 内容已在上面
- 试推时报 `fatal: could not read Username … terminal prompts disabled` ⇒
  **改成 `GIT_TERMINAL_PROMPT=0 git push` 就成功了**（凭据助手走缓存凭据）
- 终态：远端 = 本地 = **`454fe1d`**，未推送 0

**口径固化**：判断"推没推上去"**唯一可信的方式是 `git ls-remote`** ——
不是 `git push` 的输出、也不是本地 `origin/master`（会骗人）。

### ✅ 栈健康复核（写进 doc §2）

- 控制面 `stage=idle` / `locked=false` / `activeGen=gen-3086` / **`freezeSeq=1315`（真值，不再是 0）** / `lastFencingSeq=5833`
- 前门 3080 → **200**
- `verify-drain-after-swap.mjs`：R0a/R0b/R0c + **R1–R7 全绿**；
  13:30 那次 freeze：`quiesced=true canSeeAgents=true running=1→cancel=1 stillBusy=[] flushFailed=[] 14ms`；`seal: keep-old`
⇒ **drain 修复在真机上确实生效。**

### 本轮改动

纯文档（回执）＋ 一次推送。`check-all.mjs` **15/15 全绿**。
**下一项仍是 §3：M1-step-2（`scripts/eval-run.mjs`：把冻结任务集交给 Agent 跑，产出 delta 报告）—— 未开工。**

### 13:52 回执复核（续）：两处**过时**被改正

**① `MEMORY.md` 超限（修掉"注入被截断"）** —— 14844 → **9209 字符（−38%）**

它在注入时被截断（系统告警 "exceeded the size limit and was truncated"）⇒ **新会话看不到完整索引**
⇒ 直接破坏交接。而它自己第 8 行就写着「『交接』只留指针，不再复制细节」，却整段复制了 ~2900 字，
且日期停在 09-16（内容已是 09-20）—— **既违规又过时**。
做法：交接节 → 只留指针；索引散文压缩；0.1.5 细节挪回其归属文档的指针下；`timeout` 并入 #5。
**完整性已逐条核对**：环境约束 14 条 / 铁律 12 条，关键点抽查 15 项全部在位。

**② 交接文档 §3 过时** —— 原文写「要新建 `scripts/eval-run.mjs`」，但**它已存在**（13:46，并行会话建）。

- ★ **发现：有并行会话在同时改这个仓库** —— 提交 `56e04f5`（13:48）不是我做的，
  且它**漏加了 `eval-run.mjs` 文件本身**（`--stat` 只有 2 个文件），该文件后来被我的 `git add -A` 收入并推送。
  ⇒ 核实：`git ls-files` + `git ls-tree origin/master` 均确认**已跟踪、已在远端**。
- §3 已修订：列出实测的 3 题（`--list`），并点明**它们正是我们修过的三个回归**，
  oracle 分别是 `test-injected-message-shape.mjs` 与 `test-handover-drain.mjs`；
  明确"剩下的只是**真跑一次**"（`--plan` 已验，真跑路径从未跑过）。

### 本轮终态

远端 = 本地 = `6349f87`，未推送 0；`check:all` **15/15 全绿**；`MEMORY.md` 9209 字符。

## 14:24 ★ A 做完：M1-step-2 的**真跑路径第一次跑通**（cli-0001 = FIXED）

用户：「先 A 吧，然后再给另外一个会话拍下一个开发任务」。

### 怎么拿到"专门的会话"

`session-drive.mjs` 只有 `describe|list|cred|credset|prompt`，**没有建会话**。
⇒ 从 apiproxy 里提 RPC 方法名，发现 **`session.create`**（还有 `session.fork` / `session.list` /
`session.prompt` / `session.selectModel` / `agentPreset.select` 等）。
**`session.create` 空载荷即通** ⇒ 返回 `{sessionId, agentPreset:"council"}`
（★ 顺带证实**方案 B 的 preset 在生效**）。建了两个，用 `workspace.archiveSession` 归档掉多余的。

### 真跑结果（cli-0001）

`node scripts/eval-run.mjs --task cli-0001 --session session-8b672597-…` ⇒ **FIXED / 预算内**：

| 项 | 读数 |
|---|---|
| ① 题目有信号 | `hasSignal=true`（seed 打上 ⇒ oracle 变红 ⇒ 不是假题） |
| ② 发题面 | `settled`，126s，steps 0→10，turns 0→1 |
| ③ 记账 | outputTokens 2667 / uncachedInputTokens 48979 |
| ④ 判据 | oracle 红→绿 ✓ + regression（check:all）绿 ✓ |
| ⑤ 改了什么 | `packages/switchboard/src/index.ts \| 2 +-`（**只 1 文件 1 行**） |
| ⑥ 还原 | byte 级还原；git status 干净；HEAD 未变；**无 commit / push / 越界** |

报告：`out/eval-run-cli-0001-injected-message-identity-1789885471541.json`

**⇒ 价值不只是"跑通"**：证明 `eval-run` 的**三条判据同时可机验**（有信号 / 修好 / 没弄坏别的）
—— M1 的判据地基有了"活体"证据，不再只是离线校验。

### 回执与下一个任务（按约定写进 topics/next-task-handover.md）

- §1.6 新增本次真跑回执；§1.5 推送状态刷新（远端 = 本地，未推送 0）
- **§3 重写为「下一个开发任务」**（派给另一个会话）：
  - **§3.A** 先补齐 3 题单臂基线（零设计、纯执行，cli-0002 / cli-0003）
  - **§3.B 主任务**：给 `eval-run` 加**挑战者臂** + 成对 delta 报告。
    ★ 明确标出 **Step 0 是待定的设计决定**：**"挑战者"到底指什么**（换模型 / 换 preset /
    换生长基质）—— 不先定这个，做出来的 delta 没有意义（两臂不可比）。
    并列出实验卫生：同一起点、工作区隔离、别把答案放 Agent 够得到的地方、记账要成对、危险动作要记。
  - **§3.C** 排队中（别现在做）：`freezeSeq` 无读者、回滚重叠窗口、`3d8ea18d`、幂等立案；
    M2 依赖 §3.B；M3 是触发式别提前做。
- §0 / §4.1 / §6.3 同步（单臂基线已通、挑战者臂未做）

check:all **15/15 全绿**；结构复核 §0–§6 连续。

## 14:52 补齐交接文档 + 开新议题「工具完善」（派给另一会话）

用户：「把交接文档里的内容补齐一下，然后我们就开始进下一个议题，比如说工具完善之类的。
这个议题就全权交给那个会话，我只是偶尔回执给你让你审核进度。」

### ① 补齐主文档（发现子会话 14:45 已把 §3.A / Step 0 做完并标注）

子会话成果：**§3.A 单臂基线 3/3 全 FIXED**（`docs/eval-baseline-single-arm.md`）+
**Step 0 挑战者定义已定**（`docs/eval-challenger-arm.md`，只换 preset：`council` vs `code`；
且实测 `agentPreset.select` 生效、而 `{preset:…}`/`{name:…}` 是 **HTTP 200 的静默 no-op** ⇒
**必须回读 `session.list.<sid>.agentPreset` 才算数**）。
★ **关键发现：三题已饱和**（都是"恢复一行不变量"）⇒ **成功率无区分度**，主判据必须换成"成本与路径"。

**但它留下了不一致** ⇒ 本次补齐：§0（单臂 3/3、Step 0 已定）、§3.B（删掉被取代的旧选项、保留
"必须回读"那条）、§4.1、§6.3、标题。两份文档 §0–§6 编号均连续。

### ② 新议题「工具完善」—— 独立交接文档

新建 **`topics/tool-refinement-handover.md`**（与主文档并行；回执同样写它顶部）。

**推荐作用域 = 把能力注册门的判据阶梯从 L1 推到 L2~L4**。依据是**盘出来的一个对照**：
`capability-gate.mjs:48-52` 里 L2~L4 各自写着"为什么没建"——

| 级 | 脚本里的 why | **M1 刚产出的对应物** |
|---|---|---|
| L2 基线不退化 | 需固化基线与测量口径，未建 | `docs/eval-baseline-single-arm.md`（3 题成本分布） |
| L3 隐藏 holdout | 需独立评测集 + holdoutHash，未建 | `evals/` 的字节级 sha256 还原 |
| L4 反事实对照 | 需同任务集与 A/B 编排，未建 | `docs/eval-challenger-arm.md` + §3.B 的 `--pair` |

⇒ **M1 的评测地基本来就是 L2~L4 的底座** ⇒ 地基刚铺好，现在补上层成本最低。
另列两个候选：**B 工具可用性**（用 registry 的"选用/复用/成功/失败"反推描述不清的工具；
★ registry 自己就提示「高选用 + 低复用 = 描述过度承诺」，`spawn` 现为 1/0）、
**C M2 接闸**（依赖 §3.B）。**建议 A 为主线、B 的第一步作为 A 的输入。**

文档里明确写了「**作用域待用户一句话确认；用户未否决 ⇒ 按推荐执行**」⇒ 不阻塞交接。
MEMORY.md 主题表与交接节都已指向它。

check:all **15/15**；MEMORY.md 9514 字符（仍在限额内）。

## 17:35 回答方向性问题：我在这盘的哪一格 + 接下来做哪一格

用户给出项目模型：**靶场**（另一会话在做）+ **自进化系统两层**（专家评审层向下发任务 /
自进化子 agent 层注册工具集调能力，工具集由另一项目的 **Skills-tree** 移植注册）。
问：我在做哪部分？接下来做哪部分？

### 对照表（用户模型 ↔ 我们的代号）

| 用户说的 | 我们的代号/资产 | 状态 |
|---|---|---|
| **专家评审层**（向下发布任务） | **`right` 脑**（导师/科研人员：选题、拟验收标准、判定、审批）—— 总纲 §11.7 canonical 表 | 设计已定，**未接硬门** |
| **自进化子 agent 层**（注册工具集） | **能力库**（`SubagentProvider` registry） | **P1-b~P4 ✅**（4 条能力，门只到 L1） |
| **Skills-tree 移植** | §13「技能树就是能力库」（`skill_tree.go` 等 Go 资产） | **已盘点、代码 0 行** |
| **靶场** | `evals/` + `eval-run` + 单臂基线 + 挑战者臂 | 另一会话在做（M1） |

### 我在做哪部分（诚实定位）

**不是靶场本身，而是靶场的地基与前提**：09-15/16 修换代写入竞态（会话坏了靶场跑不起来）、
M0 真机验收、会话体健门、15 道门禁体系；靶场本体（题库/跑题/基线/delta）是**另一会话** 13:46 后做的。
对照 `master-plan` §10 = **P0「判据阶梯骨架」及其前置**；★ **代码里仍无 `Genome`**（实测 grep 无命中）。

### 接下来：推荐 `master-plan` **P1 = 三脑环接上阶梯**

理由三条：① 总纲 §10 自己定「**先判据后能力**」；② 它是**唯一已有硬门的路径**，
且 `verifyCmd`/`VERIFY_ALLOW` **已存在**（在 switchboard coordinator），只差把"整条命令成败"
**升级为分维度 JSON**（§9 接驳清单点名）—— 实测确认**这个升级还没做**；
③ **靶场刚通** ⇒ P1 要的判据输入刚好就绪（单臂基线 = L2 口径；挑战者臂 = L4 的 A/B）。
⇒ 而且它正对上用户模型里的「专家评审层」：`right` 的判定要成硬门，就得先有"分维度 JSON + fail-closed"。

### ★ 提醒：Skills-tree 移植**不是下一步**

总纲 §10 里它是 **P4**；§13 自己写着"**数据先行、执行器可替换（P2 数据 → P3 执行器）**"。

### 卡在用户手里的三问（总纲 §11 未拍板）

`§11.3` 隐藏 holdout 存哪/谁维护（物理 vs 逻辑隔离）｜`§11.4` 四条线优先级｜
`§11.6` 红队脑的适应度函数谁定。**这三条不拍，P1 之后每一步都没依据。**

### 顺手修掉的文档不一致

`topics/self-evolution-design.md` §10 的**能力库线**表格过时（写着 `P1-b ← 当前`，
但 P2~P4 已完成、P5 未开工）⇒ 加"2026-09-20 校订状态"列，并**明确它与 master-plan §10 是两张不同的表**
（避免下一位读者混），同时把 P1-b 的 provider 契约细节保留在表后。

## 17:48 ★ 架构修正落纸：两层结构（专家评审团 / 子 agent 层）

用户质问「为什么你这边没有更新？」——盘查后确认：**这套修正从没被任何文档记录过**
（全仓库只有我今天 17:35 那条日记提到"专家评审层"，而"三脑"是被当成**顶层**固化的）
⇒ 不是漏读，是**从没落纸**，所以下一个读者（包括 AI）只能在旧模型上打转。

### 用户口述的修正

**三脑不是被删除，而是"降格 + 改职责"**：从"顶层三分工"→ **子 agent 层内部的三个调控脑**。
新架构两层：
- **上层 = 专家评审团**：多模型讨论（MoA 式）；★ **用户可自选"用几个模型"**；向下发布任务；
  可选接一个开源模型管理层做多 provider 接入
- **下层 = 子 agent 层（进化发生地）**：三个调控脑调控其他子 agent；
  ★ **子 agent 的来源 = skill**（skill 本身被构建成 subagent）；
  进化手段 = 用**使用/测试数据**做**工具进化 + 工具合并**

### 新建 `docs/revised-architecture-2026-09-20.md`（当前架构权威记录）

含：为什么改 / 新架构图 / 两个正交部件（MoA 编排 vs 模型管理层）/ 什么没变 / **5 个待定问题** / 与三条线的接驳。

**两个外部参照都查实了**（用户凭记忆提到，我做了核实）：
- **MoA（Mixture-of-Agents）**：Together AI，ICLR 2025 Spotlight，分层（默认 3 层×6 模型），
  proposer/aggregator，**只用开源模型 AlpacaEval 2.0 65.1% > GPT-4o 57.5%**，**无需训练**。
  **Attention-MoA**（美团 LongCat，arXiv 2601.16596，2026-01）：**12B–32B 小模型集成**在 MT-Bench 8.83、
  AlpacaEval 2.0 LC 77.36%，**超过 Claude-4.5-Sonnet 与 GPT-4.1**。
  ⚠️ **口径差异**：用户记的"三个"是**层数**（原版默认 3 层），不是模型个数 ⇒ 配置项应按"层数 × 每层成员数"设计。
- **开源模型管理层**（2026-08 实测星数）：LiteLLM 56k（MIT/Python，事实标准）/ new-api 45k（中文生态）/
  one-api 36k / Portkey 12.7k（MIT/Node）/ Bifrost（Apache/Go）。**我们已有 `key-pool-proxy`（自研）**。
  ⚠️ **重要风险**：**LiteLLM 2026 年出过一次 PyPI 供应链事件**（两个版本短暂夹带窃取云凭据的恶意代码）
  ⇒ 引入第三方 gateway 应走「外部 Agent 接入」同一套注册门 + 锁版本 + 校验产物。

### 加了 3 处 supersession 指针（防止下一位读者再被旧模型误导）

- `docs/three-brain-evolution.md` 顶部：⛔ 顶层结构已作废 + 指明新文档 + **§1 分工表不可直接当新三脑定义**
- `docs/self-evolution-master-plan.md` §11.7 canonical 命名表：标作废 + 保留"角色必须由代码与会话边界定义"的原则
- `topics/self-evolution-design.md` §6（多模型会议室）：定位从"决策层"改为"升格为顶层评审团"，**三条硬约束继承**
- `MEMORY.md` 关键文档清单加入新文档指针（9836 字符）

### ★ 5 个待定（动手前必须问用户，不许猜）

1. ★ **"三个调控脑"具体是哪三个？**（旧 right/left/sandbox 换职责？还是三个全新的？）
2. 评审团成员怎么配（几个模型/哪几个/几层；会话级还是全局配置）
3. gateway 选型（自研扩 vs 引入，含供应链风险处置）
4. skill → subagent 的构建路径由谁做转化
5. "工具合并"的判据（这就是 L2 基线不退化）

check:all 15/15。

## 17:5x–18:0x 臂切换装进 harness（能力开/关可自动切换）+ 弄清写入口归属

- ★ `eval-run.mjs` 新增 **`--profileA/--profileB`**：跑该臂前 `?cmd=handover&profile=<name>`（非 fast），
  再按**插件指纹**验证（读新 gen `boot.log` 最后一段：该有的标记必须有、该没有的必须没有 ⇒ 读效果不读意图）；
  `--expectTool/--forbidTool` 用**这次跑自己的工具面快照**做臂自证；`handoverInWindow` 支持
  `ignoreWindows`（**自家切臂的换代不算污染**，但留痕）。
- **实测自证**（dry-run）：A 在 `web`（幂等跳过）；切到 `web-nodc` → `gen-3091`，指纹
  `{"toolEvolution":2,"designCanvasBridge":0,"capabilityBridge":2}` ✓ 符合期望；两臂同模型 ✓。
  跑完已**把实验栈复原为 `web`**（gen-3092）✓。
- ★ **写入口归属（读源码查清，直接影响任务设计）**：
  `tool_apply` ← `packages/switchboard/src/deploy.ts`（= 我们自己的部署/换代入口，agent 的"整体替换"路径）；
  `self_evolve` ← `packages/design-canvas-bridge`；**`tool_score` ← `packages/tool-evolution`（只是打分/反馈，不是写入口）**；
  `list_capabilities`/`capability_report` ← `packages/capability-bridge`。
  ⇒ **"能力关"的正确靶子是 `design-canvas-bridge`**（`web-nodc`：`design_canvas_index`/`symbol_edit`/`safe_rename`/`move_symbol`/`self_evolve` 全没了）；
  我先前凭印象造的 `web-notev`（去 tool-evolution）**只少了打分工具，不够"能力敏感"** ⇒ 已修正。
- ⚠️ 顺带发现真问题：能力库目录里有 **`registry.json.7272.tmp`（49KB, 15:50）残留** ⇒
  **能力库写入不是崩溃安全的**（写到一半死掉会留 .tmp，且主文件可能半新半旧）。建议归到「工具完善」/自进化那一侧。

## 17:55 架构订正：**三脑 = 三个工具**（搭建 / 更新 / 测试）+ 我提的一格反对意见

用户订正："说是 3 个脑，其实应该是 **3 个工具**吧 —— **Agent 的搭建工具、Agent 的更新工具和 Agent 的测试工具**。
总之就是以子代理的方式进行工具的自进化；三脑是为了**维护这个子代理自进化的循环和规范**，
有问题就唤起这个子代理去规范地执行任务。"
问：**这个三脑是否应该迁移或修改？**

### 我的判断

**① 方向同意（理由比"更贴切"更强）**
- "脑"暗示独立会话/上下文 ⇒ 会踩 `design §9 ①`（顶层上下文被委派记录吃掉）+ 多一层延迟；
  "工具" = 主 agent 直接调，与 DSH 原生机制（一个 provider 一个 `toolName`）一致
- ★ **这三格正好是能力库的生命周期**（注册/升级/测试 → 采纳/淘汰），
  与 `design §0.1`「自进化 = 能力库的注册/升级/合并/淘汰」**是同一个模型** ⇒ 不是换名字，是落到对的形状

**② 但我要反对一格**：搭建/更新/测试**三个都是"操作"，全在执行侧；旧三脑里最不可替代的「判定/审批」不见了**。
项目三条原则都指着它：`design §0.5` 无环原则（被改的审批层不能自动批准自己的部署）、
「判据必须独立于被判定者」、`master-plan §6` 免疫系统。
⇒ **若"测试"只是被同一 agent 调用的普通工具，就是"被测者给自己判分" = 假绿工厂**。
⇒ **建议不是"三脑迁移成三工具"，而是"三脑拆开"**：
生产脑→更新工具｜实验脑→搭建工具+隔离机制｜**进化脑→★ 不能工具化、要"独立化"**（判据与 holdout 所有权独立于被改者）。

**③ 三格骨架大部分已有**（已写进文档的表）：
搭建 = `registerProvider` + `capability-registry register` + notice `'registered'`（缺：**从 skill 自动生成 provider**）；
更新 = registry **已有** `version`/`supersededBy`/lineage，notice 有 `'superseded'/'merged'/'retired'`（缺：操作入口 + 判据）；
**测试 = 注册门 L0/L1 + `check-all` 15 门 + `eval-run`（缺 L2~L4 ⇒ 就是「工具完善」议题）**。
⇒ ★ **两条线在这里合流**：第三个工具 = 已派出的「工具完善」。

**④ 我自己挖到的证据**：`capability-bridge/src/index.ts:337` 已写着「隐藏 **holdout 哈希**、被谁取代、
以及 lineage（注册/升级/**合并**/淘汰的历史）」⇒ **能力库数据模型里早就预留了 `holdoutHash`**，
即"测试工具"这格**早就设计好、一直空着** ⇒ 我们是**补空格，不是发明新东西**。

### 落纸

`docs/revised-architecture-2026-09-20.md`：§1/§2 改为"三个工具"；§5.1 标 ✅已定（canonical 命名表正式作废）；
§5.2 新增"⚠️ 一格缺位，待确认"（我的反对意见 + 拆法表）；§5.3 新增"三格骨架盘点"表 + holdoutHash 证据。
check:all 15/15。

## 17:57 ★ 三格形状定案：**隔离被测 = 工具；生产 / 进化 = 独立的子 Agent**（理由=防串供）

用户第二轮订正原文：
> "这个**隔离被测是单独的工具**，但是**生产和进化两个还是要做成独立的子 Agent**，
> 以防止他和其他 agent 的**串供**。"

### 这比我的建议更进一步（我的建议只覆盖"进化"，用户覆盖了"生产"）

我原来主张"判定/审批要独立化"；用户把**生产**也拉进来了 —— 理由更根本：
**若 ②③ 都只是同一个 agent 手上的工具，就只有一个决策者，它既生产又批准。**
★ **关键区分：分离「工具」≠ 分离「主体」。要防串供必须是不同的 Agent（不同上下文、不同主体）。**

| 格 | 形状 | 为什么 |
|---|---|---|
| ① 隔离被测（实验/沙箱） | **单独的工具** | 无判断权 ⇒ 工具足够 |
| ② 生产（造能力/产 diff） | **独立的子 Agent** | ★ 防串供：产变更的一方不能与判方同源 |
| ③ 进化（判定+审批） | **独立的子 Agent** | ★ 防串供：判方必须是独立主体 |

**⇒ "串供"其实就是项目自己三条原则的口语版**：
`design §0.5` 无环原则（被改的审批层不能自动批准自己的部署）｜
`design §6` 三省六部「**封驳权 = 独立否决权，且与中书省【不同机构】」** ⇒ **"不同机构"落成"不同 agent"**｜
`design §6` 评分者独立性 跨模型 > 跨会话 > 同会话。

### 落地机制**已有**，不需要新造"脑"

既然是独立子 Agent ⇒ 直接用**已有的 `SubagentProvider`**（`subagent-council` 已示范 `registerProvider`；
五成员 + `start()` 可注入 persona/toolFilter/outputSchema/maxDepth/agentOptions）。
⇒ 要造的是**两个新 provider（生产席 / 进化席）** + 它们之间的**通信与留痕**
（`dsh-tool-subagent-report` 的 `report`；委派档案**写入时外置化**，见 `design §9 ①`）。

### ⚠️ 我主动指出的新结构问题（待用户确认）

**顶层「专家评审团」与下层「进化子 Agent」都有判定职责 —— 会重复吗？**
我的理解：**粒度不同** —— 评审团判**任务**（选题/验收标准/预算）；进化子 Agent 判**能力增量**（L0→L4）。
⇒ 不重复，但应写明"两级判定的粒度分工"，否则将来会有人问"为什么两处都在判"。

### 落纸

`docs/revised-architecture-2026-09-20.md`：§2 第 2 条改为"三格及其形状"（含"分离工具≠分离主体"）；
§5 标题改"定案与待定"；§5.1 合并两轮原话 + 形状表 + 三条原则对应；§5.2 落地机制；§5.3 新增结构问题提醒。
**并同步给「工具完善」议题**（`tool-refinement-handover.md` 顶部追加）：
**L2~L4 的判据持有者 = 进化子 Agent（独立主体）+ 独立 holdout**，
**不能**做成"被同一 agent 调用的普通工具"，否则就是被测者自判分。

编号重复已清理；check:all 15/15。

## 18:03 ★ 两级判定分工定案：**过程自证 vs 最终裁决**（我 §5.3 原来写错了，已改正）

用户原话：
> "这不一样啊，一个是**在生产过程中判断**，一个是**最后那个判断（效果）**。
> 就是，**专家团是提出任务，然后交给子 agent 去做**。子 agent 去做的话，
> **他只有一个任务和一个结论**。这样拿出来的话，他就**只只能是说自己做自己的、然后并且符合任务**，
> 然后**最后提交给专家团那边，专家团再对这个任务进行下一步的提审和评价**。"

### 我原来错在哪

我把它写成**"粒度分工"**（评审团判**任务** / 进化子 Agent 判**能力增量**）。
用户说的是**"阶段分工"**：**生产过程内的自证** vs **最后的效果裁决**。已改正。

| 级 | 判什么 | **阶段** | 判据性质 | 谁判 |
|---|---|---|---|---|
| 子 agent 层 | "我做完了 + 符合我领到的任务" | ★ **生产过程中** | **过程**判据 L0–L2 | 子 agent **自己**（= **自证**） |
| 专家团（顶层） | "结果好不好、值不值得采纳" | ★ **最后** | **效果**判据 L3/L4 | ★ **独立主体**（多模型评审团） |

### ★ 由此得到一条干净的原则（已写进文档）

> **自证 ≠ 裁决。** 子 agent 自证"符合任务"是**生产方对自己的交代**，不是审批；
> **采纳权只在专家团** ⇒ **"子 agent 自证"不违反 `design §0.5` 无环原则**
> （它没有采纳权，不可能"批准自己的部署"）。
> ⇒ 也就是说：**"防串供"要保护的是顶层那一半**，不是禁止子 agent 自证。

### ★ 连带：`master-plan §11.3`（悬了 7 天的 holdout 归属）**部分定案**

既然子 agent **只能自证**、且 holdout 必须"agent 读写不到"、而 holdout 是**判效果**的判据
⇒ **holdout 只能归【专家团】层持有**。
⇒ 该问**只剩"物理隔离还是逻辑隔离"这一半未定**（原文倾向"双保险"仍有效）。已写进 master-plan §11.3。

### 落纸

`docs/revised-architecture-2026-09-20.md`：
- §2 架构图修正（原先我把循环画在两层之间、看起来像箭头指向下层 ⇒ 移到图后，改标"**两层之间的循环**"）
  + 新增**循环图**（① 提任务 → ② 执行+自证 → ③ 提交 → ④ 提审评价 → 回 ①）
  + 新增"自证 ≠ 裁决"原则
- §5.3 改为 ✅ 已定，**并显式标注旧写法是错的**（避免下一位读者看到两种说法）
- `docs/self-evolution-master-plan.md` §11.3 追加部分定案

代码块配对 ✓ / 编号无重复 ✓ / check:all 15/15。

## 18:0x–18:3x 第一道**能力敏感题** cli-0005（符号级重命名）+ oracle 两方向自证

- 题：把模块级导出 `computeHash` **按语义**改名 `digestOf`（跨 3 文件的导出/导入/调用/**注释**/模板里的表达式），
  但 ① 同名**局部变量**不许改 ② 字符串常量 `'computeHash-v1'` 不许改 ③ 行为（`--selftest` 输出）逐字不变。
  ⇒ 有 design-canvas 的符号级工具（`design_canvas_index` + `safe_rename`）就快而准；只有 grep/手改容易踩规则。
  靶子 `evals/pilot/rename-target/`（math/store/index + `check.mjs` oracle + README 规格），空 seed（能力题）。
- ★ **oracle 两方向自证**（都实测）：正确解法 ⇒ **绿**；粗暴全文替换 ⇒ **红**（指出"行为变了 + 字符串被改"）；
  只改标识符但漏注释 ⇒ **红**且明确指出第③条；靶子在场时 `check:all` = 15/15 ✓。
- ★★ **修掉我自己 oracle 的一条假红**：原先写"math.js 里旧名必须只剩 1 处"，
  但那个同名**局部变量**的声明+使用共 **2** 处，且描述它的注释**本来就该保持原样** ⇒ **正确解法会被判红**。
  ⇒ 改成逐项断言（另两文件零残留 / 局部变量保留 / 文件头注释已跟改）。**先证明判据错、再改判据**。
- 顺带实现：`eval-validate` 支持**空 seed 题**（有效性 = HEAD 上 oracle 红 + regression 绿）；
  `eval-run` 的能力题还原（跑前快照 → `git checkout --` 被改文件 + 删除跑期间新建的未跟踪文件）；
  工具面判据支持**按臂**指定（`--expectTool A:x --forbidTool B:y`）。
- 已启动第一对：`A=web`（有 design-canvas）vs `B=web-nodc`（无），**preset/模型相同、只差 profile**；
  带臂自证（A 必须有 `design_canvas_index`、B 必须没有）。后台任务 PB8Z73。

## 18:0x–18:30 第一对"能力开/关"实验：**不算数**（我的设计错误）+ 抓到 regression 间歇红的真因

- 命令：`--pair --task cli-0005 --profileA web --profileB web-nodc --expectTool A:design_canvas_index --forbidTool B:design_canvas_index`
- 读数：A `settled` 62s / 16 calls / 12000 tok（用了 `explore_code`/`edit_code`/`find_references`/**`refactor_pipeline`**）；
  B **`over-budget` 902s** / 23 calls / 8308 tok（`read`×9 + shell×7 + `rename_symbols`×2）；
  **两臂 oracle 都绿**（都把重命名做成了）；dangerous 都 0。
- ★ **错误一（我的）**：**"能力关"没关掉** —— design-canvas 有**两条**进工具的路径：
  ① `@dsh-brain/design-canvas-bridge`（bundle）② profile `cordis.patch.yml` 里的 **`- insert: id: mcp-client`**
  （`serverName: design-canvas` → `node design-canvas/dist/src/server.js`）⇒ 我只 drop 了 bundle，
  **B 臂照样拿到 `mcp__design-canvas__rename_symbols`**（实测轨迹为证）⇒ 两臂都"有能力"，比较无效。
- ★ **错误二（我的）**：臂自证工具名写错（我按包内名 `design_canvas_index` 写，模型实际看到的是 `mcp__design-canvas__*`）
  ⇒ A 臂被判"自证不过"是**假红**。判据要按**模型实际看到的工具面**写，并支持**前缀匹配**。
- ★★ **抓到「regression 间歇性红」的真因**（此前两次"红了不复现"）：留档的尾部显示 4 个门**崩溃**在
  **宿主注入的 `node-safe-delete` 钩子**里（`tryTrash … node-safe-delete-shim.cjs`）——
  门的自证会删临时文件，钩子在"删不掉/不存在"时 **fail-closed 直接把脚本崩掉** ⇒ `check:all` 红。
  已做：判据子进程加 `CODEBUDDY_SAFE_DELETE_ENABLED=0`；含该 shim 的崩溃标 `regressionInfra`（基建假红，如实打印）。
  **建议归「工具完善」**：门的自证删临时文件前先判存在。
- 顺带：`registry.json` 在 **18:27:36**（实验窗口内）被改 + 有 15:50 的 `.tmp` 残留 ⇒ 能力库**共享可变且非崩溃安全**
  ⇒ 我的"能力库快照/还原"要先做。
- 已**把实验栈切回 `web`**（gen-3094）✓。下一步：`make-profile-variant --drop-insert mcp-client`（真正的"能力关"）
  + 臂自证前缀匹配 ⇒ 重跑。

## 18:35 ★ 角色定案：**进化脑 = 发现问题（scout）／生产脑 = 解决问题／专家团 = 审核+评价**

用户原话：
> "其实**进化脑就不再负责审批了**。审批还是**专家团队**的事情。**生产脑自己负责自己的审批**
> （查验自己的任务是否符合标准）。**进化脑则是去把 skill 去市面上选型、以及把 skill 和现有的 Agent
> 进行对比，看看有没有什么需要进化的地方，然后提交工单给专家团**。
> **生产脑专门负责解决问题，进化脑专门负责发现问题，评审团专门去审核他们的问题，
> 以及给出更有角度的、更有力度的观点。**"

### ★ 我认错：我先前把"进化"写成"判定 + 审批"，是错的

用户明确：**进化脑只负责"发现问题"（提工单），审批归专家团。** 已在文档里显式标注旧写法是错的。

| 角色 | 形状 | **动词** | 产出物 |
|---|---|---|---|
| **进化脑**（= scout） | 独立子 Agent | **发现问题**（选型 + 对比） | **工单** |
| **生产脑** | 独立子 Agent | **解决问题**（+ 自审） | **diff / 新能力** |
| **专家团** | 顶层（多模型） | **审核 + 评价** | **采纳／驳回 + 更有力度的观点** |
| （隔离被测） | **单独的工具** | 跑隔离沙箱 | 隔离环境 |

**★ 三个动词不重叠**（发现/解决/审核）、**产出物不同**（工单/diff/裁决）
⇒ 每个角色的"交付物"**可被另一个角色独立检查** —— 这是这套分工最干净的地方。

### ★★ 两个"现成设计"一字不差地对上了（不是新发明）

| 用户说的 | 文档里的现成设计 |
|---|---|
| 进化脑去**选型 skill / 对比 / 提工单** | `design §11` 的**侦察（scout）**：「把自进化从**自我修改**变成**能力组合** —— 瓶颈从"造不造得出来"变成"**发不发现得了**"，并天然引入新角色 **侦察（scout）**（产出候选：接口 + acceptance + 来源，**不产代码**，**必须过注册门**）」 |
| 那个「**工单**」 | `master-plan §7 假设引擎`：给论文加 `proposes: {path, change, expectedGain, **falsifier**}` = **带证伪条件的改动提案**（"可证伪才算科学"） |

⇒ **进化脑 = scout，工单 = `proposes{falsifier}` 提案。** 两者 2026-09-14 就设计过，
**只是从没有名字把它们和"三脑"对上** —— 现在对上了。

### ★ 我补的一条精确性（写进文档）

**生产脑"自审"的标准必须来自专家团**（提任务时给的 `acceptance`），**不是自己拟的** ——
否则退化成"自定自判"。⇒ 与 `master-plan §11.2` 完全一致：
「**自动晋升的判据不是"谁提的课题"，而是"验收标准是否已被独立批准"**」。

### ★ 新增一个待定问题（§5.9）

**进化脑的"市面"从哪来？** 候选池是：我们自己的能力库 / `SkillNode` 技能树（未移植）/
外部 skill 市场 / 别的项目的 agent？**这决定进化脑的能力边界**，也决定它能不能真的"发现得了"
（`design §11` 的战略意义就在此：瓶颈从"造不造得出来"变成"发不发现得了"）。

### 落纸

`docs/revised-architecture-2026-09-20.md`：§2 下层改为**三角色**（含动词/产出物表）+ 顶层循环不变；
§5.1 合并用户三轮原话 + 角色形状表 + **显式标注旧写法是错的**；§5.2 两个新 provider（发现席/解决席）；
§5.3 加"自审标准来自专家团"；§5.4 改为**三角色骨架盘点**（并指出**进化脑这一角色基本是空的**）；
§5.9 新增"市面从哪来"；§6 工具完善那条改为"两级判定都要它的口径"。
**顺手修掉一个编号断档**（4 之后直接跳 6）。check:all 15/15。

## 18:5x 回答用户的两问（B 的 token 为何更少 / 为何撞上限）—— 按"每次调用的真实耗时"实测

- 写了个一次性脚本按 `tool/call`→`tool/result` 的 `time` 配对算每次调用耗时，结论**推翻了两个直觉猜测**：
  · **A 臂 16 次调用总共只花 2.2 秒**（最慢 0.5s，全是 MCP 调用）；
  · **B 臂 23 次调用总 1037 秒，其中一次 shell 调用独占 1033.5 秒（99.6%）**：
    它带 `sandbox_permissions: danger-full-access` 在**等审批**，审批被取消 ⇒ 卡死到超预算；
  · `rename_symbols` 两次共 **0.3 秒**（索引是热的）⇒ **"预解析/索引没做好"不成立**。
- ★ **真因是我的题缺陷**：oracle 当时用 `spawnSync` 起子进程 + **管道 stdio**，
  而 `workspace-write` 沙箱会以 **EPERM** 拦管道 stdio ⇒ **agent 自己跑不动 oracle** ⇒ 只能申请提权 ⇒ 卡审批。
  **已修**：自检抽成 `selftestLines()`、oracle 改 **in-process** 调用（spawnSync 计数 0），两方向复验通过。
- **B 的 token 少不可解读**：撞上限 ⇒ token 是 **censored**；且 1033s 在等审批不在生成。
  用户的两个假设（重型流水线 vs 基础能力够用）要验，必须让 B 也 **settled** 再比。
- 指标新增（`extractMetrics`）：`callsTotalMs` / `slowestCalls` / `approvalWaitHits`
  ⇒ 复算那次会话：`最慢3次=[{shell,1033543ms},…]`、`等审批痕迹=1` —— 一眼看出"一次调用吃掉 99.6%"。

## 19:41 ★ 进化脑的"市面"有了答案 + 融合判据 = 项目判据公式的实例

用户回答 §5.9 并给出完整流水线。原话（节选）：
> "进化脑这一部分，它最开始是一棵 **skill 树**……**任何 Agent 开源社区里的 skill 都可以拿来使用**。
> 你只需要**使用工具去对每一个 skill 进行判分** —— 如果是**纯指导型的 skill 判低一点**。
> 然后把这些东西和**现有的子 agent（skill 树里的相似节点）比较**，然后**两个两个融合、然后优化**。
> **融合的判据就是：无回归；然后能够拓展新功能节点，或是更有效、错误率更低、消耗更少等方向的进化。**
> 其实到这里还就是一个 **skill 的进化**，我们只是把它**做成更规范的子 Agent** 而已。"

### ★★ 融合判据 = `design §1` 字典序公式的**逐条实例**

| 用户说的 | 既有判据 | 挡位 |
|---|---|---|
| **无回归** | **L2 基线不退化**（逐维度 fail-closed） | **硬门** |
| **能拓展新功能节点** | **能力面** | **硬门** |
| **更有效 / 错误率更低 / 消耗更少** | **效率面**（工具错误率 / 耗时 / token） | **软门·只排序** |

⇒ 正是「**先过能力门，再在"已经做到"的候选之间比效率**」。
而 `design §1` 给的理由是："加权和里永远存在'省 token 但任务做不成'的净收益为正的坡道
—— **那正是 reward hacking 的数学根源**"。**用户的话与该公式同构。**

★ "纯指导型判低分"也不是偏见：`design §13` 已写「**纯 prompt 技巧是最脆弱的资产，
会被模型升级本身淘汰**」⇒ 判低分是在**预测它会被淘汰**。

### ★ 流水线字段现有率极高 —— 又一次印证 §13 那句话

| 步骤 | 现成 | 缺 |
|---|---|---|
| ② 判分 | `Score`/`UseCount`/`SuccessRate` | 判分工具（现只有 `tool_score` 自评分） |
| ③ 相似比较 | `graph_cache`/`rrf` | 移植 |
| ④ 融合 | **`Absorb`/`AbsorbOutcome`** + `Exclusive` | 融合执行器 |
| ⑤ 判据 | 两级判定 + L0–L4 | **L2~L4**（= 工具完善议题） |
| ⑥ 产出 | `Extends`/`Requires` + `EditHistory`/`MergedFrom`/`AbsorbedBy` | 升格路径 |
| 淘汰 | `sleep_prune` + `Status` | 移植 |

⇒ 印证 `design §13`：「**能力库不需要设计，`SkillTree` 已经是了；它唯一缺的是执行方式**」。

### ⚠️ 我主动提的安全问题（写进 §7.4）

「任何开源社区的 skill 都可以拿来用」⇒ 它同时是**代码**（带 `Script`/`Tools`）⇒ **不可信输入**。
必须走 `design §11`「外部 Agent 接入四个硬问题」同一套门（acceptance 由我们定 / 凭据边界 /
可靠性上报 / 版本追责），**外加 L1 安全门 + L3 红队用例**（master-plan §10 的 P5 就是它的验收）。
现实参照：**2026 年 LiteLLM 出过 PyPI 供应链事件**（夹带窃取云凭据代码）⇒ 这类风险已发生过。

### ★★ 工具层踩坑（第二次）：「命令可能被执行两次」

`cat >> file <<EOF` 把 §7 **追加了两遍**（沙箱被拒 → 提权重跑）⇒ 文档出现重复章节。
**这已是第二次**（上次是 MEMORY.md 指针被"提前"加好，让我误判）。
⇒ 已固化进 `MEMORY.md` 环境约束 #15：**追加/写入类操作必须当成可能跑两次来设计，
写完立刻校验（`grep -c` 关键标记 / 查重复标题），不凭脚本输出下结论。**
本次靠 `grep -n "^## "` 发现重复并截断修复（17849 → 14867 字符）。

check:all 15/15；§1–§7 结构干净；§7 只出现一次。

## 19:57 ★★ 订正 §7：**融合是开发脑的活，进化脑只提 PR**（我又写错了一处）

用户原话：
> "有没有可能**我不是让你直接拿 skill 出来当子 agent 用**？我们首先要通过**进化脑去提 PR**。
> 然后审核就是**评审团去评审**，然后**发给开发脑**，然后**开发脑去把它进行完整的开发**，
> 就是说**把里面提到的工具全都打包好、并且融合好、测试好**。然后**再交回、回传**。
> 但具体的机制怎么评审……反正**重点在于后续这个会根据数据重新来过**。
> 重点就是在于：这三个【角色】**加上 Skill 树**，然后**不停地增长新的子 agent**……
> **不同的能力面就做不同的职业准则，同样的能力面去合并，然后相似的能力面也融合，看看能不能更好用。**"

### 我错在哪

§7 初版我把"判分 → 相似比较 → **两两融合** → 产子 agent"整条放在**进化脑**名下，
**好像进化脑自己会融合、会产子 agent**。**错。** 进化脑**只提 PR**；
**融合/打包/测试是开发脑的活**。（其实用户上一轮就说了"提交工单给专家团"，是我自己把融合塞错了位置。）

### 订正后的三段流水线

```
① 进化脑【发现 + 提 PR】   —— 选型 skill / 对比现有能力 / 发现进化点；只提案，不产代码、不融合
② 评审团【评审 PR】        —— 多模型讨论；审"这个 PR 值不值得做"
③ 开发脑【完整开发】★      —— 把 PR 提到的工具**全都打包好 + 融合好 + 测试好**
④ 【回传】                 —— 交付回能力库
⑤ 【根据数据重新来过】★    —— 整个循环是**数据驱动迭代**
```

★ **别名**：用户口语里 **"开发脑" = "生产脑"**（同一角色）；**"评审团" = "专家团"**。已写进 §5.1。

### ★★ 核心机制：围绕「能力面」的三种操作（这是最实质的一句）

| 能力面关系 | 操作 | 产出 |
|---|---|---|
| **不同**（不重叠） | **新建子 agent** | 各自**自己的职业准则**（=`SubagentProvider` 的 `persona` / 职责边界） |
| **相同**（重合） | **合并** | 一个（`SkillNode.Absorb`/`AbsorbOutcome` 是现成字段） |
| **相似**（部分重叠） | **融合** | 试融合后**看能不能更好用**（判据 = §7.2：无回归 + 新功能节点 + 效率） |

⇒ **Skill 树 = 这套操作的对象与账本**；新子 agent 不断长出来，能力面决定"新建/合并/融合"。

### 落纸

`docs/revised-architecture-2026-09-20.md` §7 整节重写（含 §7.0 显式标注初版错在哪 + §7.1 三种操作表
+ §7.3 资产对照表加了"归属"列：判分/相似比较归进化脑，**合并/融合归开发脑**）；
§5.1 加别名说明（开发脑=生产脑 / 评审团=专家团）与 §7 指针。

### 工具层：又一次"命令跑两遍"，这次**幂等设计救了我**

替换 §7 的脚本被跑了两次：第一次替换成功；第二次因标记不存在而**报错退出**（而非重复追加）。
⇒ 正是我刚固化进 `MEMORY.md` #15 的"写入类操作必须当成可能跑两次来设计"在起作用。
复核确认：§1–§7 无重复章号、代码块配对 ✓、check:all 15/15。

## 19:4x–20:0x 把"能力关"臂**真**做出来（两层证明）+ 能力库快照工具 + 重跑

- ★ 根因：能力有**两条**进工具的路径 —— bundle（`@dsh-brain/design-canvas-bridge`）+ profile `cordis.patch.yml` 里的
  `- insert: id: mcp-client`（指向外部 MCP server）。只 drop bundle 关不掉。⇒
  `make-profile-variant.mjs` 新增 **`--drop-insert <loader-id>`**（行级删 `- insert:` 块，含"块内多条目只删一条"的处理与自证）。
- **配置层证明**（`dsh --dump-config`）：`web` = 582 行/143 id；`web-nodc` = 555 行/141 id；
  **差异恰好是 `design-canvas-bridge` + `mcp-client`**，其余一致 ✓（也确认 `check:profile` 的 582 行基线没被动）。
- ★ **工具面证明**（真的发一句"只回一个字：好"，再读 `request/header.tools`）：
  · `web-nodc` → 工具面 **30** 个、design-canvas 家族 **0**、`mcp__*` **0**；
  · `web` → 工具面 **102** 个、design-canvas 家族 **67**、`mcp__*` 64。⇒ 臂**真**分离了（这次不是"我以为关了"）。
- 新增 `scripts/capability-snapshot.mjs`（`--save/--restore[--force]/--diff/--show`）：sha256 + **拒绝覆盖并发写者** +
  force 给实验台。顺带确认 `registry.json` 的 `updatedAt` 就在我实验窗口内（19:58）⇒ **共享可变状态**是真的。
- `eval-run` 的 `--expectTool/--forbidTool` 支持**前缀家族匹配**（`mcp__design-canvas__*`）——上次假红的根因是
  我按包内注册名写期望，而模型看到的是 MCP 名字。
- 已启动重跑（后台 APhosy）：`--profileA web --profileB web-nodc` +
  `--expectTool 'A:mcp__design-canvas__*' --forbidTool 'B:mcp__design-canvas__*'`（臂自证这次是**按模型实际看到的**写的）。

## 20:0x 用户提"题目的空间没分开？" —— 具体机制已排除，但共享面清单值得常备

- **用户假设**：会不会第二臂做的是第一臂已经做过的题（空间没分开）⇒ 所以它很快。
- **排除的证据（两条独立）**：① 能力题（空 seed）跑前**必须**验"oracle 现在是红的"（harness 打印 `✓ 有信号`）
  ⇒ 第二臂不是从"已完成"的工作区出发；② 第一臂收尾是 `git checkout 3 个文件` + `还原后 git status 干净 ✓`。
- **但共享面确实不止工作区**（用户的直觉指向的是对的）：别的会话日志 `~/.dsh/sessions/**`（另一臂的完整推理）、
  我的报告 `out/eval-*.json`（另一臂的轨迹）、能力库 registry、**design-canvas 索引/缓存**（冷热跨臂继承 ⇒ 性能混淆）、
  switchboard 运行态、构建产物（lib junction）。
- 新增 `scripts/eval-isolation-audit.mjs`（只读审计：数工具参数是否碰到这些面）+ `--pair` 跑完**自动**跑并写进报告。
  对上一对复跑：两臂 **0 处触碰**（脚本自带免责声明：**没触碰到 ≠ 没泄漏**）。
- 待办（更强的隔离）：每臂独立会话/报告目录；把另一臂产物移出 agent 够得到的位置；或每臂独立 worktree；
  以及把 `.design-canvas` 的冷热状态按臂记账（现在会被下一臂继承）。

## 20:15 收尾 §5 + 起第三条线交接文档（架构线）

用户"可以"（同意收尾 + 起架构线交接文档），并问"下一步是什么"。

### ① §5 收尾

- **第 8 条标 ✅**：「合并/融合有没有劣化」的判据 = **无回归 + 拓展新功能节点 + 更有效/错误率更低/消耗更少**
  （= §7.2 那三条；**归开发脑执行、评审团裁**）。映射：无回归=L2 基线不退化（硬门）／
  新功能节点=能力面（硬门）／效率=效率面（软门·只排序）⇒ `design §1` 的**字典序，不是加权**。
- **加了状态摘要行**：✅ **已定 4 条**（1 三角色/3 两级判定/8 融合判据/9 市面来源）／
  **⬜ 待拍 3 条**（5 评审团成员配置、6 gateway 选型、7 skill→subagent 谁转化）；
  第 2、4 条是**说明性**（落地机制 / 资产盘点），不是待定项。
  （★ 我先前口头说"待拍 5 条"是**数错了** —— 把两条说明性条目算进去了。）

### ② 新建 `topics/architecture-handover.md`（第三条线，自包含）

- **§0 一句话**：把三角色落成可运行的 → **发现席（进化脑）/ 解决席（开发脑）两个 provider + 顶层评审团编排**，
  跑通「提 PR → 评审 → 完整开发（打包/融合/测试）→ 回传 → 按数据迭代」
- **§1 逐格盘点**：**进化脑这一格基本是空的**；开发脑/评审团各有一半
- **§2 ★ 依赖关系 + 一个看似矛盾的顺序问题（已想清）**：
  - `master-plan §10`「**先判据后能力**」（全局节奏）vs `design §13`「**数据先行**」（技能树线内部顺序）
  - ⇒ **不冲突**：**全局补齐 L2~L4 的同时，开始移植 Skill 树数据模型**，两者并行互不阻塞
  - ⇒ 架构线两条腿：**数据层现在就能走**；**执行层骨架能起，判据部分等工具完善线**
- **§3 起点**（读什么/照抄什么/建议顺序/**四条"不要做"**：不做成同一 agent 的两个工具、不给发现席生产写权、
  不自拟标准自审、不重造能力库）
- **§4 待定 3 条**（指向 §5）
- **§5 验收最小硬门**（含：**判据未实施就标 `unenforced`，不许把未做的级标成通过**）
- **§6 回执位**

### 三条线全景（都在 MEMORY.md 主题表与交接节里有指针）

| 线 | 文档 | 状态 |
|---|---|---|
| 靶场 M1 | `next-task-handover.md` §3.B | 另一会话在做 |
| 工具完善 L2~L4 | `tool-refinement-handover.md` | 已派 |
| **架构线** | `architecture-handover.md` | ⬜ **刚起，待派** |

复核：三份交接文档章节均 7 个、无重复章号；MEMORY.md 10557 字符；check:all 15/15。

## 20:1x 用户要求"先3再1"，并追问"1 会不会留痕迹被第二臂看见"

- 做完 **3**：`eval-run` 每臂跑前/跑后记索引冷热（`indexBefore/indexAfter/indexDelta`）。
  实测现状：`.design-canvas` = **53 文件 / 43MB / 最新 18:13** ⇒ **热**；正在跑的那对（20:02 启动）**两条臂都继承了它**
  ⇒ 正是"跨臂性能混淆"，现在看得见了。
- 用户追问（**问得对**）：把产物挪走（第 1 项）**不等于**没痕迹 ⇒ 我加了 `traceInventory()`（跑前记录并打印）：
  ① **会话目录里别的会话的完整转录**（最危险）② 我的报告 `out/eval-*.json`（含另一臂轨迹）③ 索引冷热与 mtime
  ④ **git log/reflog 与 commit message**（我写的信息本身就在泄露"我在做什么"）⑤ 构建产物（lib junction）
  ⑥ switchboard state.jsonl/gen 日志 ⑦ 文件 mtime。
- 结论：**痕迹藏不住，只能"消掉信息"或"真隔离"**。两条真解：
  **(A) 实例随机化**（同一任务族、每条臂一个**等价实例**，随机符号名/文件 ⇒ 推断再准也没答案可抄；仍可比）；
  **(B) 真隔离**（每臂独立 HOME/容器 + git worktree ⇒ 痕迹物理不可见；这正是 Docker 那条线的价值）。
  建议：先 (A)，(B) 与 Docker/公开靶场一起做。

## 20:19 加入「多会话共用仓库」的提交纪律（环境约束 #16）

用户同意加。**写成有分寸的版本，不一禁了之**：

- 现象一（实证）：**别的会话漏加了它自己刚建的文件** —— 提交 `56e04f5` 的 `--stat` 里没有
  `scripts/eval-run.mjs`，是后来被 `git add -A` 才收进来的 ⇒ **差点丢一个刚建好的脚本**
- 现象二（实证）：我的 `git add -A` 把**别人正在写的半成品**一起提交了
  （`docs/eval-capability-task.md`、`evals/pilot/*`）
⇒ **提交前先 `git status`**，看清哪些不是自己的；**优先 `git add <自己的路径>`**；
只在确认"其余都是别人的干净改动"时才用 `-A`。
⇒ ★ **但不能把 `-A` 一禁了之** —— 现象一恰恰说明它同时是**兜底**。
**两条都要**：精确加为主 + **周期性查"有没有该提交却没跟踪的文件"**。

本次已按新纪律执行：`git status` 检查 → 只有 MEMORY.md 一个改动 → 精确 `git add` 该路径。

**MEMORY.md 体量 11582 字符**（早前被截断时约 16k）⇒ 仍在安全区，但**在增长，要盯着**。

## 20:02–20:18 重跑（臂真分离）结果：**A 臂无效（我的时序错）+ B 臂用 30 个工具把题做出来了**

- **A 臂（web）无效**：工具面只有 **4 个**（应 102）⇒ 只能调 `capability_report`/`list_capabilities`、13 秒就停、oracle 红。
  真因：**换代刚 flip 完、插件还在注册工具我就开跑了**（`verify-boot-health` 只保证"没有被拒的插件"，
  **不保证工具已注册完**）。⇒ **臂自证正确地**把它判成"不能算作该臂 ✓" —— 机器没错，是我的时序错了。
- **B 臂（web-nodc）**：用 **30 个工具**（零 design-canvas / 零 mcp__）**把题做出来了（oracle 绿 ✓）**，
  `regression` 也 **绿 ✓**；但 **over-budget 901s**。
- ★ 关键读数：B 的**工具调用总耗时只有 3.5 秒**（最慢 0.66s）⇒ **901 秒不在工具上，在"模型等待/生成"**
  （11 步 ≈ 82s/步；A 那次 2 步 13s）⇒ **本次 wallMs 不可比**（两臂落在不同时间窗，provider 延迟差异大）。
- ★ 两个正面收获：① `regression` 两条臂都绿 ⇒ 今天定位的 **safe-delete 假红修对了**；
  ② 臂自证/隔离审计真的在拦错。
- 已修：新增 **就绪闸 `waitArmReady`**（真读一次工具面；按 profile 的**家族要求**判，web 必须看得见
  `mcp__design-canvas__*`）+ 报告记 `readyGate`/`profileToolMust`（干跑已验证）。
- B1 的补丁（`ensureWorktree` + 每臂 `cwd` + 判据在 worktree 里跑）**已写好未应用**（`out/_patch-worktree.mjs`）。

## 20:38 用户批准 → **B1 应用并验证通过**，随后启动"工作区隔离 + 臂真分离"的第一对

- 接线：`--worktreeA/B` → `ensureWorktree()`（`git worktree add --detach HEAD` + `node_modules` junction）
  → **该臂 session 用 `--cwd <worktree>` 建** → seed/oracle/regression 与工作区快照都在该 worktree 里跑。
- ★ **干跑实测通过**（这次前置全绿）：
  ```
  [工作区] A: D:\project_develop\_wt\cli0005-A（新建=true node_modules=true）
  [工作区] B: D:\project_develop\_wt\cli0005-B（新建=true node_modules=true）
  [臂切换] 现役 gen-3098 指纹已符合 web ⇒ 幂等跳过
  [就绪闸] A 臂（web）✓ 工具面 102 个      ← 按家族验证（web 必须有 mcp__design-canvas__*）
  [臂切换] → web-nodc → gen-3099 指纹 designCanvasBridge:0 ✓
  [就绪闸] B 臂（web-nodc）✓ 工具面 30 个
  ✓ 两臂同模型（deepseek-official/deepseek-v4-flash）
  ```
  并回读确认两臂会话 cwd 分别是两个 worktree ✓；`git worktree list` 显示两份检出同在一个 commit ✓。
- **`session-create.mjs` 本来就支持 `--cwd`**（主干已加）⇒ "目录选择器"确实是一句话的事。
- 已启动第一对（后台 **ZnqyXw**）：worktreeA/B + profileA=web / profileB=web-nodc +
  按臂家族自证（A 必须有 `mcp__design-canvas__*`、B 必须没有）。跑前存了能力库快照 ✓。
- 仍未做：**B2**（独立 HOME/容器）——它才关得掉"别的会话转录/能力库/索引"这三条共享面。

## 20:40–20:55 那对又无效（两个独立原因）→ 修 4 处；残留阻塞：**换代坏了**

- **原因一（我的事故）**：靶子被**误提交成"已解"**——我做 oracle 两方向自证时把"正确解法"写进靶子文件，
  另一个会话在同一窗口提交整个工作区（共享 index 第三次）⇒ HEAD 里是改名后的版本 ⇒ cli-0005 永远绿。
  ★ 本轮实验**正确地**报了「题目没有信号 ⇒ 弃跑」，**没有产生假数据**。已修回未改名（`4aded58`）+ 复核 ✓。
- **原因二（环境）**：`?cmd=handover&profile=web-nodc` 连续 **3 次 `b-not-ready`** ⇒ B 臂实际跑在 `web`（102 工具）。
  boot.log 给出错因：**gen-3101 撞上保留端口 3101**（key-pool-proxy，现役 gen 正占着；`netstat` 证实 3100/3101
  都属同一 pid）+ gen-3102 `MODULE_NOT_FOUND`。⇒ 归口 **switchboard（P2，只报不修）**，
  报告 `docs/switchboard-handover-bnotready-2026-09-20.md`（含建议：端口避开保留集合 + 把错因带进 `result.note`）。
- **已修三个 harness bug**：① pair 审计块引用 `out` 早于声明 ⇒ TDZ 崩溃（报告没写出来）；
  ② `ensureProfile` 指纹不符只警告不失败 ⇒ 上次把 A 臂当 B 臂、读数全废 ⇒ 改**硬失败**；
  ③ 就绪闸缺"缺失型"判据（`web-nodc` 的 must 为空 ⇒ 102 个工具也判"就绪"）⇒ 加 **forbid 家族**。
- **B1 已验证通过**（干跑：两个 worktree + 每臂 `--cwd` + 就绪闸 102/30 + 同模型 ✓）；任务集有效 ✓。
  ⇒ **只要换代恢复（或走人工重启绕法），这对实验立刻可跑**，且前置比之前任何一次都干净。
- 绕法：由用户终端 `WEB_PROFILE=web-nodc node scripts/relaunch-switchboard.mjs` 起 B 臂，跑完再起回 `web`。

## 20:35–21:00 ★ 用我自己的子代理派活（用户建议）+ 撞上一个依赖链接故障

用户建议：「你没有自己的**子代理**吗？可以安排 3 个子代理……**你自己去创建去管理会比我手动更科学**，
而且**回执也不用我去读**。」

### 采纳：派了架构线的数据层（由我的子代理 `skill-tree-port` 执行）

**为什么只派 1 个而不是 3 个**：靶场线在另一会话手上、工具完善线用户已派 ⇒ **只有架构线没主**。
重复派 = 两个 agent 干同一件事 + 互相踩。

**前置已核实**：技能树源码在 `D:\project_develop\ai-base\agent-shell\internal\memory\`
（`skill_tree.go` 36.6KB / `sqlite_store.go` 48.2KB / `skill_import.go` 19.2KB / `heat.go` / `gravity_field.go` / `sleep_prune.go`）。

**子代理产出**（已提交 `987b7d1`）：
- `docs/skill-tree-port-plan.md`：6 文件 + 5 外溢依赖的逐项映射（行号）、§13 三分归类、
  5 层依赖顺序、14 条 Go 特有边角
- `packages/skill-tree/`：新包骨架，**SkillNode 33 字段与 Go 逐一对齐（双向核对 0 缺 0 多）**、tsc 通过
- 三个最难移：sqlite-vec / `sync.RWMutex` / `skill_import.go`（导入即不可信输入）
- ★ 未料到：`sanitizeID` 藏在**不移植**的 `builder.go` 里却被依赖；`sleep_prune` 依赖的
  `graph_cache/graph_writer` **不在本次 6 文件内**

### ★ 但撞上一个环境故障：仓库的两条依赖链接消失了

**实测（20:19 → 20:43 之间变坏）**：
```
共享树 ~/.dsh/profiles/node_modules/@deepseek-ai  → 232 个包，完好 ✓
仓库   node_modules/@deepseek-ai                  → ★ ENOENT
仓库   node_modules/@dsh-brain                    → ★ ENOENT
```
⇒ **不是树没了，是「指向它的链接」没了**（`.gitignore:37-38` 自己写着"junction-link 到 profile node_modules"）。
⇒ `check-all` **15 门里 8 门红**，全是 `ERR_MODULE_NOT_FOUND` ⇒ **三条线的门全部跑不了**。

**排除项**：① 不是我的子代理（它没跑 npm；npm 日志最新是 09-15）；② 时间窗 20:19–20:43
正是**靶场会话应用 B1**（`git worktree add` + `node_modules` **junction**）的窗口。

**新增 `scripts/relink-node-modules.mjs`**（默认干跑 + 幂等 + 建完真解一次依赖验证）。
干跑结论：**只差 1 条链接**（`@dsh-brain` 的目标 `~/.dsh/profiles/node_modules/@dsh-brain` 不存在
⇒ 跳过，不建悬空链接）。**未执行 `--apply`，等用户确认。**

### ★★ 修正我上一轮的一个错误

我上一轮说"`conveyor-context` 还在，是子代理误判" —— **错了**。`git status` 显示：
```
 D packages/conveyor-context/{cordis.patch.yml,package.json,src/index.ts,src/spec.ts,tsconfig.json}
```
⇒ **它的 5 个文件在工作区里被删了（未提交）**，我只是看目录还在就以为它在。
**⇒ 子代理没误判，是我判错了。**（教训：**"目录在" ≠ "文件在"**，要 `git status` 或列文件。）

### ★★ 纪律 #16 当场起作用

工作区里那 5 个 `D` 是**别人未提交的删除**。若按老习惯 `git add -A`，
就会把"删除 conveyor-context"一起提交进我的 commit ⇒ **正是 #16 要防的场景**。
本次只 `git add` 我自己的 5 个文件。

⚠️ **本次提交未经门验收**（依赖链接未修，`check-all` 跑不了）—— 已写在 commit message 里。

## 21:00 ★★ 我误诊了，而且把情况弄得更坏了一次（已撤销）

用户批准执行 relink。**结果证明我的诊断方向是反的。**

### 真实结构（与我推断相反）

```
仓库 node_modules/@deepseek-ai/*            = 【源】真目录   ← ★ 被删的是这个
~/.dsh/profiles/node_modules/@deepseek-ai/* = 【指向仓库的链接】232 条，现全部悬空
```

**共享树全量统计：链接 232 个 / 真目录 0 个** —— 一条命令就能看出来，**而我当时没查**。

### 我做了什么、错在哪

1. 我建了 `仓库/node_modules/@deepseek-ai → ~/.dsh/profiles/node_modules/@deepseek-ai`
   ⇒ 因为共享树又指回仓库 ⇒ **自指循环** ⇒ **把"缺失"变成了"循环"，比原来更坏**
2. 我的验证也错了：用 CJS `require.resolve` 去解 **ESM-only** 的 `@deepseek-ai/cordis` ⇒ 假红
3. **已撤销**（`fs.unlinkSync` 只删链接、未触碰目标）⇒ `@deepseek-ai` 回到 ENOENT（= 我动手前的状态）
4. `@dsh-brain → ~/.dsh/profiles/web/node_modules/@dsh-brain` **保留**（方向正确：仓库→profile，
   且实测 `@dsh-brain/switchboard` 可解析）

### ★ 教训（已写进新脚本头部，也写进这里）

> **在"改共享环境"之前，先把「原来的结构是什么样」验证清楚。**
> 我只看了 `.gitignore` 的一句注释 + "树里有 232 个包"，就推断了方向；
> **没有去验证那 232 个条目到底是【真目录】还是【链接】** —— 而那一步就能戳破我的推断。
> **一步就能验证的事，别跳过。**

### 处置

- **删掉** `scripts/relink-node-modules.mjs`（前提是反的，留着害人）
- **换成** `scripts/check-node-modules-links.mjs`（**只读**）：输出真实结构 + 方向判定
  （全量统计"链接 vs 真目录"）+ 恢复路径建议
- **恢复路径（未执行）**：a) 按 lock 重装（`package-lock.json` 有 204 条 `@deepseek-ai/`）⇒ `npm install`；
  ⚠️ 会跑 postinstall，那 5 个 patch 脚本会动 `~/.dsh/profiles/web/`（**属 P2 安全层**）；
  b) 或从备份/另一份完整检出拷回 `node_modules/@deepseek-ai/`
- **不擅自执行**：① 会动 P2；② 多会话正在用这个仓库（共享 index）

### 这次唯一的好消息

**我自己的错误被我自己兜住了**：建错 → 验证（假红）→ **不信假红去查真因** → 发现方向反了 → 撤销。
**没有留残骸。** 这正好是项目那条纪律的实践：「**见假红 → 先证明判据错了，再改判据**」——
我没有为了消红去改被检对象，而是去查"为什么验证不过"。


## 21:0x–21:35 派子代理修「换代端口撞号」——已修并核实；但被另一个会话的删除挡住了重启

- **子代理交付**（commit `c7ba4d4`，3 个路径）：`packages/switchboard/src/coordinator.ts` 新增
  **`RESERVED_GEN_PORTS = [3101]`**（注释写明归属 key-pool-proxy）+ 导出 **`allocGenPort(portBase, slot)`**
  （★ 关键设计：不是"撞上就 +1"——那会让"跳过 3101 落到 3102"与下一个 slot 撞车；
  现在是**整体后推**，保证单射：实测 `3099, 3100, **3102**, 3103…`，3101 永不出现）；
  `abort` 把 staging 代 boot.log 错因摘要（`bootErrorHint`，截断 240 字符）带进 `result.note`；
  新增 `scripts/test-gen-port-alloc.mjs`。
- **我核实过**：`node scripts/test-gen-port-alloc.mjs` → **16 passed / 0 failed** ✓；
  源码里三处标记都在 ✓；构建成功 `b1789910762643` ✓。
- ⚠️ **它如实交代的短板**：`check-all` 是 **12/3**（不是 15/0）——3 项失败**全由另一个会话删掉
  `packages/conveyor-context` 引起**（`cannot resolve profile bundle`），与它的改动无关 ✓；
  **真机 handover 验证做不到**（活动控制面是旧构建 + 规矩禁止杀宿主）✓。
- ⚠️⚠️ **阻塞重启的并发写者**：`packages/conveyor-context` 的 **5 个文件删除仍在工作区未提交**，
  而 `profiles/web` 的 bundles **引用它** ⇒ **现在重启控制面很可能起不来**。
  ⇒ 我不擅自动别人的在编工作；已把这条高亮给用户。
- 另：子代理**改了共享 `node_modules/@deepseek-ai`**（开工时整体缺失，它按 lock 的 integrity 离线从
  npm 缓存补齐 ~200 个包，**没跑 npm install**）——如实记录，必要时可删掉重装。
- 待办（用户重启后）：`?cmd=handover&profile=web-nodc` 应 success、新代应为 **gen-3102**（3101 被跳）、
  boot.log 无 EADDRINUSE；再切回 `web` 也要 success；然后跑那对能力实验。

## 21:40 用户澄清：conveyor-context 是**有意删除**（不是误删）⇒ 我改用 profile 变体，不动 P2

- 后果（实测）：`dsh --profile web --dump-config` **exit=1** ——
  `cannot resolve profile bundle "@dsh-brain/conveyor-context"` ⇒ **任何重启都会失败**，
  而重启是让"端口跳号修复"生效的前提 ⇒ 必须先解决"profile 引用了一个已删除的包"。
- 解法（**不恢复包、不改 P2 的 profiles/web**）：造两个变体
  · `exp-base` = web − conveyor-context ⇒ dump-config **exit 0 / 575 行** ✓
  · `exp-base-nodc` = exp-base − design-canvas（bundle + mcp-client）⇒ **exit 0 / 548 行** ✓
  ⇒ 实验跑在 A=`exp-base` / B=`exp-base-nodc`；等用户那条线的**新版 conveyor** 落地，丢掉 exp-base 即可。
  并把这两个名字加进 `eval-run` 的三张映射表（指纹/工具面 must/forbid）✓。
- 待用户重启（`set WEB_PROFILE=exp-base` + `node scripts\relaunch-switchboard.mjs`）后我立刻：
  ① 验端口修复（handover→exp-base-nodc 应 success、新代应为 gen-3102、boot.log 无 EADDRINUSE、再切回 exp-base 也 success）；
  ② 跑那对能力实验；③ 接 verifyCmd。

## 23:10–23:50 三线合并 + 用子代理派活 + 清掉我自己造的垃圾

用户：「动仓库的那个会话写了回执给你，你看一眼。然后**我干脆直接合并吧，让你就你这一个会话去做**，
**你全都用子代理的形式自己去派任务**。」

### ① 读了回执 `docs/receipt-to-main-2026-09-20.md`（它回答了我几个悬案）

- **`@deepseek-ai` 是谁修的**：他们的子代理**按 lock 的 integrity 离线从 npm 缓存补齐 ~200 个包**，
  **没跑 `npm install`** ⇒ **没动 P2**（比我那条错路干净）
- **`conveyor-context` 是有意删除**（**用户确认**，另一条线在做更好的替代）⇒ 我误读成"我这条线删的"
- **A2 端口撞号已修**（`RESERVED_GEN_PORTS=[3101]` + `allocGenPort` 单射后推），**待一次重启真机验证**
- **B1–B4** 四条异常归「工具完善」；**C** 三条治理纪律（与我的 #16 同族）
- **他们的基线：`check-all` 12/3**（3 红全由 conveyor-context 引起）

### ② "合并"= 调度合并（不是 git 合并）

实测：分支只有 `master`、`git worktree list` 只剩主仓库、本地=远端。
⇒ 合并 = **三条线不再各自开会话，统一由主会话用子代理派活**。
写了 `docs/main-session-consolidation-2026-09-20.md`（当前真相 + 两个真阻塞 + 调度原则 + 待办登记）。

**两个真阻塞（都不在我手里）**：
① **`conveyor-context` 收尾**（那条线）—— 不收尾 ⇒ 3 道门恒红 + **`profiles/web` 起不来 ⇒ 任何重启都失败**
② **一次重启**（用户终端）—— 之后靶场线才能：端口修复真机验证 → 第一对能力开关实验 → 接 `verifyCmd`（M2）

### ③ 派了 2 个子代理（只派不依赖重启的两条线）

- **#1 架构线（完成，已核实）**：`packages/skill-tree/src/text/tokenize.ts` + 单测，
  **主会话真跑 22 pass / 0 fail** ✓；**钉死了口径**（UTF-8 字节长度 / 分隔规则 / 50 stopword /
  jaccard 双空=0 / Tier-2 `>0.5` 与 FindSimilar `>=minJaccard` 是两套阈值 …）
  ★ **它报回 5 处移植时的可疑**，其中两处很要紧：**CJK 永不入 token ⇒ 中文去重 Tier-2 恒失效**、
  **中文 principle → ID 空 → randomSuffix ⇒ 不可复现**（与项目"可复现"纪律冲突）
- **#2 工具完善（还在跑）**：正在改 `scripts/` 9 个文件 + 新增 `capability-store.mjs` / `lib-safe-fs.mjs`
  （B3 原子写 + B4 门自证"删前先判存在"）⇒ **等它报告**

### ④ ★★ 清掉我自己造的垃圾：3 个 0 字节文件已在 HEAD 里

**从目录快照里发现**根目录有三个乱名文件。追查：`git log --follow` 指向 **`f694467`（我 17:48 的提交）**，
mtime 集中在 **17:05:08 的 9 毫秒内**。
⇒ **是我的**：某条命令把中文 Markdown 断句当成重定向目标，生成 0B 空文件；
**17:48 我的 `git add -A` 把它们一起提交进 HEAD** ⇒ **提交后 `git status` 干净，垃圾就隐身了**。

★ **最有价值的发现**：第三个文件真名里的 `*` **其实是【私有区字符 U+F02A】**（码点实证：
`U+72B6 U+6001 U+FF1A U+F02A U+F02A U+8BBE …`）⇒ 所以我后来手打 `*` 去删**匹配不上**；
而且**我的删除脚本在 stat 第三个时崩了，前两个其实没删成** —— 是后来用 `readdir` 拿真名才清干净。
⇒ **已固化进 `MEMORY.md` 环境约束 #17**（长文本走消息文件 / `-A` 后要扫文件名 / 乱名文件查 mtime 与引入提交 /
**提交干净 ≠ 没提交垃圾**）。

### 提交

`70f9751`（子代理 #1 的 L0 层）｜`4ad8ebb`（删 3 个垃圾文件）｜`docs/main-session-consolidation-*.md` 待提。

## 23:50–00:10 子代理 #2 交付有缺陷 → 我验收抓到并修，恢复基线

### 派了 2 个子代理（只派不依赖重启的两条线）

- **#1 架构线**（完成，核实通过）：`packages/skill-tree/src/text/tokenize.ts` + 单测，**真跑 22 pass / 0 fail**；
  钉死口径（UTF-8 字节长度 / 分隔 / 50 stopword / jaccard 双空=0 / Tier-2 `>0.5` vs FindSimilar `>=minJaccard`）
  ★ 报回 5 处移植可疑：**CJK 永不入 token ⇒ 中文去重 Tier-2 恒失效**、**中文 principle → ID 空 →
  randomSuffix ⇒ 不可复现**（与项目可复现纪律冲突）等
- **#2 工具完善 B3/B4**（**交付有缺陷，我修了**）

### ★★ 子代理 #2 的两个缺陷（这正是"验收"的价值）

1. **6 个文件用了 `removeIfExists` 但一个 import 都没加** ⇒ 6 个门全 `ReferenceError` 崩
2. ★ **`test-boot-health.mjs` 的 import 被插进了【多行 import 的中间】**（`import {` 与成员之间）
   ⇒ `SyntaxError: Unexpected reserved word` ⇒ 该门恒崩
   ⇒ 这是"插到最后一行 import 之后"的固有陷阱：**多行 import 的开头行也是"import 行"**
   （我自己的 fixer 脚本有同样的隐患，已删除）

**验收方式（没采信它的自述）**：真跑它改过的每个门 → 4 个报 `Node.js v22.18.0`（未捕获异常退出）
⇒ 去看**完整报错**而不是停在"它说改好了" ⇒ 定位到缺 import / import 位置错。

**修后**：全部 `scripts/*.mjs` 语法自检 0 错；**`check-all` = 12 通过 / 3 失败 = 恢复到基线**；
`test:boot-health` 32/0 等 5 个门全绿。

### 交付内容（`e8ed604`）

- **B4**：新增 `scripts/lib-safe-fs.mjs`（`removeIfExists` = 删前先判存在，幂等不是偷懒）+
  改 6 处调用点 —— 治"宿主 safe-delete 钩子 fail-closed 把门打成崩溃"（回执 §B4）
- **B3**：新增 `scripts/capability-store.mjs`（唯一读写口：`atomicWriteJson` = tmp+fsync+rename；
  `readJsonTolerant` + `formatReadFailure` 读侧容忍半文件）+ 改 3 处
- ⚠️ **未做**：B4 的两方向自证脚本、B3 的读侧容忍测试 ⇒ **标为未实施，不计作通过**

### 两条新纪律候选（已记）

- **删/改文件用 `readdir` 拿真名，别手打字面量**（我遇到文件名里含私有区字符 U+F02A ⇒ 手打 `*` 匹配不上）
- **"插到最后一行 import 之后"是错的**：要先判断该 import 是否多行（以 `{` 结尾且不以 `}` 结尾）

### 当前阻塞（不变，都不在我手里）

① `conveyor-context` 收尾（那条线）⇒ 3 道门恒红 + **`profiles/web` 起不来 ⇒ 任何重启都失败**
② **一次重启**（用户终端）⇒ 靶场线才能继续（端口修复真机验证 → 第一对实验 → 接 `verifyCmd` M2 闭环）


## 23:55 WorkBuddy 内存取证（用户问：「越用越卡，是不是内存泄露」）

**结论：不是泄露。** 两条独立证据链：

- **后端 daemon**：应用自带 `~/.workbuddy/logs/<date>/daemon-memory-diag.log`（每 5s 一条，本日 8649 行，
  覆盖 03:42Z–15:45Z）。12 小时 RSS 在 **190–340MB 区间震荡**，agent 活动时冲 457MB 后 5s 内回落到 361MB。
  `external` 恒 4–12MB、`arrayBuf` 恒 0.0MB ⇒ 无 JS 堆泄露，也无 native/ArrayBuffer 侧泄露。
- **前端 renderer**（pid 14708）：两轮独立采样抓到**完整 GC 锯齿** ——
  `1628 → 1636 → 1043（一次回收 590MB）→ 1203`。**有回落 = 有 GC = 不是泄露**；
  进程数同期 16→13 自行回收，无进程堆积。

**「卡」的真因（三条，均有数据）**：
1. **系统内存被压满**：16GB 物理，但**已提交 18.5GB（> 物理）** ⇒ 走页面文件（已用 1803MB/峰值 3143MB）；
   `Memory Compression` 占 614MB；daemon 日志记录到可用内存**一度只剩 917MB**（08:09）。
2. **常驻基数高且随会话线性增长**：WorkBuddy 13–18 进程 / 3.6–4.5GB。
   **每个 agent 会话 = `cli/bin/codebuddy` + `windows-child-process-containment` 两个进程。**
3. **磁盘膨胀**：`~/.workbuddy` = **2461MB / 60818 文件**（logs 443MB、traces 404MB、
   changes-detail 148MB、file-tree-manifests 52MB 且**单文件 17MB**、workspace 449MB）。
   Crash-Log 10 份**全是** `child_process_crash / signal:15 / exit:1`，**无一份 OOM**。

**★ 方法论（可复用，勿再踩）**：
- **单点内存读数无意义**，必须看时间序列；**有回落 = 有 GC = 不是泄露**，单调不落才是泄露。
- 优先读**应用自带的内存自采样日志**（比外部 tasklist 采样连续得多，且自带 heap/external 分侧）。
- 判「卡」要盯 **已提交内存 (commit)**，不是物理可用内存 —— 后者在换页时仍可能好看。
- 崩溃报告先**分类**再下结论：全是子进程 SIGTERM/exit1 ≠ OOM 证据。
- 端到端定位：`--type=` 列全（renderer/gpu/utility/无 type）+ `cmdline 入口路径` 才能分清
  「Electron 固定开销」与「每会话成本」。

**产出**：`out/workbuddy-memory-forensics.md`（报告）；
脚本 `out/wb-mem-sample.mjs`（时间序列采样）/ `out/probe-wb-procs2.mjs`（进程结构+类型+句柄）/
`out/wb-sizes.mjs`（存储占用）/ `out/wb-diag.mjs`（读 daemon diag 日志 + Crash-Log）/ `out/wb-sysmem.mjs`（commit 内存）。


## 00:05 WorkBuddy 进程构成（追问补测：为何 2 个会话却有 17 个进程）

**进程数不由会话数决定**，三个来源：
1. **Electron 框架底盘 8 个**（main / renderer / gpu / daemon / network / audio / crashpad / edge-sync）
   —— 其中 **renderer 单进程 1.47GB**，是最大头，与会话数无关。
2. **prewarm 预热池**：daemon 日志原文 `cli prewarm pool size=1 source=hardware-default totalmemGiB=15.8`；
   触发条件是 **`input_focus`（光标进输入框就预热）**；`tryAcquire` 命中就用现成的，
   未命中走 `acquire_miss → cold start`；**每被取走一个立刻 `post_acquire` 补一个** ⇒ 实例会累积。
   实测 3 个 codebuddy **全带 `--prewarm`**（用户只开 2 个会话，第 3 个是备胎），每个 ≈270–390MB。
3. **containment 沙箱容器**：每个都是**完整的 WorkBuddy.exe 进程**，固定 **≈106MB / 191 句柄**，
   形态是 `--codebuddy-windows-job-launcher --parent-pid <宿主> -- <真实命令>`。
   **载荷实测**：`bash.exe -c ...`（=每条 shell 命令一个常驻容器）、
   `sheetagent/... node`、`weixinpay/... run-node.cmd`（**插件预热也起容器，与用户会话完全无关**）。

**★ 「一个会话占 18G」是口径混淆**：
- **18.9GB = 全系统提交内存(commit)**（Windows + QQ 738MB + Edge 548MB + Defender 528MB + Docker 321MB + …）
- **WorkBuddy 工作集 = 4.18GB**；其中会话直接相关 ≈1.68GB；**单个会话 ≈0.4GB**
- 高频监视（200s/3s）显示：**16 个进程 200 秒内零退出** ⇒ 容器与池实例偏常驻
- 浪费点排序：renderer 1.47GB（框架固定）> 插件预热容器 4 个 ≈0.42GB > prewarm 备胎 ≈0.3GB

**★ 方法论**：`tasklist` 只能看进程名，**必须用 CIM 拿 `ParentProcessId` + `CommandLine`** 才能分清角色
（renderer / gpu / daemon / codebuddy / containment / 插件容器）并画出进程树；
**命令行载荷是"这个进程在干什么"的最硬证据**，比时间对齐更可靠。

## 00:00–00:30 ★★ 解除全局阻塞：摘掉自研 conveyor-context，改用回上游上下文管理 ⇒ check-all 15/0

### 用户澄清（关键）

「`conveyor-context` 是**我自己做的**（替换 DSH 自带的上下文管理），**后来我把自研那个删了**」
⇒ 所以**要用回上游自带的**；「或者你看看上游开源社区有没有更好的？**没有的话就直接用上游的**」。

### ★ 调查结论：不必去外面找

**本地实测**上游自带的上下文管理是**一整套**：
`dsh-compaction` · **`dsh-compaction-basic`**（★ 有我们 500 行补丁 = O3「兜底后端」）·
`dsh-compaction-tool-result-pruner` · `dsh-command-compact`（外加 `dsh-session-projection`/`-cache`、`dsh-session-query`）。
⇒ **摘掉自研的之后这套仍在链上**（dump 里 compaction 出现 3 处）⇒ **直接用上游即可**。
（从外部找一个压缩系统来替换原生，只会更难集成，且会丢掉我们已有补丁。）

### 改动（P2 安全层，先备份）

- `~/.dsh/profiles/web/package.json`：从 `dependencies` + `dsh.profile.bundles` 各摘掉一项
  （`cordis.patch.yml` 实测**无**引用，未动）
- 新增 `scripts/drop-conveyor-from-profile.mjs`（默认干跑 / 幂等 / 先备份 / 改完立刻验证）
- `scripts/check-all.mjs`：profile 门基线 **582 → 575**
- `scripts/check-config-tolerance.mjs`：PKGS 移除 `conveyor-context`
- 落定 `packages/conveyor-context/` 的删除（用户有意删的）

### ★★ 两处"判据过时"（不是为消红而改）

① profile 门断言行数 —— 582 是**含 conveyor 时**的数，该包已删 ⇒ 不可达；
   575 的正当性由**实质判据**佐证：exit 0 / stderr 空 / pet 0 / dup 0 / conveyor 0 处 / compaction 在链上。
② `check-config-tolerance` 期望 conveyor 产物存在 ⇒ 期望过时。

### ★★ 又踩了一次"拿错尺子"（今天第二次，值得记）

我先按**非空行**数出 **565** 写进基线 ⇒ 门立刻报 `行数 575 ≠ 期望 565`。
**门的口径是 `stdout.split('\n').length - 1`**，不是非空行。
⇒ **教训：改基线的数字，必须用【判据自己的度量口径】（去读判据的代码），别自己另数一遍。**
（第一次拿错尺子是 `wc -c` 字节 vs JS `.length` 字符。）
★ 附带印证：靶场会话的 `exp-base` 变体实测 **575** —— 正是同一口径，他们早就用对了。

### 结果

- `dsh --profile web --dump-config`：**exit 0**（原 exit 1 崩溃）/ stderr 空 / 575 行 / pet 0 / dup 0
- **`check-all`：15 通过 / 0 失败**（本轮起点 12/3）
- 脚本幂等；回退 = 把 `package.json.bak-2026-09-20T16-01-13-619Z` 拷回

### ★ 剩下的唯一阻塞

**一次重启**（用户终端）：`set WEB_PROFILE=exp-base` + `node scripts\relaunch-switchboard.mjs`
⇒ 之后靶场线才能：端口修复真机验证 → 第一对能力开关实验 → 接 `verifyCmd`（**M2 闭环**）。

⚠️ 注：**`profiles/web` 现在装配正常了**，所以"任何重启都失败"这条**已解除**。


## 00:07 WorkBuddy 清理清单（用户要求「列出大占用，我一个个关」）

**★ 关键新发现：每次交互固定新增 ≈600 MB**（实测 00:06:04–00:06:39，仅因执行一条命令触发）：
- codebuddy 会话宿主 +1（274MB，prewarm 池补充）
- 沙箱容器 +2（sheetagent 109MB / weixinpay 107MB —— **插件预热每次都重建**）
- 沙箱容器 +1（110MB，shell 命令自身）
⇒ 这是「越用越卡」的**持续性**来源。**禁用 sheetagent / weixinpay 插件是长期收益最大的单步**
（这两个插件用户并未使用，却每次预热各起一个 106MB 常驻容器）。

**当前快照（00:06:40）WorkBuddy 16 进程 / 4.80 GB**：
框架底盘 7 个 3.00GB（renderer **2021MB** 为最大头，不可动）/ 会话宿主 3 个 1.15GB /
沙箱容器 5 个 0.53GB / 扩展服务 1 个 0.12GB。总量在 8 分钟内从 4.18GB 涨到 4.80GB。

**可操作性结论**：WorkBuddy 内部**不能逐个 kill**（杀框架进程直接崩、杀会话宿主丢当前对话），
只能走「关会话 / 禁插件 / 重启应用」三条路。**能"一个个关"的其实是外部应用**：
QQ 469MB / msedge 548MB / Docker 427MB / PhoneExperienceHost 131MB；
MsMpEng(Defender) 537MB 不建议关，应改为给项目目录**加排除项**。
**重启 WorkBuddy 一次可回收 ≈2.3GB**（renderer 2021→约500 + 宿主 1174 + 容器 538 + 扩展 125）。

**★ 坑：Windows PID 会复用** —— 同一 pid 在不同时刻可能是完全不同的角色。
比对时必须以「**启动时刻 + 命令行**」为准，不能只看 pid
（本次 pid 4088 从 containment 变成 codebuddy，差点误判为「容器被误分类」）。

**产出**：`out/workbuddy-cleanup-list.md`
（A 内部进程逐个表 / B 全系统 TOP20 / C 按收益排序的动手顺序 / D 磁盘占用清理表）。
